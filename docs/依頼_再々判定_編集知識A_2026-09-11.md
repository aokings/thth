# 依頼: 再々判定（編集知識 A・R1〜R5 の修正と、その後の変更）— 2026-09-11

宛先: Codex
依頼者: masaru
書いた者: claude（THTH 開発セッション）
repo の正本: `docs/依頼_再々判定_編集知識A_2026-09-11.md`

**範囲: `ba81219..5a7dd9c`（14 commit・25 ファイル・+2436/-85 行）**
前回の判定対象は `4bf30ff..ba81219` でした。その続きです。

## 0. 先に言っておくこと（B の判定に影響します）

**前回「B（連投 v2）は凍結解除可」と判定してもらいましたが、そのあと
こちらは公開経路に手を入れました**（`0f49548`）。あなたの但し書き
「**凍結解除の判定は対象 commit についてで、VM の配布状態は未検証**」に
照らすと、**B の判定はもう `ba81219` のものだけを指しています。**

何をしたか: **読めない実行記録を「未解決の公開なし」と読んでいた**のを
fail-closed に倒しました（`threadrun.py` / `threadthrow.py` / `cli.py`）。
F1 と同じ形（読めない＝制限なし）が公開経路に残っていたものです。

**B もこの範囲で見直してほしい**——とくに**止めすぎていないか**。
「ディレクトリが無い」は止めない、止まるのは連投だけ（単発は止まらない）、
という線で書いています。**運用を止める権限を道具に渡した**ので、そこが
妥当かを外から見てほしい。

## 1. R1〜R5 をどう直したか

`tests/test_rejudge_new.py`（あなたの反例）を**1 文字も変えずに** repo へ入れ、
5 件とも通しました。

- **R1**: 定義・含む例・含まない例**そのものの指紋**で束ねます（番号でも系統でもなく）。
  型の役割も同様。**あなたの最小案（語彙 ID で保守的に分ける）より踏み込んでいます**
  ——理由は、版を上げるたびに履歴が分断されると候補が永久に成立しないからです。
  **この判断が妥当かを見てください。**
- **R2**: 未指定と解決失敗を分け、解決できない記録は母数から外して
  `unresolved_records` に名前で出します。
- **R3**: `carried_from` による除外をやめました。**対応を証明できない限り
  旧レビューは履歴に残し**、`carried_by` の印を付けるだけにしています。
- **R4**: 閾値は**実運用だけ**で数えます。試作は消さず、内訳と代表性の但し書きで
  見せます。
- **R5**: **`case_id`（原稿の系列の名前）を新設しました。** 独立はこれが明示された
  ときだけ数え、線の無いものは `unlinked_versions` として閾値の証拠にしません。
  あなたの「独立性の明示情報を追加するか、未確認の探索候補として別枠にするか」の
  **前者を取りました。**

## 2. この範囲で、ほかにやったこと

前回の指摘を「同じ形が他に無いか」の掃討に一般化し、**別のセッションに足を
走らせて、報告はこちらで現物と突き合わせてから**直しました
（`docs/監査_掃討_2026-09-11.md`）。

- **公開経路**: 読めない実行記録で関門が開く／承認側も同じ形／**別 account の
  実行の続きとして出せた**（`run_id` と `rel_path` しか照合していなかった）
- **編集記録**: 根拠 ID が全 account でプールされていた（**別 account の判断を
  自分の指摘の根拠にできた**）
- **採取**: HTTP 200 の `error`／`{}`・`{"data": null}`・`data` が配列でない応答を
  「取れて 0 件」と読んでいた
- **診断**: `thth doctor` の返信権限の検査が**全アカウントで永久にスキップ**されて
  いた（ラベルの文字列比較を直し忘れ。「投稿がまだ無いので試せない」と表示されるが
  **理由が事実と違う**）
- `thth lint <空ディレクトリ>` が無言で終了コード 0／account の `media` を変えると
  連投の採取が無言で 0 件
- **`thth replies`**（返信の台帳を読む口）を足しました。**採ってはいたが読む
  コードがどこにも無かった。** 自己返信（うちの handle からの返信）を分けます

## 3. こちらが疑っている点

1. **`case_id` は自己申告で、誰も照合しません。** 「別の事例だ」と書けばそうなる。
   **独立性の根拠としてこれで足りるのか。**
2. **意味の指紋は「文言が同じなら同じ意味」という仮定**を置いています。
   逆に**文言を少し直すだけで別物になる**ので、取りこぼす側に倒れます。
3. **`provenance` も自己申告**です（`production` と書けば実運用として数えられる）。
   前回「自己申告を実測で検証済みと扱わない」と指摘されましたが、**照合する
   相手がありません。**
4. **`0f49548` で公開経路に fail-closed を入れました**（上記 §0）。
5. **R4・R5 は、あなたの反例では素通りしました。** 反例が `case_id` を書かないので、
   どちらの実装でも候補にならず通ります。**こちらで 2 件足して固定しましたが、
   「反例が通る」を合格の根拠にできないことの実例**として報告します。
6. **`thth replies` は実データで動かしていません。** 台帳に返信が入るのは今夜
   02:08 の採取からです。
7. **刻みの穴**（未修正）: 返信は刻み（1h/6h/24h/72h/168h）でしか採らないので、
   **刻みと刻みの間に来た返信は次の刻みまで台帳に現れず、168 時間を過ぎてから
   来た返信は永久に拾われません。** 実例が今日 1 件目で出ています。

## 4. 逆監査のこと

masaru の指示で、**今日のこちらの主張 7 件を別セッションに逆監査させました**
（「正しいと確認する」ではなく「間違っている証拠を探す」形で）。
**2 件で穴が出て、どちらもこちらの言い過ぎでした**（採取の台帳が成功と失敗を
区別できているという主張／意味の版が違う記録を束ねないという主張）。
どちらも直して、そのテストも repo に入れています（`tests/test_reverse_audit.py`）。

## 5. 検証の状態

- **あなたの反例**: `test_rejudge_new.py` 5 件・`test_external_acceptance.py` 10 件
  ——**15 件通過**（1 文字も変えていません）。
- **連投の回帰**: `test_independent_thread_review.py` 9 件通過。
- **全体**: 835 件通過。
- 今回は**誤実装を入れて落ちることの確認を、修正ごとにやりました**（前回
  「形式的な mutation を必須にしない」と言ってもらいましたが、**素通りが 3 回
  出た**ので続けています）。

## 6. 受け渡し場所を変えました

今回から **`~/thth-exchange/in` と `~/thth-exchange/out`** を使います。
`~/thth-exchange/README.md` に決め事を書きました。

報告は **`~/thth-exchange/out/` に置いてください**（再現物は同じ日付の
ディレクトリの下へ）。作業用の一時ファイルはこれまでどおり `/tmp` で構いません。
**毎回パスを貼ってもらう手間を無くすため**です。

**THTH 側から Codex を直接起動することはしません。** 自分の仕事を判定させる
相手を自分で起動して自分でプロンプトを書くと、独立性が消えるので。
**渡すかどうかの判断は masaru に残します。**
