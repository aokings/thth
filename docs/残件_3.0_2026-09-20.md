# 3.0 に向けた残件（2026-09-20 昼・2.10.0 配布済み・2.11.0 実装中）

**2026-09-20 夕: 2.11.0 配布済み。3.0 の設計は `docs/設計_3.0_クローズドのサーバ型_2026-09-20.md`（2.12.0／2.13.0／3.0.0 の 3 段）にまとめた。本書はその材料。**

**2026-09-23 追記: 2.14.1 まで配布済み。App Review は通過。招待者 1 人（kopicha・Mastodon）の通しを実測した（`send_request` 経路。予約公開と削除は同日中に実測予定）。手順は `docs/導入_招待されたら.md`, `docs/運用_招待する側.md` に書いた。残るのは配布物の署名と、C3／C4 の裁定と、X の実投稿。**

3.0 は「masaru 以外の人が、自分で導入して回せる版」。新機能の版ではなく、条件を満たしたことを確かめる版にする（設計 2.11.0 §1・masaru との合意 2026-09-20）。

## 0. 3.0 の条件（これが全部○になったら 3.0）

| 条件 | いま | 満たす版 |
|---|---|---|
| 導入は「`thth auth` が出す URL を開いて承認を押す」だけ。秘密を人も LLM も触らない | **○ 済**（09-23 実測。kopicha が URL を開いて承認。取り違えは `mastodon_identity_mismatch` で止まった） | 2.11.0（Bluesky はアプリパスワードを秘密管理ツール経由・DPoP は先送り） |
| 3 媒体＋X で投稿・承認・採集・観測が同じ規律で動く | **△** Threads／Bluesky／Mastodon は動く。X は adapter まで（2.14.0）。X の実投稿は masaru の App 待ち | 2.14.0（§2-1） |
| Threads の Advanced Access（App Review） | **○ 通過（2026-09-23）** | 済 |
| 管理者が全 account を読める・変更が記録される | **○ 済**（2.9.0。`thth admin log` / `thth board` で 09-23 の通しを照合した） | 済 |
| 版とリリースノートを道具が自分で伝える | **○ 済**（2.10.0。hint の固定は P3 に残す） | 済 |
| 導入文書が 1 枚で、招待された人が読んで通る（サーバ型: 承認を押す＋自分のセッションに資格情報を置く） | **○ 済**（`docs/導入_招待されたら.md` が招待された人の 1 枚、`docs/運用_招待する側.md` が masaru の通し手順） | 3.0.0 |
| 配布物の署名が確かめられる（`board` の「署名: 未確認」が消える） | **× 未着手**。selfupdate は release の先端を取るだけ・署名検証なし | 3.0 前に 1 版（§3） |
| **招待した人が自分の LLM セッションから投稿を出せる** | **○ 済**（2.12.0 の書く口。09-23 に `thth_draft_put` → `thth_approval_request` → 承認ページ → 公開を通した） | 済 |
| **退出**（失効・削除・記録） | **△** `thth account leave` は実装済み（2.12.0）。招待者での実測は同日中 | 3.0.0 |

## 1. いま動いているもの: 2.14.1 まで配布済み（09-23 更新）

2.11.0（auth）・2.12.0（書く口と退出）・2.13.0（画像と動画）・2.14.0（X adapter）・2.14.1（umask と lint の理由・承認ページの Origin）を配布した。09-23 に招待者 1 人の通しを実測した。以下は 2.11.0 当時の記述。

### 2.11.0（当時の記述）

秘密を人が触らない auth。預かり所（thth.me・Durable Object）・Mastodon の自動登録・X の auth・`app set --by`・`token set --stdin` の Bluesky 対応。Claude が担うのは預かり所の deploy（masaru の一言）と監査・配布。masaru が担うのは X の開発者ポータルで App を作ること（急がない）と、配布後の kopicha-mastodon の `thth auth` による入れ直し。

## 2. 3.0 までに要る設計（09-23 に状態を追記）

1. **○ 済: X の adapter（2.14.0 で実装）。ただし実投稿は masaru の X App 待ち**（開発者ポータルで App を作るのは masaru）。以下は当時の記述: 投稿・返信・自分の投稿の実測（views は取れる）・`where`／`mentions`。**費用の裁定が先**（現行の X API は従量課金。読み取りの量をどこまで許すか、masaru の裁定）。auth の骨は 2.11.0 で揃う。
2. **× 先送りのまま。Bluesky の atproto OAuth（DPoP）**: 2.11.0 で延期。client metadata の配信・PAR・DPoP 鍵の管理・全 API 呼出しへの DPoP。Python の依存ゼロを守るなら暗号は `cryptography` の任意依存にするか、標準ライブラリで足りる範囲に限るかの裁定。
3. **据え置き。3.0 には入れない（masaru 裁定待ち）。C3／C4（Meta Platform Terms）**: C3 = 絡んだ他人の枝の 24h／72h 観測、C4 = 語の 1 日 1 回の観測。`docs/照合_MetaPlatformTerms_枝と語の観測_2026-09-18_Codex.md` の保留（生データを保存しない実装が未実装・削除追随の方式が未定義）。App Review は 09-23 に通過したので、規約の照合をやり直して裁定できる状態になった。**3.0 には入れない（masaru 裁定待ち）。**
4. **○ 済（2.12.0 で実装・09-23 に通しを実測）。裁定（masaru・2026-09-20 昼）: 3.0 はクローズドのサーバ型。** 招待制・masaru のアプリ・masaru のサーバ。招待の単位は「人＋媒体アカウント」で `account add --by masaru` の延長。**2.12.0（設計未着手）**: 書く口（`approve`／`send`・queue の置き場）を利用者 scope の HTTPS／MCP に開く（二段承認と digest はそのまま・利用者の資格情報は `serve-reports` の user scope を拡張）・退出（token 失効・台帳と採集データの削除・変更ログ・thth.me の data-deletion と接続）・**X の上限金額を LLM と対話的に設定できる口**（masaru の要望: `thth admin budget x --monthly <額> --by masaru` 相当を CLI と admin MCP に置く。管理者 scope だけ・変更ログに残る・上限に達したら X の読み取りを止めて `cannot_say: budget_exhausted`・実費は X の usage か自前の計数で `admin budget` に出す。既定は 0＝読み取りしない）。
5. **× 未着手。配布物の署名**: tag に署名（GitHub の署名 commit か sigstore）し、VM の selfupdate が検証して `board` に「署名: 確認済み」を出す。小さい版で先に。

## 3. 持ち越した P3（次の版で拾う）

- `tool.notes_root_local_hint` が `~/Developer/thth` 固定（2.10.0 検収）。リリースノート本文を `handoff-report` に同梱するのが素直。
- 新 account の `handoff-report --since-last-read` が `cursor_directory_unavailable` を出す（2.7.0 の残り）。
- `analytics-report` の `cannot_say` コードが事後に付く箇所（2.7.0 の残り）。
- Threads の `collect` inbox（言及の台帳）に Bluesky／Mastodon を入れるか（2.10.0 裁定 2 で「読むだけ」。台帳に入れるなら別の裁定）。
- 2.7.0 の probe の写しは `/private/tmp` 配下で回す（Codex の手順書に固定済み）。

## 4. 運用の作業（コードでなく人）

- **○ 済（09-23）**: masaru の MCP に `serve-reports` の admin scope の資格情報を足す。招待者側の user scope（`writes: true`・`actor`）と起動口・`thth-approval-worker.service` も置いた。手順は `docs/運用_招待する側.md`。
- 各セッションが `handoff-report --since-last-read --mark-read --by <名前>` を実際に打つ（VM に栞が 1 つも無い。栞が無いと 2.10.0 の版の確認も「前回不明」のまま）。
- **○ 済（09-23）**: kopicha-mastodon を 2.11.0 の `thth auth` で入れ直した。取り違えたログインは `mastodon_identity_mismatch` で止まり、何も保存されないことも実測した。
- VM の `thth-collect@<account>.timer` が 8 account とも未ロード（2.9.0 の `admin timers`）。採集が `thth@` の中で回っているのか、collect の timer を有効にすべきかを確かめる（`systemd_gen.py` の設計と VM の実態の照合）。
- 09-20 の実機タグ投稿 2 本の 24 時間後の実測（`after`・`analytics-report --by tag`）と、`thth-notes` への記録。
- **○ 済（2026-09-23）**: App Review 通過。`where`（他人の投稿）と `who`（プロフィール）が Threads で開く。実測はこれから。
- **残り**: X の実投稿（masaru が開発者ポータルで App を作る）。招待者の予約公開と削除の実測（同日中）。

## 5. 順番の案

2.11.0（auth）→ 2.12.0（サーバ型の書く口・退出・X の予算口）→ 2.13.0（画像と動画の投稿）→ 2.14.0（X の adapter）→ 2.14.1（umask と lint の理由・承認ページの Origin）まで済んだ。

**09-23 時点で残るのは 3 つ。**

1. 配布物の署名（小さい版で先に）。
2. 招待者の予約公開と削除の実測（同日中）。
3. X の実投稿（masaru の App）。

C3／C4 は 3.0 には入れない（masaru 裁定待ち）。Bluesky の DPoP も 3.0 の条件ではない。
