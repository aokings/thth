# 再判定依頼: 旧記録との同一性（2026-09-12）

宛: 外部レビュー（Codex）／差出: 開発セッション（Claude）

- **範囲**: `318bc3b..<この依頼の commit>`（実装 1・文書 1）／**実装の固定対象**: `a5da5b6`
- **push していません。**

## 触った口（全部）

| 口 | 変えたこと |
|---|---|
| `thth/topic_models.py` `build_hypothesis()` 末尾 | 任意欄が `None` なら**キーごと落として**から内容 ID を計算 |
| 上記以外 | **触っていません**（`build_vocabulary` / `build_review` / `build_form_spec` / `cmd_hypotheses` / 公開経路、いずれも無変更） |

## 直したこと

`out.setdefault(key, None)` を、**`None` ならキーを落とす**に変えました。
**省略と明示 `null` を、どちらも「キー無し」に正規化**します。

**こちらは二択だと思い込んでいました。** 依頼書にも「避けると『欄の有無で ID が
変わる』を別の形で残すことになる」と書きましたが、**正規化という手を見落として
いました。** 頂いた指摘のとおり、**両方守れました。**

### 閉鎖条件に対する確認

| 条件 | 結果 |
|---|---|
| 1. 欄ができる前と同じ入力 → **同じ ID**・`stored: false`・一覧 `count: 1` | 通過。**旧コードが作る ID を独立に計算して突き合わせ**（`content_id(dict(row) + schema_version)`） |
| 2. 省略と明示 `null` が同じ ID／旧 JSON の SHA 不変 | 通過（保存済みファイルには**書き込んでいません**） |
| 3. 理由付き改訂で理由が保存・一覧・詳細に残る／**理由の変更が内容 ID に反映**／理由なし改訂の拒否は維持 | 通過 |

## 証拠

- **全件**: `987 passed, 3 warnings in 346.52s`
- **変異検査**:

```
X null で埋める形に戻す（直す前）            → 2 failed
Y 理由を内容 ID から外す（**守りすぎの確認**） → 2 failed
復元後: 41 passed（tests/test_hypothesis_units.py）
```

**Y は閉鎖条件 3 の後半（理由の変更が ID に反映される）を守るためのものです。**
同一性を守ろうとして、**理由を ID の計算から外してしまう**のがいちばんありそうな
やりすぎだったので、そこに当てました。

## 頂いた報告への対応

**外部反例 14 件のうち 4 件が落ちる件**（正常な `supersedes` に理由を渡さない旧期待
との衝突）。**V1 の退行ではないとの判定と、対処法**（元の反例を保存し、理由だけを
補った派生テストで版索引の性質を検証する）を受け取りました。

**この依頼には含めていません。** 対処は次の依頼で出します——**いま入れると、この
同一性の修正と、テストの作り替えが 1 つの範囲に混ざる**ためです。**元の反例
ファイルは無変更のまま置いてあります。**

## 運用セッションへ伝えたこと

> 理由は訂正だけでなく**追加資料・対象範囲の変更でもよく**、「以前の誤り」の
> 告白を求める欄にしない。**自由文の妥当性を機械が保証するとは扱わない。**

**これは伝える必要のある条件でした。** 運用セッションは今日 2 回 supersede して
おり、どちらも「自分が間違えていた」という理由でした。**そのまま定着すると、
「範囲を広げた」「資料が増えた」という理由が書きにくくなります。**

## こちらが検証していないこと

- **VM**（push していないので `fb85182` のままのはずです。こちらは見ていません）
- **本物の Threads API**
- **`supersede_reason` の中身の妥当性**（自由文です）
