# v3.1 A 実装検証

基点 8f7e4cf。実装前契約照合は exchange/out/2026-09-18_v31_実装前の契約照合.md。

## A1

根投稿の strict な marks_by_post/by_mark を追加。既存 snapshot 値は変更せず、既存テストは追加キーを除いた再帰的射影で同値確認する。
`pytest tests/test_v31_analysis.py tests/test_analytics_comparison.py tests/test_analytics_report.py -q`: 49 passed。
帯上端 `>=` を `>` に単一置換すると A1 テスト失敗、逆置換後全件成功。ログ exchange/out/2026-09-18_v31_検証/writer/A1-mutation.txt。採集・外部 API は未実行。

## A5

snapshot・比較・施策・刻みの全中央値へ iqr/min/max/spread_reason を追加。既存四分位値は維持。n=1〜3 は IQR null、奇数中心を除く指定算法。min_n 分岐の単一変異でテスト失敗、逆置換後 `test_v31_analysis / analytics_comparison / analytics_report / study_report`: 77 passed。ログ A5-mutation.txt。

## A8

各レポートの cannot_say に code/text を並記。text を固定文に壊す実変異が失敗し逆置換後成功。対象5テストファイル: 85 passed。旧 text キー全面禁止は新契約と衝突するため details 内限定・本文 sentinel 非露出検査へ更新。
独立監査の A1 指摘 marks=[true] を exact-int 検査で修正、全刻みの bool/float/string 否定ケースを追加。

## A2

account 一致・日時/枝宣言一致の絡みのみ root ID 別に集める。相手の直接返信のみ反応とし、未取得 null と取得済み0を区別。本文・username は出力しない。対象 analytics テスト53 passed、復元後 v31 テスト6 passed。直接返信判定 != を == にする単一変異で fail、逆置換復元。ログ A2-mutation.txt。

## A3

採集成功と見送り/失敗/不明を分離。対象 analytics 54 passed、復元後 v31 7 passed。exact-int を isinstance(int) に変える変異で bool-collected が誤成功となることを検出、逆置換復元。A2 の同秒同サイズ pyc 残留を発見したため、変異後キャッシュを除去して復元結果を確認。ログ A3-mutation.txt。

## A6

不明kindを unknown 層に保存し全体件数と照合。CLI/MCP/service の同一契約。86 targeted passed。unknown を lost にする実変異で fail、逆置換+pyc除去後成功。ログ A6-mutation.txt。
独立監査 A2 修正: root 不明行を返信 ID 別の null root に保持。fetch/reply の post_id 照合、id_missing、親不明、作者仮名不正を unknown とする。A3 は mode=collect のみ、同時刻の矛盾成功を last_success_at に採らない。

## A7

collected_at 厳密 cutoff、未到達/未取得 null、成功0件を区別。対象4ファイル83 passed。古い timestamp を collected_at に代用する変異で fail。逆置換は共通部分に2箇所一致したので復元専用の条件行を assert 単一一致して復元、pyc除去後9 passed。ログ A7-mutation.txt。A2 後続不完全fetchを過去成功で隠さない修正、A3 新理由は旧理由の末尾へ追加。

## A9

最後の未成熟投稿+24h の予定を追加、対象外/成熟群は含めない。最終 max を最初 min にする単一変異が fail、逆置換+pyc除去後 test_v31_analysis / study_report: 37 passed。ログ A9-mutation.txt。

## 独立監査追加修正

A7 は cutoff 内最新fetchが不完全なら null、返信はその最後の完全fetch時刻までに限定（last_observed_at と母集団を一致）。A3 は壊れた runs がある場合 last_* を全て null とし、完全な最終試行と断定しない。A6 MCP の by 指定は null/空/不正軸や比較なしを拒否。v31 targeted 10 passed。A7 元の否定ケースを保ち、72h 検証 fixture に対応する完全fetchを加えた。

## A4

保存なし読取、明示 mark-read/by のみ保存、前回との差分、unknown→0のdelta null、symlink拒否を意味テスト。if mark_read を無条件にする実変異で読取テストfail、逆置換+pyc除去後 test_v31_analysis / operations_handoff / report_service: 49 passed。ログ A4-mutation.txt。HTTP/MCPに書込口は追加しない。cursor は max64KiB・重複JSON鍵拒否・通常ファイル・非blocking・原子的保存。

## A4 独立監査: 祖先差替と制約文の修正

祖先を is_symlink で確認した後に絶対pathで再openする方式には差替窓があった。rootから各componentを O_DIRECTORY|O_NOFOLLOW・保持dir_fd相対で開き、mkdirも親dir_fd相対とした。未open祖先の差替は拒否、既にopen済み祖先の名前差替は保持済み実体に留まり outsideへ転送されない。両タイミングの意味テストを追加。O_NOFOLLOWを除く実変異がfail、逆置換+pyc除去後 targeted52 passed。ログ A4-ancestor-mutation.txt。
`--since-last-read` のときだけ制約文をsnapshot差分・全イベント復元ではない旨へ変え、既定呼出の既存制約文は維持した。

## A10 実台帳の読取検証と限界

統括が実行した `exchange/out/2026-09-18_v31_検証/vm-A1-A10-final.json` を実装担当が照合した。2026-09-18 17:37:09 JST、対象実装 `3a3b4eff3d266747e34f5a020692319edf310661`、比較基点 `8f7e4cf754973899a8748dd884fbf3a7738bb606`。VMの Python 3.12.3 に標準入力のメモリsource overlayを渡し、書込・socket・subprocessを拒否するguardの下で既存台帳だけを読んだ。配布済みVMコードを更新した検証ではない。

対象は60日・min_n=5。snapshot / comparison / handoff の旧キー射影を比較し、5account全て差分0、cannot_say_detailsの同順同文/コード形式も一致した。

|account|所有確認済み根投稿|絡み|snapshot 24h views中央値|
|---|---:|---:|---:|
|kopicha-threads|23|1|51|
|nigamilab-threads|8|0|1|
|asmon-kanto-threads|22|0|299|
|masaru-bluesky|0|0|null|
|masaru-mastodon|0|0|null|

kind/hour_band/topicの3軸で各accountの層別母数合計は元の母数に一致。媒体をまたぐ合計は作らない。cursorは不在で changes_since=null と対応理由を返した。327入力ファイルのinode/size/mtime_nsは読取前後で変化0。guardが阻止したのは旧新それぞれのself-update由来git試行（2件）だけ。VM書込・SNS API呼出・追加採集・リリースは行っていない。

限界: Bluesky/Mastodonは対象の根投稿/絡みが0で、欠測viewsをnullのまま出すことは確認できたが、非空実台帳で全機能が同じ形になることは未確認。A9は実採用宣言が無く、実台帳では未実行で合成fixtureの否定ケース3件による検証のみ。A4保存はローカル合成fixtureだけで検証し、VM cursorを書いていない。

コンテナ分離検査 `container-isolation.log`: 同名account/projectの並列2tenantで異なる観測件数1/2、read-only mount、networkなし、git無しcheckoutのimportを確認（passed）。独立入口監査はA4 24/24、A6 15/15 passed。全件通常実行は統括から `2705 passed, 1 skipped, 3 warnings in 125.84s`（rc=0）を受領。隔離Git設定の全件結果は統括の検収依頼に併記する。

このA10 commitは版の正本・server.jsonを2.6.0へ揃えるが、tag/release/PyPI/MCP registry/VMを更新する操作は含まない。

A10仕上げ検証: `test_packaging.py / test_server_json.py / test_cli_help.py / test_docs_links.py`: 27 passed in 5.97s。ローカルwheel/sdist生成・隔離venvへのno-index導入を含み、公開操作なし。
