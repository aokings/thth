# 朝の報告: THTH ナイトシフト（2026-09-26）

前夜 01:00 ごろの masaru「寝ますのでナイトシフトで。最後までやり遂げて」から、朝までの記録。

## 結論

- **3.12.0 はできた。main の `2e04fca`、CI 緑。** Python 7950 passed・1 skipped、Worker 140 件 pass。
- **配布はまだ。** Worker の deploy を打ったところ、自動モードの判定（Production Deploy）で止められた。回避はしていない。release と tag も同じ扱いなので、下の 4 行を masaru が押す。
- 外の人に開く前に、**masaru が決めること**が 3 つある（下の「決めてほしいこと」）。

## 押してほしい 4 行（この順）

Worker が先、VM があと（VM の常駐が Worker の新しい口を叩くため）。

```bash
cd /Volumes/NexDev/Developer/thth/callback && npx --no-install wrangler deploy
```

```bash
git -C /Volumes/NexDev/Developer/thth push origin 2e04fca:release
```

VM が release を拾ったら（数分）、**この 1 回だけ**常駐を手で入れ替える（古い常駐は自分で終わる仕組みをまだ持たないため）。

```bash
ssh wt 'sudo systemctl restart thth-approval-worker && systemctl is-active thth-approval-worker'
```

```bash
git -C /Volumes/NexDev/Developer/thth tag v3.12.0 2e04fca && git -C /Volumes/NexDev/Developer/thth push origin refs/tags/v3.12.0
```

masaru の口座（kopicha・nigamilab・asmon など）の投稿の道は変わらない。3.11 以前の招待の口座（`inv-jiangshi-lab-fce905` など）は `approval: all` 扱いのままで、今までどおり承認ページを通る。

## 3.12.0 の中身（詳しくは `docs/リリースノート_3.12.0_2026-09-26.md`）

1. **承認は口座ごとに選ぶ**（`approval`: `none`／`publish`／`all`）。新しい招待の既定は `none`。`--approval` で指定できる。審査員向けは `all`。
2. **安全装置**。最短間隔（返信には掛けない）、1 日 8 件、削除は 1 日 5 件、10 分に 3 件で自動停止、夜間。LLM から変えられるのは締める向きだけ。止まった口座は LLM からは戻せない。
3. **持ち主の画面 `https://thth.me/activity`**。出た・消した・予約・止まった理由を見て、secret で止める／戻す／予約を取り消す／設定を変える／LLM の鍵を発行する・取り消す。
4. CLI（`thth account set|resume|status`）、MCP（`thth_schedule_request`・`thth_settings`・`thth_account_status`）。
5. 09-25 夜の穴 6 つ。常駐は版が変わると自分で入れ替わる、`/pending/` の末尾スラッシュ、入り直しで戻り続ける件、完了ページの form、運営者の理由表示、umask。
6. `350f865`（連投を型外に数えない）も入っている。

## 決めてほしいこと（推薦つき）

1. **外の人が自分の LLM から MCP につなぐ道が無い。** 今の MCP は stdio で、起動口は VM の上（`ssh <server> thth-mcp-invitee`）。ssh を持たない人は、鍵を発行してもつなげない。
   - 推薦: `https://thth.me/mcp` にリモートの MCP（Streamable HTTP）を置く。Worker が受けて VM へ中継し、鍵で認証する。公開の口が増えるので、夜間には作らなかった。
   - これが無いと「外の人も masaru と同じように使える」が成り立たない。Meta の審査員は `approval: all` の承認ページの道で試せるので、申請そのものは先に進められる。
2. **利用規約と privacy の改訂。** 今は「毎回本人が承認する」と約束している。下書きは `docs/下書き_利用規約とprivacyの3.12改訂_2026-09-26.md`。本体（`callback/public/`）はまだ変えていない。
   - 推薦: 下書きのとおりに改め、施行日を新しくする。**改訂が済むまで、新しい招待（`approval: none`）は作らない。**
   - `llms.txt` の冒頭 "THTH is a human-approval gate…" を変えるかも、masaru の判断。
3. **設計 §9 の夜間の裁定 7 つ**を確かめてほしい（`docs/設計_3.12.0_承認を関所から選ぶものへ_2026-09-26.md` §9）。特に次の 2 つ。
   - LLM から変えられるのは締める向きだけ
   - 新しい招待の既定は `none`、古い招待は `all` のまま

## Meta の申請

- 申請の文（`docs/手順_Meta申請_世間の層_2026-09-24.md` §1〜§2）を 3.12.0 に書き換えた。柱は、持ち主が自分の AI アシスタントで使う・承認は持ち主が選ぶ・安全装置と `/activity`。審査員の口座は `--approval all` で作り、承認ページで試してもらう。
- `threads_keyword_search` は Dashboard で選び直せない（アクションは「アクセスレベルを下げる」「削除」だけ）。集計の保存は、新しい申請の説明の中で申告する。「質問する」は答えを探すだけの窓口で、担当者への問い合わせは却下のあとにしか開かない。
- **録画 0 は、今の撮り方（`--approval all`、運営者が下書きを置く）のままで筋が通るようになった。** 昨夜の素の動画 `~/Movies/thth-appreview-rec0-20260925-235324/review0_raw.mp4`（13 分）は、区間 3 に `write_unavailable` が映っている。そこだけ撮り直すか、全体を撮り直す。道具（`tools/appreview_rec0.sh`）は招待を `--approval all` で作るように直した。3.12.0 の配布のあとで撮る。
- 録画 0b（持ち主の AI アシスタントの流れ）は、決めてほしいことの 1（リモートの MCP）が済んでから撮る。
- 提出の前に、Dashboard で `threads_delete` の API の呼び出しが 1 以上になっているか見る。昨夜の 00:2x に 1 回成功しているので、2 日以内に反映されるはず。

## 片付けていないもの

- 録画用の口座 `inv-jiangshi-lab-fce905` は台帳に残してある（撮り直しに使う）。
- writer の作業場所（`.claude/worktrees/agent-*`）は残してある。
