# 再判定: V1（2026-09-12）

**元の一覧クラッシュと新規登録の型検査は修正されています。ただし、既存の不正型を明示する閉鎖条件に1つ漏れがあり、V1は完全閉鎖には至っていません。** 残るのは読取時の条件1つです。公開処理の問題ではありません。

## 対象

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_再判定_V1.md`
- 入力SHA-256: `0df0758d426e226234fbce49c7ddef618ece0fcbec865a29ab713d1c563b613c`
- 固定対象: `626d812565b60ec4b7489095cfcefd81d7ede890`。
- 範囲: `318082b..626d812`。実際には2 commitsです。`c818af9`は依頼文書、`626d812`が実装修正であり、指定の実装対象は変わりません。
- 受付時の元repo HEAD: `f85d0356132c121831b4917d68bc8943a8467a07`、clean。指定commitの隔離cloneで検証しました。

## V1の残り [P2]: 空の不正型がnullとして黙って扱われる

`thth/topic_cli.py` の版リンクを読むループは、型の検査より先に次を行います。

```python
link = r.get("supersedes")
if not link:
    continue
```

このため、旧レコードの `supersedes=[]` / `{}` / `0` / `false` は型検査へ届かず、`broken_version_links=[]` になります。新規登録ではこれらを拒否するのに、読取では正常な「指し先なし」と同じ扱いです。

主担当が、内容IDを整合させた旧形式レコードを一時storeへ置いて確認しました。

| 保存済みsupersedes | 一覧 | broken_version_links |
| --- | --- | --- |
| null | 正常 | 空（正しい） |
| [] / {} / 0 / false | 正常 | 空（不正型の診断が欠落） |
| 非空の配列 | 正常 | 該当記録の診断あり（修正済み） |

元のクラッシュが残っているという指摘ではありません。前回の「既存の不正型を明示し、黙って正常な版関係として扱わない」という閉鎖条件の残りです。

**最小修正は、読み飛ばす条件を `if link is None:` にすることです。** その後に現在の非文字列の検査を通せば足ります。参照先の実在性や循環検査を追加する必要はありません。

**閉鎖条件:** 上記4種類の保存済み不正値が明示診断され、nullは診断されず、正常な仮説一覧が読めること。既存の非空配列の検査と正常な版関係も維持してください。既存レコードの削除・変更は不要です。

## 確認できた修正

新規の配列・objectを保存前に拒否する方針と、既存の非空配列の版リンクを索引に使わず `broken_version_links` に分ける方針は妥当です。本体が読める旧レコードを一覧から消さず、版関係の診断を別に出すことも問題ありません。

前回の版索引4件とU1/U2の7件はbyte無変更で実行し、**11件すべて通過**しました。追加の独立検査は **2 passed / 1 failed**。失敗は上記の空の不正型の診断だけです。旧非空配列・objectに正常な実在IDを入れた場合には、正常親へ後継を誤って付けず、診断と本体を表示し、保存JSONも不変でした。

## 検証の範囲

主担当が差分、上記の独立fixture、関連回帰を確認し、Sol/highが元の反例と旧レコード混在の独立監査を担当しました。関連回帰は **60 passed in 14.49s**。全958件の成功は実装側の申告であり、今回独立に全件を再実行した数字ではありません。

元repo・VMへの書込、push、本物のThreads API呼出しは行っていません。配布・本番の状態は未検証です。参照先の実在性や循環は依頼どおり対象外です。

**再設計は要りません。nullと空の不正型を区別する条件だけを直せば、この残件を閉じられます。** 再現物と実出力は同名の添付ディレクトリに保存しました。
