# 検収: T1（core ＋ 厚い CLI ＋ 薄い MCP の読み取り系）— 2026-09-09

事実だけを書く。判断や次段の提案は末尾の「統括への報告」節（別途チャットで渡す内容の控え）にまとめる。

## 1. 作ったファイル

```
bin/thth                            # CLI の唯一の入口（argparse）
bin/thth-run                        # systemd 用の薄い bash ラッパ（thth run を呼ぶだけ）
thth/__init__.py
thth/jst.py                         # watchtower/watchtower/jst.py を写した（import せず）
thth/redact.py                      # access_token=/client_secret=/Authorization:/hc-ping.com/ を伏字
thth/lock.py                        # AccountLock（flock 優先・fcntl 無ければ mkdir fallback）
thth/accounts.py                    # accounts/<account>.json の読み・検査・$THTH_ROOT 展開
thth/queuefile.py                   # front-matter 解析・媒体節抽出・文字数（絵文字は UTF-8 バイト）
thth/select.py                      # select の 11 条件（順序どおり）
thth/inflight.py                    # state/<account>/inflight.json の読み書き
thth/runs.py                        # state/<account>/runs-YYYY-MM.ndjson への追記
thth/writeback.py                   # front-matter 3 行書き換え ＋ git add/commit/pull --rebase --autostash/push
thth/adapters/__init__.py
thth/adapters/base.py               # Adapter interface（Post/PublishResult/Reply）
thth/adapters/threads.py            # ThreadsAdapter（base_url 差し替え可能。T1 は偽 API にだけ向ける）
thth/core.py                        # throw_once()。lock 確保・inflight・select・throw・write-back・runs の core 唯一の入口
thth/lint.py                        # thth lint / thth preview の実装（CLI・MCP 共用）
thth/report.py                      # thth queue / thth board の実装（CLI・MCP 共用）
thth/cli.py                         # argparse 本体（lint/preview/queue/throw/run/board）
mcp/server.py                       # 薄い MCP（stdio・JSON-RPC 自前実装）。thth_lint/thth_queue/thth_preview/thth_board の4本
accounts/nigamilab-threads.json     # production: false で commit
systemd/thth@.service
systemd/thth@nigamilab-threads.timer
tests/                              # 下記 2 節参照
```

## 2. テスト

`python3 -m pytest tests/ -q` の結果:

```
61 passed in 10〜12秒程度（環境により変動）
```

テストファイル一覧と対応する受け入れ番号:

| ファイル | 受け入れ | 備考 |
|---|---|---|
| tests/test_lint.py | 1 | umami-bile 正常 + 7 事故（thth欠落／+09:00無し／publish_at壊れ／節無し／501字／status未知／hashtag） |
| tests/test_preview.py | 2 | バイト単位一致・CLI 出力一致 |
| tests/test_charcount.py | 3 | 絵文字境界（🍶=4バイト、500字ちょうど/501字超過、異体字セレクタ） |
| tests/test_queue.py | 4 | draft/approved/posted/型外の集計・次に出るもの |
| tests/test_select.py | 5 | 11条件を1条件1テスト＋正常系2本 |
| tests/test_production_mode.py | 6 | production:false は --production 付きでも dry-run。test_20260907 を含む |
| tests/test_run_missing_env.py | 7 | env/token 無しで run が exit 2・何も書き換えない |
| tests/test_inflight.py | 8 | inflight残存でthrowがexit 1・board表示 |
| tests/test_lock.py | 9 | test_20260909_direct_throw_bypassed_lock・別プロセスがロック保持中に直の throw が弾かれる |
| tests/test_fake_api.py | 10 | 偽API 6種（正常/4xx/5xx/429/timeout/公開成功直後の中断→次回inflight停止） |
| tests/test_roundtrip.py | 11 | bare origin 隔離 clone で本番1巡・front-matter 3行のみ変化・本文不変 |
| tests/test_mcp.py | 12 | 4本のMCPツール・CLI差し替えで出力が変わることを確認・stdio JSON-RPCの実地確認 |
| tests/test_secrets_grep.py | 13 | watchtower tests/test_secrets_grep.py を写す |
| tests/test_redact.py | 14 | redact() 4パターン＋runs/writebackを通ることの確認 |
| tests/test_writeback_rebase.py | (事故) | test_20260901_leftover_worktree_breaks_rebase |
| tests/test_jst.py | (事故) | test_20260828_utc_date_overwrote_previous_day |

`bin/thth`（本物の accounts/nigamilab-threads.json・fixture 相手）を Mac 手元で直接実行し、
`lint`・`preview`・`board --json`・`queue --json` が ssh 無し・トークン無しで動くことを目視でも確認した。

## 3. 詰まった点

- なし（3 回試して止めた事故は無い）。強いて言えば、文字数の「末尾改行 1 個」を含めて
  500 字と数えるかどうかで実装当初テストの期待値がずれた（§4-1 参照）。設計文書を読み直して
  自分で決め、テストを直した。

## 4. 設計と食い違った点・実装で埋めた空白（直さずに記録する）

1. **文字数カウントに含める「末尾改行 1 個」**: `queuefile.extract_section()` は
   §4.1 の指示どおり節の本文を「前後に何も足さない・末尾改行1個」に正規化する。
   この末尾の改行 1 文字を 500 字の文字数カウントに含めている（含めないという解釈も
   ありうる）。501 字フィクスチャは「通常文字 500 字＋末尾改行 1 個＝501」で作った。
   実際に Threads API に投げる text はこの正規化後の文字列そのものなので、投げる文字数と
   検査する文字数を一致させる目的でこの解釈を採ったが、設計書はここまで明記していない。

2. **絵文字の判定基準**: §2.2 は「絵文字は UTF-8 バイト数」とだけ書いており、
   「何を絵文字とみなすか」の基準が無い。`unicodedata.category(ch) == "So"`
   （Symbol, Other）＋地域指示記号（国旗）を絵文字とみなす実装にした。異体字セレクタ
   （U+FE0F、カテゴリ Mn）は絵文字扱いにしていない（例: ☕️ は 4 字＝☕のUTF-8 3バイト＋
   異体字セレクタ 1 字）。本物の Threads のカウント方式とは検証していない（L3）。

3. **select 条件 8 のエラー名**: §3.3 は「媒体の節が無い／文字数超過 → 落として
   `error="too_long(NNN)"`」と 1 文で書いているが、節が無い場合に `NNN` に何を
   入れるかは定義できない。実装は節が無い場合を `error="no_section"`、文字数超過の
   場合だけ `error="too_long(NNN)"` に分けた。

4. **`--now` の意味**: 発注書に `thth throw [--now]` とだけあり、意味の定義が
   無かった。実装は「静かな時間帯・最短間隔（select 条件 6・7）だけを無視して
   今すぐ試す」とした（承認 gate・post_id・型外・文字数等の他の条件は無視しない）。
   受け入れテストの対象外なので動作確認のみ。

5. **`thth queue` の「次に出るもの」**: `select_one()`（いま出せるかどうか）とは
   別に、`report.queue_summary()` の「次に出るもの・いつ」は quiet_hours・
   min_interval・文字数・重複チェックを見ない「予定」の案内にした（post_id 無し・
   approved・publish_at が形式的に正しいものの中で最早）。設計は「次に出るのは
   何か・いつか」としか書いておらず、この粒度は実装が決めた。

6. **`git pull --rebase` に `--autostash` を追加**: §4.3 は `git pull --rebase`
   としか書いていないが、test_20260901（中断が残した未ステージ変更）を通すために
   `--autostash` を付けた。中断者の変更を保持したまま rebase が通ることを
   `tests/test_writeback_rebase.py` で確認している。

7. **ロックの「待つ」か「弾く」か**: 発注書 §5-9 は「待つか『既に実行中』で弾かれる」と
   両論併記だったので、watchtower `wt-run` の flock（`LOCK_NB`）と同じ「即座に弾く」を
   採った（ブロッキング待機は実装していない）。

8. **`accounts.APP_DIR` の隔離**: テストが実物の `accounts/nigamilab-threads.json`
   を汚さずに動くよう、`THTH_APP_DIR` 環境変数（未設定時は `accounts.APP_DIR` を
   使う）を追加した。これは設計に無い実装用の拡張だが、環境変数を設定しない限り
   本番の挙動（`accounts/` は常に app repo 直下）は変わらない。

9. **`thth throw` と `thth run` で production 判定の入力が違う**: `thth throw` は
   CLI の `--production` フラグが立っていないと常に dry-run（台帳が production: true
   でも）。`thth run` は常に `production_flag=True` で `throw_once()` を呼ぶので、
   実際に本番になるかどうかは台帳の `production` 1 つだけで決まる。§3.7 の表
   （`thth throw` は「早く走らせるだけ」、`thth run` が timer の本来の実行経路）と
   整合させる意図でこう分けたが、設計書に明記された分岐ではない。

10. **T3 以降の項目**: `thth run` は T1 では throw のみを行い、collect・refresh は
    未実装（発注 §6 のとおり）。`runs` レコードの `collected`・`refreshed`・`quota`
    は常に `0`／`False`／`null`。

## 5. やらなかったこと（発注 §6 のとおり）

本物の Threads API・トークン・`auth`／`refresh`・replies 採集・insights・healthchecks
ping・systemd の有効化・`production: true` の commit・`approve` の MCP 公開・X アダプタ・
2 つ目のアカウントには一切触れていない。
