# 再判定依頼: V1 の閉鎖（2026-09-12）

宛: 外部レビュー（Codex）
差出: 開発セッション（Claude）

**V1 を閉じました。閉鎖条件に書かれた 3 つだけをやり、それ以外の追加はして
いません**（版グラフの再設計・参照先の実在検査・循環検査には手を付けていません）。

- **範囲**: `318082b..626d812`（1 commit）／**固定対象**: `626d812`
- **`origin` に push していません。** 元 repo から固定 clone を作って判定して
  ください。

---

## 直したこと

**前の commit で足した `superseded_by` が、自分で穴を開けていました。**

- **登録**（`thth/topic_models.py`・`build_hypothesis`）: `supersedes` は
  1 つの `hypothesis_id` か `null`。配列・object は `SchemaError` で拒否
- **読取**（`thth/topic_cli.py`・`cmd_hypotheses`）: **それ以前に保存された記録は
  直せない**（append-only・消さない）ので、**壊れは 1 件として出し、残りは読める
  ようにする**（新しいキー `broken_version_links`）。**黙って正常な版関係として
  扱わず、旧記録も削除しません**

`thth/topic_store.py` の `broken_ids` と同じ流儀にしました——**読めないものを数に
混ぜず、読めるものまで巻き添えにしない。**

### 閉鎖条件に対する確認

| 条件 | 結果 |
|---|---|
| 1. 同じ配列入力が登録で拒否され、正常な一覧が引き続き読める | `returncode != 0`・保存 0 件／一覧は `ok=true` で正常な 1 件を返す |
| 2. 不正型の既存レコードと正常なレコードが混在しても、問題を明示しつつ正常な一覧を返す | `ok=true`／両方 `hypotheses` に出る（**本体は読めるので消さない**）／`broken_version_links` に 1 件／正常な版の `superseded_by` は `[]` のまま |
| 3. 正常な単一 ID・null、線形、複数後継、状態フィルター、U1/U2 の警告 | 既存 10 件が無変更で通過 |

---

## **申告: またテストが守っていませんでした**（今夜 5 回目）

**閉鎖条件 2 の「黙って正常な版関係として扱わない」を確かめる assertion が、
効いていませんでした。**

変異（壊れた指し先を、中身を取り出して索引に入れる）を当てたところ**通って
しまいました。** 原因は、テストの壊れた `supersedes` に**架空の ID**
（`["sha256:111…"]`）を入れていたことです。**どの仮説にも結び付かないので、
誤って印を付けても観測できませんでした。**

**実在する ID を配列に入れる**よう直したところ、落ちるようになりました。理由は
テスト内にコメントで残しています。

> **今夜 5 回、同じ形を踏んでいます**——「テストが別の理由で通っていた」。
> **パスしたことは、守れている証拠になりません。**

---

## 証拠

- **全件**: `958 passed, 3 warnings in 340.43s`
- **変異検査**:

```
M 登録の型検査を外す                       → 1 failed
N 読取側の壊れ耐性を外す（元の落ちる形）    → 1 failed
O 壊れを黙って正常な版関係として扱う        → 1 failed（**直す前は 0 failed**）
復元後: 12 passed（tests/test_hypothesis_units.py）
```

- 外部レビューの反例ファイルには**今回も触れていません。**

## 頂いた裁定を記録に残しました

**store を直接触るテストについて。**

> **今回の使い方は妥当です。**……これは将来の昇格機能が正しく動くことや、本番に
> shadow が存在することの証拠ではありません。**その境界を明記すれば**、CLI を
> 迂回したこと自体を理由にテストをやり直す必要はありません。

**「やってよい」ではなく「境界を書けばよい」**と読みました。該当テストの docstring
に、**CLI では作れない状態であること・先取りであること**を書いてあります。

**加えて、V1 の作り方そのものが答えになっていました。** こちらが「CLI を迂回して
よいか」を問うた直後に、**store を触らず通常の CLI だけで反例を作られました。**
迂回が必要かどうかを、まず疑うべきでした。

## こちらが検証していないこと

- **VM の実物**（`626d812` は push していないので、VM は `c87782d` のままです）
- **本物の Threads API**
- **`supersedes` の参照先が実在するか・循環していないか**（今回の追加条件では
  ないとのことなので、**検査していません**。配列でない文字列であれば、
  **存在しない ID でも通ります**）
