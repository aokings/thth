# リリースノート 3.14.1（遠くの道の `posts` が request_failed になる直し）

2026-09-26。3.14.0 の通し確認（masaru の Mac・`inv-jiangshi-lab-fce905`）で見つけた。

- 手元の CLI（`thth/remote.py`）が、200 の答えに `error` の欄があるだけで断りと読んでいた。`thth posts --json` の形は正常でも `"error": null` を持つので、`posts` だけ `request_failed` になっていた。符丁の文字列のときだけ断りにした。
- 変わるのは手元の CLI だけ。Worker と VM は 3.14.0 のまま（deploy 不要）。`pip install -U thth` で入る。
- 通し確認の記録: `account status`・`mentions`・`queue` は 3.14.0 で通った。`posts` はこの版から。

3.14.0 の「配布のあとに見つけたもの」（`/activity` の値の欄の自動入力・ブラウザ式の `thth login`・masaru の Mac のラッパ `~/.local/bin/thth` と pip の `thth` の同名）は次の版へ。
