# 朝の報告: THTH ナイトシフト（2026-09-27）

09-26 夜の masaru「ナイトシフトでやってください」から、朝までの記録。前の朝の報告（09-26）は `docs/朝の報告_THTH_ナイトシフト_2026-09-26.md`。

## 結論

- **3.14.1 は配布済み**（`389cc04`・PyPI・VM）。遠くの道の `posts` が `request_failed` になっていた 1 行の直し。
- **3.14.2 は main で CI 緑**（`8781ed1`）。ブラウザ式の `thth login`・`/activity` の値の欄・Mac のラッパ・privacy の 1 文。**配布は Worker の deploy（masaru の手・下の 1 行）待ち。** そのあと tag は Claude が押す。
- masaru の手が要るのは **Worker の deploy 1 行**と、そのあとの**通し確認 3 行**だけ。

## 押してほしい 1 行

```bash
cd /Volumes/NexDev/Developer/thth/callback && npx --no-install wrangler deploy
```

出力に `env.LOGIN (Login)` が出れば成功。終わったら「deploy した」と一言。Claude が `release`（VM は変更なし・枝を揃えるだけ）と tag `v3.14.2`（PyPI）を押す。

## そのあとの通し確認（masaru の Mac・3 行）

PyPI に 3.14.2 が出たあと（tag から数分）:

```bash
pip install -U thth
```

```bash
thth login
```

ブラウザで `https://thth.me/login/XXXX-XXXX` が開く（開かなければターミナルの URL を開く）。口座名 `inv-jiangshi-lab-fce905` と口座の secret を入れて「この機械に鍵を渡す」。ターミナルに「保存しました」。鍵の貼り付けは無い。**ラッパ `~/.local/bin/thth` は 3.14.2 の版に差し替え済み**（`login`・`logout` だけ手元の `thth`・それ以外は VM）。

```bash
python3 -m thth account status inv-jiangshi-lab-fce905
```

（`thth account status` はラッパだと VM で動くので `python3 -m thth`。）安全装置の数が返れば通っている。ついでに `python3 -m thth posts inv-jiangshi-lab-fce905`。

## 昨夜やったこと（時系列）

1. 3.14.0 の通し確認（masaru の手）で `posts` だけ落ちた → 手元の CLI が `"error": null` を断りと読んでいた → 3.14.1（`389cc04`）: 直して release・tag。PyPI 3.14.1・VM 3.14.1。
2. 設計 3.14.2（`docs/設計_3.14.2_ブラウザ式のlogin_2026-09-26.md`）を書き、writer（Opus）が実装。Python 7983 / Worker 130 で rc=0。`wrangler deploy --dry-run` 通過。
3. 申請の手順（`docs/手順_Meta申請_世間の層_2026-09-24.md` §2.1）と招待の 1 枚（`docs/導入_招待されたら.md`）を、完了ページの実物の文言（「口座の secret を表示する」「アシスタントの鍵」）とブラウザ式の login に合わせた。
4. privacy に「ブラウザでのサインインでは鍵を最長 10 分預かり、受け取った時点で消す」の 1 文（英日）。施行日は 2026-09-27（deploy が別の日になったら直す）。
5. ラッパ `~/.local/bin/thth` を 3.14.2 の `bin/thth-remote` で上書き（中身は 3.13.0 より前の repo の版のコピーで、手直しは無かった）。

## 決めてほしいこと

無し（推薦で進めた）。裁定した点はリリースノート 3.14.2 の「未決」: 口座に鍵は 1 つ（2 台目で login すると 1 台目は切れる）・許可のあとに期限が切れた稀な場合。

## Meta の申請へ

3.14.2 が配布されたら、申請の文はそのまま貼れる形になっている（§1.1 前置き・§2.1 審査員の手順・§5 録画 0 の台本）。残りは masaru の手: 録画（0 と 1〜10）・Dashboard への貼り付け・招待 3 本の作成（§2.2）。`threads_delete` の呼び出し数が Dashboard に出ているかは未確認。

## 片付けていないもの

- writer の worktree に残る `callback/node_modules` のシンボリックリンク 3 か所（`agent-aea853afcc31d916f`・`agent-a817b0ad658825f9f`・`agent-a31819fa58ad8bc5d`）。`rm` を許可されなかったので手で。
- 3.13.0 の互換の後始末（`RELAY_PUBLIC_KEY` への置き替え・Worker の旧 path `/approval/*` の受け口）は次の版。
- `/activity` の値の欄の自動入力の直しは、実機のスマホで未確認。
