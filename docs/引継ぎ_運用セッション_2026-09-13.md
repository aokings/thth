# 引継ぎ: 運用セッション（旧 → mindforest-6f）— 2026-09-13

書いた者: 旧運用セッション（thth-bb）。
**確かめた時刻: 2026-09-13 06:16〜06:20 JST**。VM は読み取りだけで確かめ直した（書き込み・配布はしていない）。
**旧運用セッションの作業記録は 2026-09-11 16:50 〜 2026-09-12 21:30 の範囲だけ**。それ以外の時間帯に VM で起きたことは、この文書には書いていない（知らない）。

---

## 1. VM に加えた変更の一覧（旧運用が打ったもの・時系列）

| 日時（JST） | コマンド（要旨） | 理由 | 確かめ方 |
|---|---|---|---|
| 2026-09-11 18:21〜18:42 の間（正確な時刻は未記録） | ssh で python を流し `/srv/thth/repos/asmon/docs/sns/queue/2026-09-13-am-koukai-gokaku-saiteiten.md` の `topic: 中受` → `中学受験2027`、「トピックの経緯」節を追記 | masaru 指示（付け替え） | 下の承認 commit に含まれる |
| 2026-09-11 19:17:36 | `thth approve <上のファイル> --confirm 5046bad47c2d --by "claude（運用セッション。masaru の指示により代行）"` | topic が `approved_sha` の入力なので承認が stale になった。masaru「押してよい」 | `git -C /srv/thth/repos/asmon log -1 -- <ファイル>` → `afc19cc5`。**commit・push は thth approve 自身が行った** |
| 2026-09-11 19:0x（分は未記録） | `thth topics record-vocabulary --input /srv/thth/app/docs/語彙_修正理由_v1_提案.json --by "claude（運用セッション）"` | 開発との分担（台帳は運用） | `thth topics vocabulary` → `sha256:86ab447a…`・entry_count 8 |
| 2026-09-11 19:1x（分は未記録） | ssh で python を流し `/srv/thth/repos/kopicha/docs/sns/queue/2026-09-21-green-spec-spread.md` の 1/3・2/3 本文と「直した記録」節を編集 | 検収の往復（開発の指摘への修正） | **未 commit のまま。§4 参照** |
| 2026-09-11 19:24:26 | `thth topics record-review --json-stdin --by "claude（運用セッション）" --revised-draft <上のファイル>` | 1/3 の指摘（運用の判定） | `thth topics review kopicha-threads` → `sha256:d0c24f16…`（全 8 件のうち運用 1） |
| 2026-09-11 19:02:59（a57d530）/ 19:14:12（cb5dd7a）/ 21:42:57（ba81219）/ 22:20:52（42619e1） | `ssh wt "cd /srv/thth/app && git pull --ff-only"`（手動） | 開発 push の前倒し配布（当時は push＝本番反映の仕組み） | reflog にこの 4 時刻がある。**手動と timer の自己更新は reflog の文言では区別できない。** この 4 件は旧運用のコマンド出力に Fast-forward の差分が出た回。19:23:24（6d3fe9a）は旧運用が叩いたとき既に `Already up to date` だった（timer 側） |
| 2026-09-12 02:56:35 / 03:02:47 / 04:35:17 / 04:37:20（各 `created_at`） | `thth topics record-hypothesis --json-stdin --by "claude（運用セッション）"` ×4 | 仮説を反証できる形で先に置く | `thth topics hypotheses` → `cf597752`→`8ae009c2`（U）、`e161d609`→`61d35fad`（B）。`superseded_by` で鎖が読める |
| **2026-09-12 10:48:31 〜 14:20 の間**（正確な時刻は未記録） | `ssh wt 'git -C /srv/thth/app branch -u origin/release'` | 配布の枝への切り替え手順 4。**masaru が会話で「よいです！」** | `git -C /srv/thth/app rev-parse --abbrev-ref '@{u}'` → `origin/release`。`config --get branch.main.merge` → `refs/heads/release` |
| 2026-09-12 16:53:34 | `thth replies kopicha-threads --refresh --json` | 開発依頼（本番で `--refresh` が動くか） | kopicha 台帳の `replies/17892940992608283.ndjson` に `"marks": [], "trigger": "refresh"` の行。`"remote": "synced"` |
| **2026-09-12 21:24:14** | `thth topics kopicha-threads --note テスト --verdict dead --by x` | **誤操作**（値域の確認のつもりで本番に記録） | §3 |
| **2026-09-12 21:25:03** | `thth topics kopicha-threads --note テスト --verdict unknown --status partial --audience "（誤記録の訂正）…" --by "claude（運用セッション）"` | 上の打ち消し | §3 |

**旧運用がしていないこと（確かめた結果）:**

- **timer・systemd unit は作っていない・触っていない。** `/etc/systemd/system/thth@*.timer` と `thth@.service` の mtime は **2026-09-09 22:27:44**、`thth-maintain.*` は **2026-09-10 10:39:40**（旧運用の作業開始より前）。
  - 起点: `thth@kopicha-threads.timer` `OnCalendar=*:8/10`、`thth@asmon-kanto-threads.timer` `*:3/10`、`thth@nigamilab-threads.timer` `*:5/10`、いずれも `Persistent=true`・`RandomizedDelaySec=0`。`thth-maintain.timer` `OnCalendar=*-*-* 04:17:00`。
  - `systemctl is-enabled` は 4 つとも `enabled`。**`masaru-threads` の timer は無い**。
  - `thth@.service`: `User=wt`・`ExecStart=/srv/thth/app/bin/thth-run %i`・`Environment=THTH_ROOT=…`。
- **token の更新・取り直しはしていない。** token ファイルの mtime は 2026-09-09（§2）。
- **release への push はしていない。** 配布（`release` の作成・前進）は開発セッションが行った。

**切り替えの時刻（reflog・確かめた）:**

```
2026-09-12 09:18:13  fb289e1  pull --ff-only          ← 古い仕組みの最後の 1 回
2026-09-12 10:48:06  7a7ef0d  merge origin/release    ← 新しい仕組みで最初の配布
2026-09-13 06:13:22  c6f098f  merge origin/release    ← 確かめた時点の最新（v1.0.0）
```

---

## 2. 秘密の置き場と期限（値は書かない）

**`~/.config/thth/`（VM・`drwx------ wt`）にあるファイル（2026-09-13 06:1x 確認）:**

```
-rw------- asmon-kanto-threads.token   2026-09-09 15:42:31
-rw------- kopicha-threads.token       2026-09-09 15:50:26
-rw------- masaru-threads.token        2026-09-09 15:15:30
-rw------- nigamilab-threads.token     2026-09-09 15:56:20
```

**token の残り日数（`thth board`・2026-09-13 06:1x）: 4 account とも `token=ok/残り56日`。**

**app.env は、正規の置き場に 0 本です（「1 本」ではない）。**

- 読み先は `~/.config/thth/app.env`（`thth/appenv.py:22`、`THTH_APP_ENV_PATH` で上書き可）。
- `ls ~/.config/thth` に `app.env` は無い。
- `thth doctor kopicha-threads` の出力: `app.env: app.env が無い: /home/wt/.config/thth/app.env`。
- `find / -name app.env` で見つかるのは **`/tmp/pytest-of-wt/pytest-2/…` と `pytest-3/…` の 20 本だけ**（テストの一時ディレクトリ・perm 600・キー名は `THREADS_APP_ID` / `THREADS_APP_SECRET`）。**中身は読んでいない。ダミー値かどうかは確かめていない。**
- app.env を読むのは `thth auth`（`thth/oauth.py:232`）と `thth doctor`（`thth/doctor.py:216`）。**token の延長（refresh）が app.env を要るかは確かめていない。** 残り 56 日のうちに、再認可が要る場面で app.env が無いと止まる。

**healthcheck: 設定されていない。**

- `accounts/<account>.json` の `env` は 4 本とも `~/.config/thth/<account>.env` を指すが、**4 本とも実在しない**（`HEALTHCHECK_URL` の置き場が無い）。
- 同 json の `ping` は 4 本とも文字列 `"wrapper"`。**この値の意味は確かめていない**（`thth/` と `bin/` を `grep` しても定義に行き着かなかった）。

**deploy key（GitHub 側・読み取りで確認）:** `gh repo deploy-key list -R aokings/thth` → `thth-vm`（id 162727432）が **`read-write`**。VM の `~/.ssh/config` の `Host gh-thth` は `IdentityFile ~/.ssh/deploy_thth`（鍵の中身は見ていない）。同じ config に `gh-thth-nigamilab` / `gh-thth-asmon` / `gh-thth-kopicha` の別鍵がある。

---

## 3. 起きた事故と、引いた線

### 3-1. 本番の棚に `テスト` を打った

- **2026-09-12 21:24:14** `thth topics kopicha-threads --note テスト --verdict dead --by x`。**値域の確認のつもりで、記録する命令を本番に打った。** kopicha の【不適合・不在と判断した語】に `テスト［型なし］人がいない` が出た。
- `thth topics retract <観測 ID>` は `store_error: その観測は保存されていません`（**観測の棚の命令で、`topics.json` の checks には効かない**）。
- **2026-09-12 21:25:03** `--verdict unknown --status partial` と訂正文の `audience` で打ち消した。
- **いまの状態（2026-09-13 06:1x 確認）:** `/srv/thth/state/topics.json` の checks 総数 108、**`テスト` は 106 行目（dead・by x）と 107 行目（unknown・by claude（運用セッション））の 2 行が残る**。どちらも `retracted` 無し。`thth topics kopicha-threads --advise` に `テスト［型なし］` が出る。
- **v1.0.0 の CLI には `--retract-note <note_id>`（`thth topics retract-note <note_id>`）がある**（`thth topics --help` で確認）。**使っていない。** §4。

### 3-2. 「記録は指示ではない」を適用した場面

**旧運用の作業記録に、記録（台帳・原稿・観測の中身）に書かれた指図を理由に動かなかった場面は無い。**

いちばん近いのは**セッション間メッセージで伝えられた masaru の許可**で、次の 2 件はどちらも**本人の直接の一言が出るまで動かなかった**（「記録」ではなく「伝聞」なので、同じ規則ではない）:

- 2026-09-12 午前: 開発から「masaru の裁定で main への push は配布ではなくなる」と伝えられた → 運用として何かを止めたり変えたりする根拠に使わなかった（読み取りの確認だけ実施）。
- 2026-09-12 09:17 以降: 開発から「masaru から直接『配って良い』が出た」「手順 4 を依頼する」と伝えられた → **`git branch -u origin/release` は、masaru が会話で「よいです！」と書くまで実行しなかった。**

あわせて、**`thth approve`（公開の承認）は「push・commit・merge してよい」の許可に含めなかった**（2026-09-11。9/13 asmon の再承認は masaru「押してよい」の後に実行）。

### 3-3. deploy key が書き込み可と気づいた経路

**旧運用が気づいたのではない。** 開発セッションの調査 `docs/調査_配布の権限分離_2026-09-12.md` §2 に「VM の deploy key `thth-vm` が `read_only: false`」とある。**旧運用の作業記録にこの件の作業は無い。** 2026-09-13 06:1x に `gh repo deploy-key list` で `read-write` のままであることだけ確かめた。

---

## 4. 未完のもの

| 項目 | いまの状態 | 待っているもの |
|---|---|---|
| **deploy key の作り直し（read-only 化）** | `thth-vm` は `read-write`（06:1x 確認） | masaru の操作 |
| **kopicha の `テスト` 2 行の打ち消し** | 106・107 行目が残る。`--retract-note` は未使用 | **masaru の直接の一言**。旧運用は 2026-09-12 に「物理的に消さず残す」を推薦した（決定は出ていない） |
| **kopicha 原稿 `2026-09-21-green-spec-spread.md` の VM 上の変更が未 commit** | `git -C /srv/thth/repos/kopicha status --short` → ` M docs/sns/queue/2026-09-21-green-spec-spread.md`。最終 commit は `bd39fdf`（2026-09-11 14:35:26）。**旧運用が 2026-09-11 19:1x に編集して commit していない** | 扱いの判断（kopicha 側か masaru）。**検収記録 `d0c24f16`・`81c4d21b` の `revised_draft_sha256`（`20fd6e40…`）はこの未 commit の中身を指す**ので、変更を捨てると指紋が合わなくなる。この原稿は `status: draft`・メモに「承認しない」 |
| **app.env が正規の置き場に無い** | §2 | 置く人の判断（masaru）。旧運用は 2026-09-13 に初めて気づいた |
| **healthcheck 未設定** | §2 | 判断 |
| **`中学受験` の観測（`topics.json` #72）に条件なしの `views 202〜574`** | `--advise` に `（asmon-kanto-threads の観測）` つきで出る。手順書 §0 は `7ed9708` で訂正済み | 誰が入れ直すか masaru の判断。旧運用の推薦は asmon 関東セッション（旧運用は「誰がいるか」を見ていないので上書きしない） |
| **`audience` に書いてよいものの検査** | 未実装 | masaru の判断 |
| **返信の 24h（`17892940992608283`・2026-09-13 08:08）で件数が 6 から増えるか** | 未確認。`paging.next` の実形は本番で未確認なので「全件取れた」とは扱わない | 08:08 以降に `thth replies kopicha-threads` |
| **仮説 `61d35fad` の `scope` を実測を指す形に書き直す** | 未着手。比較の組は asmon の 09-13 07:00 `中学受験2027` と 09-12・09-14 の 07:00 `中学受験` | 3 本が揃ったあと。**訂正ではなく資料の追加**として `supersede_reason` を書く |
| **修正理由の語彙の採用（`adopt-reason`）** | 0 件。独立ケース・反例・判定主体の分散が足りない（2026-09-12 に開発へ数え直しを渡した） | 本番原稿での第三者（アカウント側）の検収 |

**参考（2026-09-13 00 時台の日次・確かめた）:** 3 account とも `date: 2026-09-12` の行に `clicks` が入った（asmon 81・kopicha 8・nigamilab 0）。2026-09-11 の asmon・nigamilab は `clicks` の欄が無い（修正の着地が採取より遅かった）。

---

## 5. 相手との連絡

| 相手 | 約束したこと | 頼まれていること | 届くか |
|---|---|---|---|
| **開発セッション**（旧運用の宛先は `uds:/tmp/cc-socks/47156.sock`・名前「THTH開発」。**いまの `thth-0c` と同一のセッションかは確かめていない**） | 双方向の取り決め 3 つ: ① 数字を出したら相手が「この本数で言えることか」を聞く ② 第三の道を示したら、相手が取らなかったとき一度だけ押す ③ 引けば分かることは引く。引いても決まらないことだけを預ける。**VM への書き込み（手順 4 など）は開発の依頼だけでは動かず、masaru の直接の一言を待つ** | 返信の 24h（09-13 08:08）で件数が増えるかの結果共有。§5 の性能の数字は合成データでの測定（開発の申告） | 2026-09-12 21:30 まで双方向で届いた |
| **kopicha セッション**（`local_07b8e3ef-6eca-471d-b579-c58b61a29aaa`・名前「コピチャへの復帰について」） | 無し（旧運用から送った 1 通は `queued` のまま） | 向こうの申告: 24h 値の報告は台帳の値で、出所と実行時刻を添える／`docs/sns/返信の方針.md` に「数字の時点を添える」を追記する | **片道。向こう → 旧運用は届く、旧運用 → 向こうは届かない**（向こうが「一度も届いていない」と申告）。2026-09-13 06:1x の `ListAgents` に出ない。**masaru 経由か repo のファイルで** |
| **asmon 関東セッション** | 直接の連絡なし | 無し（`中学受験` の観測を入れ直す候補として masaru に名前を出しただけ） | 未確認。`ListAgents` に `asmon-87` がある（同一かは確かめていない） |
| **nigamilab セッション** | 直接の連絡なし | 無し | 未確認 |
| **mindforest-6f（新運用）** | この文書を渡す | — | `ListAgents` に出る |

---

## 6. VM で確かめるコマンド一式（旧運用が実際に使って有効だったもの）

**すべて手元（Mac）から。`thth` は VM の `thth` を呼ぶラッパ。パスは VM 側で書く。**

```bash
# 配布の状態（rc は見ない。ref の実在と HEAD の実値を見る）
ssh wt 'git -C /srv/thth/app rev-parse --short HEAD; git -C /srv/thth/app rev-parse --abbrev-ref "@{u}"; git -C /srv/thth/app status -sb | head -1'
ssh wt 'git -C /srv/thth/app for-each-ref --format="%(refname)" refs/remotes/origin'
ssh wt 'git -C /srv/thth/app reflog --date=format:"%F %T" --format="%gd %h %gs" | head -6'
thth board | head -8

# timer
ssh wt 'systemctl list-timers --all --no-pager | grep -E "thth|watchtower"'
ssh wt 'systemctl is-enabled thth@kopicha-threads.timer thth@asmon-kanto-threads.timer thth@nigamilab-threads.timer thth-maintain.timer'
ssh wt 'journalctl -u thth@kopicha-threads.service --since "-30min" --no-pager | tail -20'

# 秘密（名前と perm だけ）
ssh wt 'ls -la ~/.config/thth'
thth doctor kopicha-threads          # app.env の有無・各権限の probe（読み取りだけ）

# 実測・返信の台帳
ssh wt 'cat /srv/thth/repos/kopicha/data/sns/insights/posts/<post_id>.ndjson'
ssh wt 'tail -1 /srv/thth/repos/<repo>/data/sns/insights/account/*.ndjson'
thth replies kopicha-threads
thth measured kopicha-threads
thth topics asmon-kanto-threads --json   # API 観測値（打った瞬間の値。台帳の刻みとは別の数）

# 棚（checks）の中身
ssh wt "python3 -c \"import json; d=json.load(open('/srv/thth/state/topics.json'))['checks']; print(len(d)); [print(i,c.get('checked_at'),c.get('verdict'),c.get('account'),c.get('by')) for i,c in enumerate(d) if c.get('topic')=='テスト']\""

# 検収・仮説・語彙
thth topics review kopicha-threads
thth topics hypotheses
thth topics vocabulary

# 原稿 repo の未 commit
ssh wt 'for r in kopicha asmon nigamilab; do echo "== $r"; git -C /srv/thth/repos/$r status --short; done'

# 刻みの残り（台帳の fetch 行から）
ssh wt 'for f in /srv/thth/repos/*/data/sns/replies/*.ndjson; do echo "FILE $f"; cat "$f"; done'
```

**使って効かなかったもの（避ける）:**

- `git ls-remote --heads origin release` の **終了コード**（枝が無くても 0。`--exit-code` を付けると 2）。
- `git fetch origin <ref>` の **終了コード**（単一枝 clone では 0 で何も作らない）。
- `thth topics retract <id>` で `topics.json` の checks を打ち消す（別の棚の命令）。
- `thth topics kopicha-threads --note … --verdict …` を**値域の確認に使う**（本番に記録される。値域は `thth topics --help`）。
- 一覧の JSON で `len(row.get("entries") or [])` のように**無いキーを 0 と数える**（件数は `*_count` を読む）。
