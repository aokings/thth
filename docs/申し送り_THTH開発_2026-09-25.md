# 申し送り: THTH 開発（2026-09-25 夜）

前のセッション（Opus 5.5・Agent Mail 名 DustyCave）から、次のセッションへ。

## 最初に読む順

1. この書
2. Claude の記憶 `thth-design-status`（先頭の「09-25 夜（セッション移行時の引継ぎ）」の段）と `MEMORY.md` の索引
3. `docs/計画_Meta申請_2026-09-25.md`（段取り 9 つ）と `docs/手順_Meta申請_世間の層_2026-09-24.md`（申請の文・録画台本）
4. `docs/見通し_Meta審査_2026-09-24.md`（なぜテスターで確かめるのか）
5. 最新のリリースノート `docs/リリースノート_3.11.1_2026-09-25.md`

## 現在地

- **配布済みは 3.11.1**（`4d5bf15`・release・tag v3.11.1・VM と PyPI とも 3.11.1・Worker には招待と承認待ちの一覧を deploy 済み・privacy と利用規約は 2026-09-25 施行）。
- **main に未配布の直しが 1 つ: `350f865`**。連投（`thth: 2`）の原稿が、board と queue で「型外」に数えられていた（Agent Mail #43・BrownBeaver・nigamilab の `2026-09-20-broccoli-genes.md`）。数えるときだけ `bundle.readable_from_queuefile` で読み直し、読める連投は状態の数に入れ、queue に `bundles` を出す。壊れた連投は従前どおり型外。Python 全件 7889 緑・単変異 1 本殺。**リリースノートは未作成・版は 3.11.1 のまま**。3.11.2 として出すかは次のセッションが決めてよい（急がない。BrownBeaver は「当面承知で運用」と言っている）。
- **Meta 申請**: masaru が Threads 口座 `sometokagaku` と `jiangshi_lab` を作った。VM に `sometokagaku-threads`（project sometoka・repo `_none`・redirect_uri `https://thth.me/callback/`）を登録済み。前に出した認可 URL は期限切れ。**masaru がテスター招待と承諾をする番**（手順は下に）。

## 決めたこと（masaru の決定・日付つき）

- 09-24 夜: 外の人も masaru と同じように使えないと意味が無い → 次の申請は 1 回に束ねる（案 2: basic・content_publish・manage_replies・read_replies・manage_insights・delete の Advanced＋承認済み 4 つの説明更新・集計 180 日も入れる）。審査員は招待リンクで入る。
- 09-24 夜: 公開βで 2 回目の審査を呼ばないよう、説明に「招待制」と書かない（「運営者が各アカウントを確かめてから有効にする」）。
- 09-25: jiangshi_lab は 3 つ目の口座を作らず、審査員の流れの録画に使う（招待リンクで入る→撮る→`thth account leave`→媒体として正式に登録）。
- 09-25: 9/26 13:59 JST まで会社アカウントは軽い仕事だけ（Agent Mail #156・週の枠 95%）。サブエージェントを並べる仕事はそのあと。
- 常設: 裁定は結果が大きく割れるときだけ問う。release/tag の push は CI 緑のあと Claude が押してよい。

## 残り（優先順）

1. **masaru が「準備できた」と言ったら**: `ssh -tt wt 'PATH=$HOME/.local/bin:$PATH; PYTHONUNBUFFERED=1 thth auth sometokagaku-threads --by masaru'` を背景で起動し、出た URL を masaru に渡す（10 分で切れる）。済んだら `thth doctor sometokagaku-threads` で、テスターに審査前の権限（content_publish など）が付いたか見る。
   - **付いた** → テスターの認可は Live 後も効く。jiangshi_lab の録画へ（`thth admin invite create --media threads --project jiangshi-lab --production --by masaru`）。
   - **付かない** → すぐ全セッション（hub と Agent Mail）に「既存の Threads 口座を認可し直さない（投稿が止まる恐れ）」を知らせる。
2. 各権限を申請の 2 週間前までに 30 日以内で 1 回以上呼ぶ（計画 §段取り）。Dashboard の App Purpose「Clients」と削除コールバック URL の確認は masaru と画面を見ながら。提出の目安は 10 月中旬。
3. `350f865` を 3.11.2 で配るか決める（出すならリリースノート→版上げ→CI→release→tag）。

## 待っているもの

- **masaru**: Meta でテスター招待（My Apps → THTH →「アプリの役割」→「役割」→「メンバーを追加」→「Threads テスター」→ `sometokagaku`、同じく `jiangshi_lab`）と、各 Threads 口座での承諾（設定 →「アカウント」→「ウェブサイトのアクセス許可」→「招待」→ 承諾）。9/25 夜の時点で masaru は手が離せない。
- Agent Mail: 未読なし（9 件とも ack 済み。#53 は #168 で取り下げ＝対応不要）。

## 触ってはいけないもの・決まり

- 選択肢カードの UI（AskUserQuestion）は使わない。問うときは番号付きの地の文。
- 秘密は会話にも repo にも出さない。承認 secret は tty でだけ・Claude の Edit/Write で秘密のファイルを触らない。Cloudflare は `~/.local/bin/cf-api`（値を出さない）。
- `git add` はパス明示（`-A` を使わない）。`pytest | tail && commit` の形にしない（rc を取ってから進む）。
- main への push は本番を変えない。release を進めると VM が拾い、tag で PyPI と registry に出る。callback/ を変えたら Worker の deploy（`cd callback && npx --no-install wrangler deploy`）。
- 試験に「— 下書き」の題・「deploy が要る」の文言・施行日を固定しない（版上げで落ちる）。
- 運用セッション（kopicha・asmon・nigamilab）のレーンは直さない。kopicha・asmon-kanto・nigamilab・masaru はすべて masaru の持ち物。

## Agent Mail

- 名前 **DustyCave**・合言葉のファイル `~/.config/agent-mail/thth.json`（600）。
- 送る・返す・ack: `~/tools/mcp_agent_mail/.venv/bin/python /Volumes/NexDev/Developer/media-hub/hub/agentmail/am.py --as thth reply|ack|send …`。読む口は am.py に無いので、fetch_inbox を registration_token 付きで呼ぶ小さなスクリプトを scratchpad に書く（値は出さない）。
- 見張り（必須）: Monitor に `/Volumes/NexDev/Developer/media-hub/hub/agentmail/watch_inbox.sh DustyCave`（30 分で切れたら張り直す）。前のセッションの見張りは止めてある。
- hub status post には `--title` を毎回・`--am DustyCave`。
