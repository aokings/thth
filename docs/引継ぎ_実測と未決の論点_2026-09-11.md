# 引継ぎ: 実測と、まだ決まっていない論点

2026-09-11 16:50 JST 時点。**数字は VM の台帳を直接読んで確認した**（記憶からではない）。

## 1. 実測（`/srv/thth/repos/*/data/sns/insights/posts/*.ndjson`）

**採取は動いている。**1 時間・6 時間の目盛りは入っている。24 時間がまだ。

| account | topic | 型 | 公開 | 1h views | 6h views | いいね |
|---|---|---|---|---|---|---|
| asmon | `学校説明会` | 行動 | 9/11 07:03 | **164** | **323** | 1 |
| kopicha | `コーヒー` | 一般名詞 | 9/11 08:08 | 5 | **19** | 0 |
| kopicha | `お茶` | 一般名詞 | 9/11 12:38 | 5 | （未） | 1 |
| nigamilab | `秋の味覚` | 一般名詞 | 9/10 19:05 | 4 | **5** | 0 |

**24 時間の目盛り**: `秋の味覚` は 9/11 19:05、`コーヒー` は 9/12 08:08、`お茶` は 9/12 12:38。

THTH が公開した投稿は**これで全部**（asmon 1・kopicha 2・nigamilab 1）。
`masaru` アカウントに 1 本（`17916074118445631`・topic なし）あるが insights が無い。

## 2. 私（Claude）の過去の主張のうち、台帳で裏が取れなかったもの

**「`中学受験` は 202〜574 views、トピック無しは 1 view、約 400 倍」**——
この数字は **THTH の台帳に無い。** asmon の insights は `学校説明会` の 1 本だけで、
ディレクトリの作成が 9/11 08:03。**それ以前の数字はセッションの口頭報告**であって、
機械が採ったものではない。

**規約 12（事実そのものを確かめる）に照らして、この数字を根拠に使わないこと。**
使うなら asmon セッションに出典を出させる。

## 3. いちばん大きい未決の論点: 「日常の一般名詞を選ぶ」は誤りか

**訂正（2026-09-11 18:40）**: 初版は出典を `docs/手順_LLM_トピック選定.md` と
書いたが、**誤り。実際は `thth/cli.py:994`**——**道具が毎回印字する助言**で、
文書より届く範囲が広い。文言はそのまま:

> 人がいまやっている行動の名前か日常の一般名詞を選ぶ。

**同じ箇所の `cli.py:996` も台帳と食い違っている**（下記）。

**これに反する証拠が 2 本、独立に立っている。**

**証拠 A（観測・2026-09-11 実地）**: `コーヒー`・`料理`・`秋の味覚` のトピック頁の
「最近」タブに、**タグ付き投稿が 0 件**だった。頁が大きいのに誰もタグとして使っていない。
一方 `中学受験` の最近タブにはラベル付きが 4 アカウント並ぶ。

**証拠 B（実測・上表）**: 行動の名前 `学校説明会` が 6h で 323、
一般名詞 3 つが 5〜19。

### ただし、まだ確定できない（交絡）

**account が違う。**asmon と kopicha ではフォロワー数も読者層も違う。
**「トピックの型の差」なのか「アカウントの地力の差」なのかが分離できていない。**

**決着させる実験**（同一 account 内で比べる）:

- kopicha の次の原稿を、**タグとして実際に使われている語**にする
  （頁の最近タブでラベル付き投稿を確認してから選ぶ）。
- 同じ account・同じ公開後経過時間で `コーヒー` と比べる。
- 差が出れば型の効果。出なければアカウントの地力。

**この実験が回るまで、助言そのものは書き換えない。**
ただし「一般名詞なら安全」と読める現在の文面は**すでに危険**なので、
**確かめる手順を先に足す**（§8 の「論理的不変条件は反例 1 件で直す」に当たる）。

### `cli.py:996` は台帳と食い違っている（初版で見落とし）

> 3. 自分で作った語・「〜と繋がりたい」型は Threads では場になっていない。

`topics.json` の 2026-09-11 15:36 の訂正で
`コーヒー好きな人と繋がりたい` は **`alive`**（タグとして実際に使われている）。
**「〜と繋がりたい」型が場になっていないというのは、うちの台帳が否定している。**
`自作語が 0 件`（`中学受験算数` ほか 8 語）は台帳のとおりなので、**2 つを分けること。**

## 4. 記録の訂正が必要なもの

**済み（2026-09-11 15:36 に nigamilab セッションが訂正済み・初版の記載は古い）。**
`topics.json` は後の行が勝つ（`thth/topics.py:146`）ので、引くと訂正後が返る。
**作業は不要。** 以下は経緯として残す。

**`コーヒー好きな人と繋がりたい` を `dead` として記録したのは、事実として誤り。**

nigamilab セッションが views=1 を根拠に `dead`（人がいない）と記録したが、
**実地調査でこのタグは 22 時間前に実際に使われていた。**
正しくは「人はいるが、うちの 1 本は伸びなかった」。
**`dead` は観測（人がいるか）、views は結果（うちが伸びたか）で、別の事実。**
`/srv/thth/state/topics.json` の `checks` に入っている。入れ直しが要る。

## 5. `秋の味覚` の事前判断が「不一致」だった経緯

原稿: `/srv/thth/repos/nigamilab/docs/sns/queue/2026-09-10-umami-bile.md`
**理由は原稿の「経緯」節に書いてあり、THTH の判断ストアには入っていない**
（このトピック選定は `thth topics` が使えるようになる前のもの）。

原稿に残っている理由:

> トピックは「秋の味覚」。「苦味」はトピックとして検索結果が無く（2026-09-10 実測）
> 人がいないので、苦味の先にいる人の多い話題に乗せる。サンマの旬に合う。

**この理由づけは、いま見ると論点を 1 つ飛ばしている**——
「苦味に人がいない」から「秋の味覚に乗せる」へ移るとき、
**秋の味覚をタグとして使っている人がいるかを確かめていない。**
実地調査（9/11）では 0 件だった。**証拠 A がまさにこれを指している。**

なお masaru から「理由が二度書き直しになった」と聞いているが、
**書き直しの経過は原稿にも台帳にも残っていない**（最終版だけがある）。
経過が要るなら nigamilab セッションに出させること。

## 6. masaru の判断待ち

**asmon の探索枠を `中受` から `中学受験2027` へ替えるか。**
対象は `2026-09-13-am-koukai-gokaku-saiteiten.md`（現在 `topic: 中受`）。

私は賛成だが、**非対称を明示して渡すこと**:

- `中受` … 実測はこれから／観測は薄い
- `中学受験2027` … 観測は厚い（asmon 関東セッションが全件ラベル付きを確認）／**実測はゼロ**

**これは 3 度目の付け替え**になる。付け替えるたびに前の語の実測が積み上がらない。

## 7. 予定（台帳で確認済み）

- asmon: 9/12〜9/14 に 3 本/日、ほぼ `中学受験`（`中受` が 9/13 朝に 1 本）
- kopicha: 9/15 に `コーヒー`（`coffee-belt-origins`・判断は記録済み `9c97fe09662c`）
- timer は 3 account とも稼働中（10 分間隔）、maintain は毎日 04:17
