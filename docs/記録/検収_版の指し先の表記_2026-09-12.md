# 検収結果: 版の指し先の表記

**判定: 合格。この修正は閉鎖してよい。コードをrevertする技術上の理由は今回の検証で見つからなかった。** 前回閉鎖済みのV1を再開するものではない。

## 対象と証拠の範囲

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_検収_版の指し先の表記.md`
- 入力 SHA-256: `118657346ddd51a6a2328d6d5b7102d6c54cc4375b014cec2a835143bba0e03f`
- 範囲: `1a31500..fb85182`
- 固定対象: `fb851825505e31031d17b0502880a8ed86b22249`
- 受付時の作業repo HEAD: `05f682bc1c612612825dc344b23219b7afb4b200`、clean。固定対象の隔離cloneで実行。
- 主担当は差分と独立CLI反例を確認。Terra/medium担当は既存反例の同一性と関連回帰を機械検証し、主担当がログを照合した。

## 確認結果

登録と仮説一覧の読取は、ともに `None` または `_REF_ID_RE.fullmatch` を満たす文字列だけを正常として扱う。短絡評価で非文字列を正規表現に渡さず、nullだけを診断対象から外す。存在しないが表記が正しいIDも受理し、実在性・循環の検査は追加していない。

主担当の独立CLI probeは13入力を実行した。nullと正常形式の不存在IDは登録成功・診断なし。空文字、空白、末尾改行、先頭空白、大文字表記、65桁、全角数字、配列、辞書、0、falseは登録拒否。旧仮説として保存すると各1件を診断し、一覧は正常終了、保存JSONのSHA-256は読取前後で不変だった。語彙登録にも同じ13入力を通して結果が一致した。

同じprobeを修正前 `1a31500` に変更せず当てると、空文字が `stored: true` となって失敗した。今回の追加検査を実際に捕まえる反例である。

依頼文には明記されていないが、`build_vocabulary()` の登録制約も同時に狭めている。この変更も差分として拾い、独立CLIと関連テストで確認した。新たな不合格項目にはしないが、今後の変更報告には仮説・語彙の両方を記載してほしい。語彙一覧に仮説と同じ診断機構を追加することは今回の受け入れ条件にしない。

## 実行した検証

| 検証 | 結果 |
|---|---|
| 前回反例14件（3ファイルのSHA一致）＋hypothesis_units32件＋topic_models/topic_cli | 95 passed / 15.27秒 |
| review_records / review_cli | 54 passed / 10.47秒 |
| 主担当独立CLI・旧記録読取・JSON不変 | 13入力すべて合格 |
| 同一probeを修正前に実行 | 空文字登録で期待どおり失敗 |

全件978件・変異検査3本は開発側の申告。本レビューでは全件を再実行せず、上記の限定回帰と独立反例を根拠にした。

## 判定前pushについて

「判定を待つ」と決めていたのなら、設計への一致やテスト件数だけでその条件を外す判断は適切ではない。ただし、手順違反だけを理由に今回の修正をrevertすることは求めない。固定対象のコードが合格し、戻す技術上の必要が確認されないためである。今回合格したことを、今後も判定前に配布してよいという許可にはしない。

また、入力冒頭の「本番に降ろした」と末尾の「VMの実物は未確認」は区別が必要。確認済みと書けるのは開発側申告のpushまでで、VMの実行commit・配布完了・実挙動は本レビューでは未検証。運用側は実行commitを確認して記録すればよく、このために本物の投稿を試す必要はない。

## 証拠と未検証事項

同名添付ディレクトリの `controller/probe.py` / `results.json` / `before.log`、`audit-c.md`、2本のテストログ、`sha256.log`、`manifest.sha256.json`、`REPRODUCE.md` を参照。

元repo・VMへ書込みなし。本物のThreads API呼出しなし。外部通信を遮断した隔離環境で検証。VM配布、実API、参照先の実在性・循環は検証していない。公開経路の変更は今回の差分にない。
