# 再判定: 旧記録との同一性

**合格。前回P2は閉鎖。理由欄の変更は今回の固定対象で検収完了とする。**

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_再判定_同一性.md`
- SHA-256: `f35e46a747ee0c12caa985c1846d417190074bb9957b3942611de7f86650c0f5`
- 固定対象: `a5da5b696880a3c1111bf838d682daa79e9f42f4`
- 実装差分: `318bc3b..a5da5b6`
- 受付時作業repo HEAD: `9ca0ef4548c89b028c9d325f2b098363c7c9e1fc`、clean。

## 閉鎖条件の独立確認

主担当は前回の `probe.py` をbyte不変で実行した（SHA-256 `aed91d24d7638d754199c69b8918faa38845d899ebfa2949e162b5b9ac9278bb`）。実際の旧fb85182のCLIで保存し、新a5da5b6のCLIで同じ入力を再登録して比較した。旧IDを式で模擬しただけではない。

| 検証 | 結果 |
|---|---|
| 旧CLI登録→新CLI同一入力 | 同じID、stored:false |
| 明示supersede_reason:nullで再登録 | 同じID、stored:false |
| 改訂登録前の一覧 | count:1 |
| 旧保存JSON | SHA-256不変 |
| 理由付き改訂 | 登録成功、一覧・詳細で理由を保持 |

旧・新・明示nullのIDはすべて `sha256:99ec8d562b1c93f5b1f1844918ea3268f0190c2a9f6a1d02c4cf6853aa023a87`。前回同じprobeが別IDで失敗した証拠も添付に保持した。

担当Terra/mediumによる `test_hypothesis_units.py` と `test_hypotheses.py` は **54 passed in 10.87s**。主担当はログと内容を確認し、同一supersedesで理由A/Bだけを変えて異なるIDをassertする実CLIテストを確認した。理由なし改訂の拒否も同じ関連テストで維持されている。

差分は任意キーがNoneの場合に保存辞書から除く処理で、文字列の理由は削除もID計算から除外もしていない。旧ID互換性と省略/null同一性を両立している。

## これで着地する範囲

今回のP2に追加修正は不要。既知の旧反例4件への理由追加は、旧仕様からのテスト保守として別途行ってよいが、**本件を閉じるための新たなレビュー往復や配布の追加条件にはしない**。元反例を保存し、派生テストの入力差分だけを明示すればよい。

全987件・変異X/Y・未pushは開発側申告であり、本レビューで独立確認した件数は上記54件と独立probe。旧14反例は理由必須化による4件の既知仕様衝突を前回分類済みのため再実行していない。

## 証拠・未検証

同名添付ディレクトリの `controller/results-new.json`、`probe.py`、前回の `results.json` / `failure.log`、`audit-c.md`、`audit-c/selected-tests.log`、manifest、再現手順を参照。

元repo/VMへの書込み、push/配布、実Threads API呼出しは行っていない。外部通信遮断guardを使った隔離cloneと一時棚で検証。判定は固定対象コードについてで、VMの配布状態・実API・理由文の意味の正しさは未検証。公開経路は今回の差分に含まれない。
