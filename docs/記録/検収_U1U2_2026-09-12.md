# 再判定: U1・U2（2026-09-12）

**U1・U2とも閉鎖、合格です。今回の依頼は終了できます。追加の修正要求はありません。**

## 対象

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_再判定_U1U2.md`
- 入力SHA-256: `9b0925d0043a48d92830904b4bae911a64a0ebbe1954960e10bd9dedafc6fce8`
- 範囲: `98aea8b..6170215`、2 commits（うち1つは文書）。
- 固定対象: `6170215789920c54012faacdd349c1e73ce98f52`。
- 元repoの受付時HEADは `33ec060c628f481e7e5e2aada54210152f58709e`、clean。検収は指定commitの隔離cloneで実施しました。

## 確認した事実

| 閉鎖条件 | 実結果 |
| --- | --- |
| M/proposedの異層予測を保持 | shares・clicksと予測本文をそのまま保存。単位警告あり |
| U/proposedへ分類して保持 | 同じく保存可能。単位警告あり |
| 旧保存レコードの読取 | 前回の無変更反例で、詳細・一覧の両方に警告が出る |
| 詳細と一覧の一貫性 | 同じIDの警告配列が一致 |
| 読取が保存内容を変えない | 2件の保存JSONの読取前後SHA-256が一致。unit_warnings非保存 |
| 既存の拒否条件 | 未知指標はschema_error。shadow・acceptedはpromotion_not_implementedで拒否。拒否後の保存件数は2件のまま |

実装は今回追加した共通単位0による拒否を削除し、一覧の各レコードに動的警告を加えたものです。新しい評価・昇格の仕組みは追加していません。

## 検証と証拠

主担当が差分・既存検査の維持・保存内容の証拠を確認し、Sol/highの独立監査を組み合わせました。

- **前回の独立反例7件をbyte無変更で実行し、7件通過。** 前回は3件失敗していた同じ検査です。主担当も再実行し、`7 passed in 7.86s`。
- 主担当の関連回帰は **55 passed in 12.47s**。仮説の登録・一覧・前回境界・欠測表示を含みます。
- 追加の実プロセスCLI検査で、本文保持・警告一致・読取前後のJSONハッシュ一致・拒否後の保存件数を確認しました。具体値は添付 `audit-b/check_u1_u2.json`。
- 前回の反例ファイル3本と、measured/threadrun/coreの実装はbase→targetでbyte一致。添付 `controller/unchanged.json`。
- **953件の全件成功は実装側の申告で、今回独立に再実行した数字ではありません。** 差分が2つの仮説処理に限定され、前回の全948件と今回の関連55件・独立反例が確認できているため、全件の再実行は重ねていません。

## 判定の範囲

今回のU1・U2について、さらに往復する必要はありません。proposedへの保存は「検証可能・有効と認定した」という意味ではなく、未検証の案を警告付きで保持する状態です。

元repo・VMへの書込、push、本物のThreads API呼出しはしていません。VMの配布状態・実運用・仮説の当否は未検証です。コードの限定検収と本番の判断は引き続き別です。

再現物・実行ログ・SHA-256 manifestは同名ディレクトリに保存しました。
