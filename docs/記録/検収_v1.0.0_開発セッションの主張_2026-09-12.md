# 検収に出す主張: v1.0.0（開発セッション Fable・2026-09-12 夜）

**これは合格の申告ではありません。** 監査 2（まっさらな文脈の Opus 5）が下の各主張を
コマンドで確かめ、食い違いを証拠つきで返すための一覧です。番号で参照してください。

対象: `main` = **`da1e35c`**（`git log --oneline -1`）。監査 2 の指摘を受けた修正は
その後の commit（`docs/…` を含むこの文書自身が `tests/test_docs_links.py` を割っていた）。

## A. 状態

- A1. `origin/release` は **`0870bc0`** のまま。**この版は配っていない**
  （`git ls-remote --heads origin`）。
- A2. `origin/main` は **`324d02b`** のまま。**まだ push していない**（同上）。
- A3. 作業ツリーは clean（`git status --short` が空）。
- A4. stash は 0 件（`git stash list`）。捨てる前に、stash の追加行 185 行のうち 147 行が
  HEAD の `thth/` に逐語で存在し、残り 38 行はコメント・docstring の言い換えだけである
  ことを確かめた（stash 自体は消したので再現不能。信じなくてよい）。
- A5. 全件テストは **1251 passed**（`python -m pytest tests/ -q -n auto -p no:cacheprovider`）。
  着手前は 1205。
- A6. `python bin/thth --version` → `thth 1.0.0 (da1e35c)`。

## B. 棚の読み出し（Track A・Opus 5・merge `9ebadd4`）

- B1. `thth.topics.observation(語)` は**観測者ごとの最新**の配列（新しい順）を返す。
  観測者の鍵は `account` → `by` → `"(記録なし)"`（`observer_of`）。
- B2. `thth topics retract-note <note_id> --reason … --by …` が打ち消し行を追記し、
  `--advise`（画面・`--json`）・`legacy_observations()`・`judgment`・`kind_of` から消える。
  `history` には「打ち消し済み」の印つきで残る。`topics.json` の行数は減らない。
- B3. `--advise` の人向け画面は 1 語 2 件＋「ほか k 件」、`--json` は `observations`
  （最大 5）＋ `observations_more`。item 直下の旧鍵 `audience` / `audience_account` / `audience_by` は無い（`audience` は
  `observations[]` の各要素の中に残る。**監査 2 の指摘で言い直した**）。
- B4. `topic_store.legacy_observations()` も (語, 観測者) 単位・打ち消し反映。
- B5. 開発セッション自身の変異: `topics.notes()` から打ち消しの skip を外すと
  `tests/test_topic_shelf_observers.py` の T-A2 系 **4 件が落ちる**（戻して差分ゼロ）。
- B6. `docs/引継ぎ_トピックの棚_アカウント側セッションへ_2026-09-12.md` §2・§4・§5・§7 と
  `docs/使い方_プロジェクトのセッション向け_2026-09-09.md` の該当節が新契約を書いている。

## C. 導入（Track C1・Sonnet 5 ／ C2・Opus 5）

- C1. `thth/VERSION` = `1.0.0`。`thth board` の先頭行に版が出る。
- C2. `thth doctor <account>` は app.env → 台帳 → トークンの順に見て、足りないものごとに
  `次の一手: 導入文書 §3 / §4 / §5` を言う（`tests/test_doctor_next_step.py`）。
- C3. 利用者向けメッセージ（print / raise / help / warnings / notice）から固有名 `masaru` を
  外した。コメント・docstring・経緯の引用は残してある（意図的）。
- C4. `docs/導入_自分のMetaアプリで動かす.md` の各手順に出典と証拠段階が付いている。
  監査 2 の再集計で 63 行中 L1=39・L2=11・L3=13（21%）（C2 の申告 40/10/13 は内訳が違っていた）。**L3 は §2（Meta の管理画面）に
  集中している。**
- C5. `tests/test_fresh_install.py`（10 件）が、まっさらな clone に偽の app.env と台帳だけを
  置いて doctor → lint → board → dry-run を通し、トークンの手前で止まることを固定している。
- C6. `thth/__main__.py` を足した（`python -m thth …` が動く）。

## D. docs の分離（Track B・Sonnet 5）

- D1. `docs/記録/` に 75 本を `git mv`。`docs/` 直下は製品文書 23 本。
- D2. `tests/test_docs_links.py` が docs 配下と README の相対リンク・`docs/….md` 表記の
  実在を検査し、無ければ**落ちる**。除外は外部 URL・`docs/sns/queue/`・プレースホルダ・
  明示 3 件（理由つき）。
- D3. `tools/docs_reorg.py` は再実行しても差分ゼロ。
- D4. `docs/README.md` が製品文書の索引。

## E. やらなかったこと（正直に）

- E1. deploy key の read-only 化・GitHub Pro（masaru の操作・支払い）。
- E2. 棚の保存形式は変えていないが、`checks[]` に**打ち消しという行の種類を 1 つ足した**
  （監査 2 の指摘で言い直した）。旧棚と構造化棚の統合・集計 export は範囲外。
- E3. `accounts/` に masaru の 4 本の台帳が commit されたまま（C2 の指摘 4）。導入文書 §4 に
  「初回に消す」手順として書いたが、**配布物として正しい形ではない**。次の版の設計項目。
- E4. `--advise` の人向け画面が縦に伸びる。実データでの見え方は masaru の目で未確認。
- E5. `note_id` は行の中身から決まるので、**同じ秒に同じ内容を 2 回記録すると同じ ID**
  になる（打ち消すと両方消える）。実害は小さいが、監査 1 に見てもらう。
- E6. `release` は進めていない。tag `v1.0.0` も監査が終わるまで打たない。
