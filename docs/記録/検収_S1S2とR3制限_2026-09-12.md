# 再判定: S1・S2 と R3 の制限（2026-09-12）

**判定: S1・S2 は閉鎖、R3 の行ごとの選別も合格。今回の範囲に、新たな要修正事項はありません。** 前回の公開Bの再送P1を理由とする凍結解除可の判断を、この固定対象でも維持します。VMの配布状態・本物のAPIでの稼働を認定するものではありません。

## 対象と検証境界

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_再判定_S1S2とR3制限.md`
- 入力 SHA-256: `cf103ea56b00d3c5cad0fbd854f028ab9b20703f2ff748e7c2e7e0f8ca0bcf1f`
- 範囲: `d9bec3b..dddba8d`、5 commits。
- 固定対象: `dddba8d30ae46baf589a7495ec39073e8deeee55`。
- 元repoから隔離cloneを作成。元repo・VMへ書き込まず、pushも行っていません。本物のThreads APIは呼んでいません。
- 主担当が差分・前回反例との比較・S2・独立再現の再実行を担当。S1はSol/high、R3と全体回帰はTerra/mediumに範囲を限定して委任し、結果を照合しました。

## S1: 不一致で停止してよい。戻す必要はありません

`root_post_id` と先頭段の `post_id` が異なる記録は、共通の破損記録検査で止まります。正常な初回公開後にrootだけを `WRONG-ROOT` に変えた独立再現では、両IDを含む診断と対象の絶対パスが出て、次の公開は `stopped`。API呼び出し総数は正常な初回の1件のままで、不一致後の呼び出しは0件でした。

前回反例の「公開が続く」というassertは、不具合の実際の影響を観測するためのもので、維持すべき仕様ではありません。前回の閉鎖条件は記録の値の一致です。入力・fixture・公開の呼び出しを維持し、期待を停止に変えた今回の修正は妥当です。比較で先頭140行が同一であることも確認しています。

正常な初回公開、確定失敗後のpending再試行は通過し、前回までの再送反例も通過しました。正常系を止めすぎる再現は今回の確認範囲ではありません。

## S2: 更新日を更新日として保存できています

`DATE_KINDS` に `updated_at` が追加され、H01・H10のO1は日付値 `2025-03-07` を維持して `updated_at` になっています。H05/H08のE3、H09の以前の修正は維持されています。日付の再調査を追加条件にはしていません。

前回の反例とschema検査を主担当が実行し、21件通過しました。

## R3: 過去を推測で復元する変更ではありません

新しい所有の証拠がある行だけを使い、所有不明の旧行は使わない修正です。今回は明示的に検収を依頼された範囲として合格とします。戻す必要はありません。

独立再現を主担当も再実行しました。

| 入力 | 確認結果 |
| --- | --- |
| 所有不明の旧行 views=10 + Aの新行 views=42 | Aには42だけ。旧行をAへ帰属させない |
| 同じ台帳をBから読む | Bの投稿系列に入らない |
| 旧行を1行除外 | JSONの `rows_unattributed=1` と人向けの除外理由・不完全な系列の説明が出る |
| 同じ投稿台帳にA/Bの所有記録が混在 | 両方で当該ファイルが `broken`、どちらの系列にも入れない |

所有記録が矛盾する投稿台帳を丸ごと除外するのは妥当です。止めるのは当該投稿の実測利用で、アカウントの公開を停止する仕組みではありません。コードは当該ファイルを `continue` し、他の台帳の処理を継続します。

`rows_unattributed` は完全な履歴でないことを伝える情報として十分です。旧行の所有を補完したことにも、過去の全測定が復元できたことにもなりません。

## 証拠と申告の補足

全体テスト結果: **943 passed, 3 warnings in 388.78s**。warningsはPythonのforkに関するDeprecationWarningです。

そのほか、S1の独立・限定検査22件、公開関連回帰90件が通過。主担当側でもS1独立2件・S1関連8件とS2関連21件を実行し、R3独立スクリプトを再実行しました。これらは全件テストと重複するため、合計件数にはしません。前回反例の入力維持と具体的な公開・系列の結果を確認した上で判定しています。

`9092372` の `cli.py` と `measured.py` は、コメント・docstringを除去したAST比較でも実行コードに変化がありませんでした。

「S1の期待以外はbyte一致」という申告は厳密には訂正が必要です。`test_r2_r4_r5.py` にもREPOパスの解決と説明の変更があります。ただしassertは変わらず、収録先のディレクトリ構造へ対応する変更なので検査を弱めていません。この記録で差異を明示し、追加修正の条件にはしません。

## 配布と今回の着地点

push前の固定cloneで検収する理解は正しいです。今回の検収もその方法で成立しました。`cmd_run()` が自己更新処理を呼び、pullで進んだ場合に再実行する経路はコードで確認しました。したがってpushが本番の更新につながる前提で扱う必要があります。ただし、今回VMの実物・timer・ネットワーク・実際の適用時刻を確認しておらず、「必ず10分以内」や現在のVMの版は認定していません。

**この依頼は閉じられます。S1・S2・今回のR3について、さらに往復する修正要求はありません。** 配布・本番運用の判断はこのローカル検収とは分けてください。自己更新機構の再設計を今回の合格条件に加えることはしません。

添付: 同名ディレクトリの独立再現、検証ログ、前回反例との差分、SHA-256 manifest。
