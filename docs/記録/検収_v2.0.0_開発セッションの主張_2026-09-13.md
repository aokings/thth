# 検収に出す主張: v2.0.0 門を無料で開ける版（v2-1・v2-2a・v2-2b・v2-2c・v2-4）——開発セッション thth-05・2026-09-13 夜

**合格の申告ではありません。** 監査 1（壊しにいく）と監査 2（主張の検証）——どちらもまっさらな文脈の Opus 5——が
各主張をコマンドで確かめ、食い違いを証拠つきで返すための一覧です。対象は `main` の先端（この文書の commit の
1 つ前 `0fb1a2b` がコードの先端）。設計の正本は `docs/設計_v2_泉と門_2026-09-13.md` §1・§2・§3・§7。

経緯: 前任の開発セッション（thth-0c）が 19:51 に停止し、BG 4 本も死んだ。途中差分を WIP として固定し、同じ依頼文で
再開した（各 BG は前任の WIP を「信じずに読み直す」よう指示）。**開発セッション（thth-05）は各 Track で、テスト・
変異 1 つ・実出力を自分で再現してから merge した。** それでも自己検収は独立検収の代わりにならない。

## A. 状態

- A1. `origin/release` は **`0bd8208`**（v1.3.0）のまま。v2.0.0 は**配っていない**。`thth/VERSION` はまだ `1.3.0`
  （tag と同時に上げる）。
- A2. 全件テストは **1727 passed, 1 skipped**（`python -m pytest tests/ -q -n auto -p no:cacheprovider`・rc=0）。
  v2 着手前（`ef6fa37`）は 1621。
- A3. `tests/test_board_running.py::test_boardはロックを奪わない` は **負荷で稀に落ちる**（BG 2 本が同時に全件を
  回したとき 2 回観測・単独では通る）。v2 の変更とは無関係だが、潰す価値がある。
- A4. worktree は `main` 1 本。BG 用 8 本は merge 後に全部削除。ローカル枝は `main` のみ。
- A5. `ef6fa37..0fb1a2b` は 31 files・+4888/−33。

## B. v2-1 `before_you_post`（設計 §1）

- B1. `thth ask before-you-post <account> --topic <語> [--kind] [--hour-band 朝|昼|夕|深夜] [--reply] [--json]
  [--window-days N] [--min-n N]`。**読むだけ**（repo も state も変えない・`test_f6`）。
- B2. JSON は §1 の 7 鍵ちょうど（`summary`・`expected`・`comparable`・`cannot_say`・`one_thing_to_change`・
  `audience`・`provenance`）。`provenance.source` は `"local"`。
- B3. `n < min_n`（既定 20）は中央値を返さず `cannot_say` に `n=…` の理由。**手元の水ではほぼ全部 cannot_say**
  ——kopicha の本物の台帳で `--topic コーヒー` は n=5・3 指標とも `—`・rc=0（開発セッションが再現）。
- B4. 媒体をまたがない: `post["medium"] != medium` の閉じを外すと `test_c` が落ちる（開発セッションが変異で再現）。
- B5. `one_thing_to_change` は差が言える群が 2 つ以上あるときだけ 1 個・指図の語（〜すべき 等 9 種）を含まない。
  無ければ `null`。
- B6. 本文・返信本文・`username` は入力にも出力にも無い（偽台帳に `SECRET-BODY`・`SECRET-USER` を仕込んで JSON
  全体と人向け出力を grep・`test_e`〜`e3`）。
- B7. MCP tool `before_you_post` の description は §1 規約 6 の固定文言。`cannot_say` だらけでも `isError` にならない。
- B8. `build_parser` への追加は `ask_cli.register(sub)` の 1 行（`thth/cli.py` 末尾・`return p` の直前）。
- B9. **契約で迷った判断（masaru 確認待ち・監査は「設計 §1 と食い違うか」だけ見ればよい）**: `medium` は必須に
  せず既定は台帳の `media`。`audience` は語につき 1 行で、**最新の観測者の自由文だけ**（他の観測者の自由文は
  落ちる）。
- B10. 前任 WIP から直した穴 2 つ: 1 件の群で `statistics.quantiles` が例外（`--min-n 1` で死ぬ）→ n<2 は
  p25/p75 を None に。除外件数を語で絞る前に数えていた → 語・型で絞ってから数える。

## C. v2-2a 台帳を repo の外へ（設計 §3・§7-1）

- C1. 解決順は `accounts_dir_info()`: (a) `$THTH_ACCOUNTS_DIR` → (b) `$THTH_ROOT/accounts/`（**正**・ディレクトリが
  あれば空でも正）→ (c) 互換: app repo の `accounts/`（1 版だけ・stderr に警告 1 行）→ どれも無ければ (b) の path。
- C2. **書き込み先は (a) か (b) のみ**。互換 (c) に落ちていても repo には書かない。
- C3. 互換 (c) を外すと `tests/test_accounts_dir.py` が 6 本落ちる（開発セッションが再現）。BG は clone 経由でも
  `test_fresh_install.py` 8 本が落ちることを確認した（VM を模した経路）。
- C4. `thth account migrate`: repo の `accounts/*.json` を (b) へ **copy**・repo は触らない・既存は上書きしない
  （中身が違えば名指しで rc=1）・冪等・`--dry-run` はディレクトリも作らない。
- C5. `thth account add <name> --media threads|bluesky|mastodon --project <p> [--handle] [--instance] [--repo-dir]`:
  `accounts.example/<media>.json` の雛形から書く。`production: false`・`scheduled: false`。既存は上書きしない rc=1。
- C6. repo の `accounts/` は **6 本のまま**（消すのは VM の migrate が済んだ後の別 commit）。`accounts.example/` は
  3 本・値は全部ダミー。
- C7. `thth doctor`・`thth board` が台帳の置き場を 1 行で言う（台帳が読めないときも）。`--json` は `accounts_dir` を持つ。
- C8. 既存テストの変更は `tests/conftest.py` の 1 か所だけ（`isolated_account_factory` が `THTH_ACCOUNTS_DIR` を明示。
  理由はコメントに）。
- C9. 運用向けの VM 手順は `docs/引継ぎ_運用セッション_2026-09-13.md` §7 と `docs/導入_自分のMetaアプリで動かす.md` §4。
  **急がない**（互換が効いている）。
- C10. **判断待ち（masaru）**: `accounts.example/threads.json` の `redirect_uri` は `https://example.invalid/` のダミー。
  `add` で作った台帳で `thth auth` を打つ人は手で直す必要があり、`doctor` は何も言わない。

## D. v2-2b `pip install thth`・skill・`thth share`（設計 §3・§2・§7-3）

- D1. `pyproject.toml`: hatchling・`dependencies = []`（依存 0）・`dynamic = ["version"]` が `thth/VERSION` を読む・
  `[project.scripts]` に `thth`・`thth-mcp`。`force-include` で `mcp/server.py` → `thth/mcp_server.py`、`skills/` →
  `thth/skills/`。`bin/thth` は残る。sdist から `accounts/`・`docs/` を除外。
- D2. `tests/test_packaging.py` は `python -m build` → **別 venv** に wheel を入れ、`thth --version`・`--help`・
  `python -m thth doctor`・MCP の起動を叩く。**repo の外の cwd で**叩き、import 元が site-packages であることを
  確かめる（前任の試験は cwd が repo で、wheel でなく repo を import していた——BG が変異で穴を実証して直した）。
  `build`・`venv` が無ければ skip でなく fail。
- D3. `skills/thth/SKILL.md`: frontmatter は name `thth`・description は「投稿する前に呼ぶ道具。…」。先頭に
  「何を保証し、何を拒み、何を絶対にしないか」。
- D4. `thth share on|off|status|log|sync`。**既定 off**（`$THTH_ROOT/state/share/config.json` が無ければ off・壊れて
  いても off）。`is_on()` を True に固定すると **10 本**落ちる（`tests/test_share.py` 8 ＋
  `tests/test_share_safety.py` 2。**訂正**: 初稿は「5 本」と書いていた。監査 2 の実測は 10 本・§G）。
- D5. **off で 0 バイト**。on のとき `state/share/outbox/<YYYY-MM>.ndjson` に §2「落ちるもの」だけ: 観測
  （語・audience・型・status・観測者の仮名・日時）とスレッドの形（post_id の**塩つき sha256**・媒体・型・語・
  時刻帯・刻みごとの数）。塩は outbox に出ない。観測者の仮名は `state/share/observer_id`（16 桁・安定）。
- D6. outbox のどの行にも `text`・`username`・`verdict`・`by`・`note`・`reason`・`access_token`・`repo_dir`・
  アカウント名・生の post_id が無い（`tests/test_share_safety.py`・`SECRET-…` を仕込んだ偽台帳で grep・書く直前の
  `_assert_clean()` が例外で止める）。
- D7. 積む口 `sync()` は出来上がった台帳を読むだけ。`collect.py`・`topics.py` は 1 行も変えていない。
- D8. `thth throw` は share に依存しない: 門の経路が `share` を import していない（ast）・share の置き場を 4 通りに
  壊しても実プロセスの `throw` が通る。`share.py` は `urllib`・`socket`・`http` を import しない。
- D9. `build_parser` への追加は `share_cli.register(sub)` の 1 行。
- D10. PyPI への登録・upload は未実施（masaru の手・設計 §7）。

## E. v2-2c 英語文書・v2-4 棚卸し

- E1. `README.en.md`・`docs/usage.en.md`・`llms.txt`。先頭は「何を保証し、何を拒み、何を絶対にしないか」。
  `thth share` の記述は v2-2b の実装に合わせて更新済み（「designed but not built」ではない）。
- E2. `docs/公開前チェックリスト_2026-09-13.md`: 秘密の値は **0 件**（grep のヒット 52 行は全部 commit SHA / SHA-256）。
  **`accounts/*.json` の `token` は値ではなく `<account>.token` への path**（BG の初稿は「実 token が履歴に残る」と
  書いていた——開発セッションが 6 本とも path であることを確かめ、訂正 commit `3c74d68`）。履歴の書き換えは不要。
- E3. `tools/split_notes.py --dry-run`: 90 本（`docs/記録/` 81＋`docs/` 直下の日誌 9）を `../thth-notes/記録/` へ移す
  計画とリンク切れ **29 件**を出す（**訂正**: 初稿は「18 か所」。監査 2 の実測は 29 件・§G。なお本文書自身が
  `docs/記録/` に増えたので移動対象は 91 本、索引に運用の引継ぎ書を足した後は 30 件）。`--execute` は
  `../thth-notes` が git repo のときだけ。**今回は dry-run のみ**。
  worktree から走らせても main repo の隣を指す（`git rev-parse --git-common-dir`）。
- E4. `tests/test_docs_links.py` は通る（新しい文書のリンクも含めて）。

## F. 監査に頼むこと

- 監査 1（壊しにいく）: B・C・D の「〜しない」を破りにいく。特に **D5/D6（落ちないもの）**・**C2（互換でも repo に
  書かない）**・**B6（本文が漏れない）**・**D8（throw が share に依存しない）**・**C4（migrate が repo を触らない）**。
  変異は置換の assert つき・逆置換で戻す・worktree の外に書かない。
- 監査 2（主張の検証）: A〜E を 1 項目ずつコマンドで確かめ、一致／不一致／部分不一致を証拠つきで。B9・C10 は
  「判断待ち」なので設計 §1・§3 と食い違うかどうかだけ。
- どちらも: **本番・VM・実 Threads/Bluesky/Mastodon API・PyPI・`~/.config/thth/` には触らない。`~/thth-exchange`
  にも書かない。repo（main）のファイルを変更しない。commit も push も禁止。** 利用者 repo（`~/Developer/kopicha`
  等）は読むだけ。

## G. 監査 2 の結果（2026-09-13）

まっさらな Opus 5 が A〜E を 1 項目ずつコマンドで確かめた。**大半は一致。** 食い違ったのが下の 4 件で、
実装担当（別 worktree）がこの日のうちに直した。**`thth/VERSION` は触っていない**（tag と同時に上げる）。

### 食い違い（部分不一致 1・不一致 3）

- **B7（部分不一致）**: MCP `before_you_post` の description が設計 §1 規約 6 の文言に
  「。言えないことは cannot_say に出る」を足していた。**設計書ではなく実装を設計の文言に戻した**（§H の P3）。
- **D4（不一致・数）**: 「`is_on()` を True に固定すると 5 本落ちる」→ 実測 **10 本**
  （`tests/test_share.py` 8 ＋ `tests/test_share_safety.py` 2）。主張の側を訂正した。
- **E1（不一致）**: 「`thth share` の記述は更新済み」は正しかったが、同じ `README.en.md` の 112 行が
  **「`thth ask` … is designed but not implemented」のまま**だった（`thth ask before-you-post` は実装済み）。
  1 行の使い方と「手元の台帳だけ・泉ではない・`provenance.source` は local・本文は渡さない・ほぼ `cannot_say`
  で rc は 0」に書き換えた。
- **E3（不一致・数）**: 「リンク切れ 18 か所」→ 実測 **29 件**（移動対象は 91 本）。主張の側を訂正した。

### あるべきもの（9 件）

**この commit で直した 4 件:**

1. `accounts.example/` が wheel にも sdist にも入っておらず、**`pip install` した人の `thth account add` が
   `雛形がありません: …/site-packages/accounts.example/threads.json` の rc=2** だった。`force-include` と sdist の
   `include` に足し、`EXAMPLE_DIR` は package の中を先に見る。別 venv で rc=0 になる試験を `tests/test_packaging.py` に。
2. `docs/usage.en.md`・`llms.txt` に v2.0.0 の口（`ask`・`share`・`account add`/`migrate`）が 1 行も無かった。足した
   （usage.en.md §9・§10・§11）。`README.en.md` とも食い違わない。
3. `README.md`（日本語）が「いまの状態（2026-09-12）」・「全件テスト 1613 件」のままで、v2.0.0 の口に触れていなかった。
   版を `2.0.0（tag 待ち）`、件数を実測に、増えた口を 1 節に。
4. `docs/README.md`（索引）が `usage.en.md`・`公開前チェックリスト_2026-09-13.md`・`引継ぎ_運用セッション_2026-09-13.md`
   を落としていた。足した（`docs/` 直下と突き合わせて、落ちていたのはこの 3 本だけ）。

**残した 5 件（理由つき）:**

5. **`LICENSE` が無い。** `README.en.md` は MIT と書き、`pyproject.toml` も `license = { text = "MIT" }` だが、
   file が無い。**repo を public にする切替と同じ、masaru の手**（設計 §7-2・`docs/公開前チェックリスト_2026-09-13.md`）。
6. **MCP registry への登録が未実施**（v2-4）。外部サービスへの登録なので **masaru の手**。
7. **「LLM に選ばせる試験」が未着手**（設計 §3 の評価欄・裁定 §7-8 で受け入れ条件にすると決めた）。
   まっさらな Claude に候補を混ぜて選ばせる試験は、この repo のテストの外側にある段取りなので着手していない。
8. **Bluesky の `handle` の既定**。`thth account add --media bluesky` の handle は `--project` の値になるが、
   Bluesky の実際の handle は `<name>.bsky.social`。`--handle` で渡せば済むが、`doctor` は何も言わない。
   媒体ごとに既定を変えるかは設計の判断なので **masaru の裁定待ち**。
9. **C10（`accounts.example/threads.json` の `redirect_uri` がダミー）**。主張の時点から **masaru 判断待ち**のまま。
   `add` で作った台帳で `thth auth` を打つ人は手で直す必要があり、`doctor` は何も言わない。

### 破れなかったもの

A1・A4・A5・B1〜B6・B8・B10・C1〜C9・D1〜D3・D5〜D10・E2・E4 は、実測が主張どおりだった。
A2 の件数は実測の時点で 1727（この commit の後は §「いまの状態」を参照）。A3（`test_boardはロックを奪わない` が
負荷で稀に落ちる）はこの巡でも再現しなかったが、主張どおり**潰す価値がある**ものとして残っている。

## H. 監査 1 の結果（2026-09-13）

まっさらな Opus 5 が B・C・D の「〜しない」を破りにいった。**D5/D6（落ちないもの）・
B6（本文が漏れない）・D8（throw が share に依存しない）・C4（migrate が repo を触らない）は
破れなかった。** 破れたのが下で、実装担当（別 worktree）が同じ日に直した。
**`thth/VERSION` は触っていない**（tag と同時に上げる）。

### P1（重い）

- **P1-1**（＝§G の 1）: 雛形が wheel にも sdist にも入っておらず、`pip install` した人の
  `thth account add` が rc=2。**直した**（`force-include` と sdist の `include`・
  `_find_example_dir()`・別 venv で rc=0 の試験）。
- **P1-2**: pip 版で `THTH_ROOT` 未設定だと `thth_root()` が **site-packages** を返し、台帳・
  state・share の outbox・塩・仮名が全部そこに落ちていた（**`pip install --upgrade` が黙って
  消す**）。**直した**——最後の段を `$XDG_DATA_HOME/thth`、無ければ `~/.thth` に。repo から
  走っているとき（`APP_DIR` に `.git` か `bin/thth`）だけ従来どおり。
- **P1-3**: 互換 (c) の機械で `migrate` より先に `account add` を打つと、`$THTH_ROOT/accounts/` が
  出来た瞬間に repo の 6 本が読まれなくなり、`add` は何も言わなかった（次の `thth run` が
  「台帳が無い」rc=2）。**直した**——書く前に rc=1 で断り、何が起きるかと `migrate` → `add` の
  順番を出す。`--force` でだけ進む。導入者（clone に同梱された他人の台帳）には
  `--force` が正しい進み方だと断り文と導入文書に書いた。

### P2

- **P2-1**: `thth account add ../pwned` が `accounts/../pwned.json` に書けて rc=0（`list_account_names()`
  に出ないので board からも見えない）。**直した**——名前を `[A-Za-z0-9_.-]+` かつ `.`/`..` 以外に
  限って rc=2。
- **P2-2**: 台帳ディレクトリが `chmod 000` のとき `board`/`account` が traceback、`doctor` は
  「台帳が無い」と**嘘**。**直した**——`list_account_names()`/`load_account()` が「読めません（権限）」と
  「ありません」を言い分け、`board`・`account` は置き場の 1 行を出してから rc=2。
- **P2-3**: 返信台帳の ndjson が壊れていると `before_you_post` の枝・最初の枝が黙って n=0
  （「壊れて数えられない」が「無かった」に化ける）。**直した**——`_population()` が
  `replies.load()` の `broken` を持ち上げ、`cannot_say` に「返信台帳の N 本が読めません……
  枝と最初の枝はその分だけ少なく出ています」。
- **P2-4**: `thth share sync` に排他が無く、4 本同時で outbox が 4 倍（484 行 / row_id 121 種）。
  **直した**——`thth/lock.py` で囲う（`state/share/sync.lock`・待ち方は `topics.record()` と同じ）。
  実プロセス 4 本の試験つき。
- **P2-5**: 語が 10 桁以上の数字だけだと `_assert_clean()` の `^\d{10,}$` が `topic` を生 post_id と
  誤認し、`share on` が rc=2 なのに **on のまま残って** 以後 `sync` が恒久的に rc=2。**直した**——
  生 post_id の検査は ID の欄だけに限り、`topic`・`audience` は対象外（長さの検査は残す）。
  `share on` は `sync` が落ちたら off に戻す。

### P3

- loud reject でなく traceback だった 3 経路（`accounts` がファイル → `account add`／
  `state/share` が書けない → `share on`／`outbox` がファイル → `share sync`）。**直した**。
- `THTH_ACCOUNTS_DIR` の相対パスをその場で絶対化（`where_line()` も絶対で出す）。**直した**。
- **B7**: MCP の description が設計 §1 規約 6 の文言に「。言えないことは cannot_say に出る」を
  足していた。**実装を設計の文言に戻した**（設計書は触らない）。`test_g` も合わせた。
- **D6 の射程**: `audience` は人が書く自由文なので、そこに名前を書けば積まれる。
  `skills/thth/SKILL.md`・`README.en.md`・`docs/usage.en.md` に「落ちないのは**返信者の**
  username。`audience` に自分で書いたものは落ちる。誰がいたかは属性で書く」と明記。**直した**。

### 生き残った変異 3 件（テストを足した）

- `share.observer_id()`・`_salt()` の 0600 —— しかも `open()` → `chmod()` の順で**一瞬 644**
  だった。`os.open(..., 0o600)` で最初から 600 にし、モードを見る試験を足した。
- `share._num()` の bool 除け（`True` を 1 として積ませない）。
- `enqueue_thread_shape` の `_trim(kind, 40)`（`hour_band`・`medium` も）。

### 直さずに残したもの

§G の「残した 5 件」と同じ（LICENSE・MCP registry・LLM に選ばせる試験・Bluesky の handle の
既定・C10 の `redirect_uri`）。いずれも masaru の手か裁定待ちで、コードの穴ではない。
