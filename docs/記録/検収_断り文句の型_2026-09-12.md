# 再判定結果: 断り文句の型とnoteの案内

**合格。前回P2の2件を閉鎖する。今回の仮説登録の案内修正は検収完了。**

## 対象

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_再判定_断り文句の型.md`
- SHA-256: `bf4f09a0e4864307e71517d1100f5f2a1443418252753162cbe88b28f764c5a1`
- 固定対象: `d168fdd82e254c16727183b099856e9afee60302`
- 差分: `2e88f49..d168fdd`
- 受付時元repo HEAD: `ef9e01dbf4272c5674bfbed81ea2b1b69ed9a28d`、clean。

主担当が差分・前回byte不変の反例・追加CLI入力を確認。Terra/medium担当が関連回帰とparser/例外出口の対応を確認し、主担当がログを照合した。

## 閉鎖の証拠

1. **前回の仮説入力10パターンはすべてHypothesisだけを返す。** 空object、claim空、kind不正、state不正、scope空、created_at不正、sources空、source.tier不正、kindにTopicObservation、kindに「記事」。終了コードは2、案内の型は入力文字列で変わらない。前回返却probe.pyはSHA-256 `3db95652189efb3d21fe4a89da9bd54661724d80eca9c6bd73d4096d49f40886`でbyte一致。前回は失敗していた同じassertが通った。
2. **noteの案内と受理条件が一致する。** 表示は「キーは必須・書くことが無ければnull」。独立CLIでnote:nullは登録成功、noteキー省略は拒否され、Hypothesisの形が返る。
3. **入力エラーでも文脈を維持する。** 配列JSON、入力引数なしも終了2でHypothesisを返す。
4. **関連回帰104 passed (19.32秒)。** `test_topic_cli.py` / `test_hypotheses.py` / `test_hypothesis_units.py`。正常登録・理由・同一性も関連回帰に含む。

parserのexpected_schema defaultは仮説登録だけに追加され、入力/schema例外でその値を渡している。`_fail`は明示値がNoneでないとき早期にその形を返すので、エラー文を後から照合して上書きしない。build系の受理条件や保存ID計算は変更されていない。

## 別コマンドと未検証の区別

語彙登録の案内全体が直ったという判定ではない。既存テストの語彙欠損エラーはReasonVocabularyを返す一方、前回controlの不正supersedesは今回も空のexpected_schemaを返す。これは前回と同じ出力で、今回の追加閉鎖条件にはしない。全型の文言推測を解消したと報告しない。

全1006件・変異AB〜AE・未push・VM想定commitは依頼側申告。本レビューが独立実行したのは上記104件、前回probe、追加4ケース。VM配布・実Threads API・全型の全エラー組合せは未検証。

## 証拠

同名添付ディレクトリの `controller/probe.py` / `results.json`、`controller/extra.py` / `extra.json`、`audit-c.md`、`audit-c/selected-tests.log`、manifest、再現手順を参照。

作業repoとVMに書込みなし。push/配布なし。本物のThreads API呼出しなし。外部通信を遮断した隔離cloneと一時棚で検証した。
