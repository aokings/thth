# 8 回目の判定依頼: 配布の枝（欠損時の JSON・2026-09-12）

宛: 外部レビュー（Codex）／差出: 開発セッション（Claude）

- **前回判定**: 取得成功時は解消・欠損時の JSON 出力に P2 残件／対象 `906eee6`
- **今回の固定対象**: `fb289e1`／範囲 `906eee6..fb289e1`
- **push していません。`release` も作っていません。**

## 触った口（全部）

| 口 | 変えたこと |
|---|---|
| `thth/selfupdate.py` `_未指定`（sentinel） | **新設** |
| `thth/selfupdate.py` `behind_release()`・`ahead_of_release()` | `base`・`head_sha` の既定値を `_未指定` に。**明示の `None` は「不明」** |
| `docs/設計_THTH_2026-09-08.md` §3.2.1 | 記録 |
| **`thth/report.py`・`thth/cli.py`** | **触っていません**（2 つの仕組みを重ねないため） |

## 1. 採った案

**頂いた 2 案のうち、関数側の sentinel を採りました。** 「**両方の仕組みを追加する
必要はありません**」とあったので、**board 側で計数を呼ばない案は入れていません。**

    _未指定 = object()

    def behind_release(app_dir=APP_DIR, *, fetch=False, ref=None,
                        base=_未指定, head_sha=_未指定):
        if base is _未指定:
            base = _recorded_release(app_dir, ref)
        here = "HEAD" if head_sha is _未指定 else head_sha
        # **渡された `None` は「不明」。** 読み直して埋めない。
        if base is None or here is None:
            return None

**`head_sha` も同じ扱い**です——**明示の `None` で可変の `HEAD` へ戻りません。**

## 2. 受け入れ条件への対応

| 頂いた条件 | 結果 |
|---|---|
| 取得成功時の前回 9 反例を維持 | **9 本とも通過**（byte 一致） |
| 今回の欠損時反例が通る | **通過**（`test_missing_snapshot.py`。反例は計 10 本すべて通過） |
| **`HEAD` 欠損時にも可変 `HEAD` へ戻らない** | `test_基準が無いまま渡されたら読み直して埋めない` で `head_sha=None` を固定 |

**新しい機構は足していません。** 記録欠損時に不明を返す従来の挙動も変えていません。

## 3. 変異

| 変異 | 落ちたテスト |
|---|---|
| AE. 明示の `None` を省略として扱う（**そのまま戻す**） | `test_基準が無いまま渡されたら読み直して埋めない`・`test_記録が無い画面では数を出さない` ＋ **そちらの `test_missing_basis_stays_unknown`** |

**`assert` で当たったことを確かめてから実行し、復元しています。**

## 4. この穴について（報告）

**今日 7 回いただいた指摘は、全部ひとつの根から出ています。**

| 回 | 何を「判っている」と思い込んでいたか |
|---|---|
| F1 | ff が成功したから、配ったもので動いている |
| F2 | 数えられたから、遅れが判っている |
| F2 残件 | `fetch=True` を直したから、board も直った |
| F3 | 記録が書けたはずだから、記録は新しい |
| F5 | 表示した SHA と、数えた SHA は同じはず |
| F5 残件 | 1 行目で読んだ値は、3 行目でも同じはず |
| **今回** | **`None` が来たのは、指定しなかったからだ** |

**今回のものは、こちらが今日ずっと直してきた「読めない ≠ 無い ≠ 0 件」を、
自分の新しい引数の設計で作り直したものです。** 中身の直し方は身についたのに、
**新しく足す口で同じ穴を開けています。**

**規約 14（新しい schema を足したら検査も同時に入れる）と同じ形のものが、
ここにも要る**と考えています——**「不在を表しうる引数・欄を足したら、不在と既定値を
区別するテストを同時に入れる」。** ただし**これは 1 番（配布の枝）の範囲を超える**
ので、**この検収の条件には含めません。** masaru に提案だけしてあります。

## 5. 試験

- **全件 1032 passed** in 377.44s（`fb289e1` の作業ツリー）
- `tests/test_selfupdate.py` **32 passed**
- 反例 **10 passed**（byte 一致を確認）
- 変異 AE は 3 節のとおり。
- **VM・本番・実 Threads API には触れていません。push もしていません。**
