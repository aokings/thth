# 検収結果: 判断の一覧

**判定: 合格。今回の変更に修正必須の指摘はありません。**

対象 `29670af3ee451e6eafe6388b6d10c2eddf67c1c0`（`d225f9a..29670af`）。受付・終了時の元repo HEADは `50b1e224b10db8e8c39ff9447553e0db70084865`、作業ツリーはclean。依頼SHA256: `4cd4910d71dde169214c03329893b4fdf1acfc40ff39ed1ffa7e837988d02e09`。

## 確認結果

- 一覧の proposal_id/article_id は保存済み判断からそのまま投影され、単体取得のIDと一致します。
- 隔離CLIで判断を登録し、一覧に出たIDを使って store.get で実際の候補比較と記事証拠を取得できました。候補1件を確認し、読み取り前後で保存JSONのSHA256が変わらないことも確認しました。
- 関連テスト2ファイル34件成功。独立追加検証1件成功。合計35件。実装側の全1009件という報告は今回再実行していません。
- 機能変更は一覧への参照ID追加。保存・内容ID・公開経路に変更はありません。

## 説明の訂正と着地

今回の変更は便利な参照IDの追加です。従来も decision_id から判断を取得して関連IDに辿れたため、「一覧は全ての関連IDを持たなければならない」という原則違反として扱う必要はありません。要約と本体の分離は、全一覧を一律に改修する指示ではありません。

依頼書とコードコメントの「単体取得はproposalsの棚を開いて繋ぎ直す」は、この対象コードには当てはまりません。cmd_decision は保存済みdecisionを読み、freshnessを加えて返します。今回、記事と候補比較の原本を確認した経路はPythonのstore.getです。CLI単体取得による原本の結合と混同しないでください。この説明だけ次の文書整理で訂正すれば足り、今回の合格を保留しません。

candidate_count は追加不要です。利用者に必要な画面・処理が出た時点で検討してください。他の全一覧に問題が無いという横断的な主張は、今回の検収範囲外です。待ち行列の2件も今回の合格条件には加えません。

## 証拠と境界

同名フォルダに controller/probe.log、controller/test_probe.py、audit-c/selected-tests.log、REPRODUCE.md、manifest.sha256.json を保存しました。機械的な関連テストと主査の参照先確認を分担し、主査がログを読み判定しています。

元repo・VMへの書き込み、push、実Threads API呼び出しは行っていません。VM配布状態と本番結果は未検証です。この合格は固定対象の変更についてです。
