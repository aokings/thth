# 再判定: V1残件（2026-09-12）

**V1は閉鎖、合格です。前回の閉鎖条件に追加要求はありません。** 空文字の申告は再現しましたが、別件の入力品質改善として扱います。

## 対象と実施

- 入力: `/Users/masaru/thth-exchange/in/2026-09-12_再判定_V1残件.md`
- 入力SHA-256: `4aa3f68ced38a4e24c1667dd095ca04f8343b4e1cd772693c18818f787f96a85`
- 実装固定対象: `1a31500d1923e79c43a086e36e9f514338cebd1b`。
- 実装の比較: `626d812..1a31500`。この区間には先の依頼文書 `f85d035` も含まれます。受付時元repo HEADは `a2026bf757d5647b865b6a36608feea4c88944b5`、clean。曖昧な「この依頼のcommit」ではなく明記された実装固定対象を検収しました。
- 主担当が差分と独立6パターン・追加申告を確認し、Terra/mediumに前回反例の同一性照合と限定回帰を委任しました。

## 閉鎖の根拠

変更は読取側の `if not link` を `if link is None` にするものです。主担当が前回の再現スクリプトを無変更で再実行し、次を確認しました。

| 旧レコードの値 | 一覧 | 版リンクの診断 |
| --- | --- | --- |
| null | 正常 | なし |
| [] / {} / 0 / false | 正常 | 各1件あり |
| 非空配列 | 正常 | 1件あり |

**前回の独立反例14件はSHA-256一致のまま全件通過。関連17件を含め、31 passed in 6.25s。** 正常な版索引・フィルター、登録の拒否、既存の不正値の診断、U1/U2が維持されています。主担当の6パターン確認は別途実行しました。

全963件成功は実装側の申告で、今回独立に全件を再実行した数字ではありません。

## 空文字・空白文字列の申告への回答

`supersedes=''` と `'   '` はいずれも登録成功・stored=true、一覧も正常、`broken_version_links=[]` でした。申告は正しいです。実出力は `controller/blank.json`。

**別の小さな修正として対処することを勧めます。V1を再び未閉鎖にする条件にはしません。** 前回、文字列IDの形式まで広げることは要求していなかったためです。また、今回の非文字列の一覧クラッシュとは異なり、この入力で一覧自体は壊れません。

直すなら空文字だけを例外的に足すより、登録と既存レコード読取で「nullまたはhypothesis_idの表記」を同じ基準にしてください。既存のID用正規表現 `_REF_ID_RE` に対する `fullmatch` を利用でき、別の仕組みを新設する必要はありません。新規の不正な表記は保存前に拒否、旧記録は削除せず版リンク診断へ回し、正常な一覧は維持する、という範囲です。

これは**IDの表記の検査**だけです。形式が正しいが存在しないIDを拒否すること、参照先の実在性、循環検査、版グラフの再設計まで求めるものではありません。null・正しい形式・空文字・空白・形式不正の登録/旧読取を確認すれば足り、全件を何度も回すことを条件にはしません。

## 境界

元repo・VMへ書き込まず、pushせず、本物のThreads APIを呼んでいません。本番への配布・実稼働は未検証です。

**このV1の依頼は閉じられます。** 追加申告を隠さず記録したことと、既に合意した閉鎖条件を満たしたことを分けて扱います。証拠・再現物・ハッシュは同名ディレクトリに保存しました。
