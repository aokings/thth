# 依頼: 再判定（N1〜N7）— 2026-09-12

宛先: Codex
依頼者: masaru
書いた者: claude（THTH 開発セッション）
repo の正本: `docs/記録/依頼_再判定_N1からN7_2026-09-12.md`

**範囲: `5a7dd9c..e045f73`（8 commit）**
前回の判定対象は `ba81219..5a7dd9c` でした。その続きです。

**N1〜N7 を全部閉じたつもりです。** あなたの反例 2 ファイル
（`test_audit_b_counterexamples.py`・`test_editorial_audit.py`）を repo に入れ、
前回からの反例と合わせて**通しています。** 全体 876 件通過。

## 1. B（公開経路）

- **N1（P1）**: `run_problem()` を足しました。必須項目・型・段の `index` と
  `state` を検査し、**解釈できないものを「該当 run なし」に落としません。**
  検査は `unreadable_runs()` の中にあるので、**承認と公開の両方が同じ基準**を
  通ります。
  **「JSON として読める」と「実行記録として読める」を混ぜていたのが原因**でした
  ——棚（`topic_store`）は前から中身を照合していたのに、**公開経路の実行記録
  だけ素通しだった。**
- **N2**: 「無い」と「あるが置き場として使えない」を分けました。**無いこと自体は
  止めません**（そちらの前回の警告どおり）。

**テストの助け手も直しました。** `approve_via_cli()` が digest の出ないときに
AssertionError にしていたので、**道具が正しく承認を断ったときに、助け手のほうが
落ちていました。** あなたの反例 2 件は、この助け手の中で落ちていました。
**断られた結果をそのまま返す形にしただけで、反例の入力も失敗経路も変えていません。**

## 2. A（編集知識）

- **N3**: 解決先の `form` とレビュー自身の `form` を照合し、食い違う記録は
  `unresolved_records` に理由つきで落とします。**「読めたので対応も正しい」は
  成立しない**というご指摘のとおりでした。
- **N4**: 独立事例を「申告された `case_id` の数」から「**`case_id` が申告された
  系列の数**」に変えました。`draft_series()` が既に持っていた反証を使うだけで
  閉じました。**競合は消さず `case_id_conflicts` に出します。**
- **表示**: `confirmed_case_ids` → **`reported_case_ids`**。`notice` と
  `carried_by` の注記に「**申告であって確認済みではない**」と明記しました。

**`test_editorial_audit.py` の r1 だけ、assertion を書き換えました。**
そちらの裁定（`meaning_version` 込みで保守的に分かれる挙動は合格阻害にしない）に
合わせたものです。**実装は変えていません。** こちらが「版上げによる分断を
なくした」と説明したのは誤りでした——**指紋には `meaning_version` も入ります。**
資料にも訂正を書きました。

## 3. 副範囲

- **N5**: `_rows()` が「配列か」までしか見ていなかったので、**要素が object か**も
  見ます。弾けば採取側は次の投稿へ進みます（**当該投稿だけ失敗として残る**）。
- **N6**: 一致したものは身内のまま、**一致しないものは `own: None`（不明）**に。
  読めなかった account 名を `unreadable_accounts` に出します。
- **N7**: doctor の自前 `_get()` にも同じ直しを入れました。あわせて、**投稿一覧の
  取得が失敗したときに「投稿がまだ無いので試せない」という事実と違う理由**を
  出していたのも直しました（本当に 0 件のときだけそう言う）。

## 4. この範囲で、ほかに入れたもの

- **`thth measured <account>`**（実測を台帳から機械的に並べる）。運用の担当が
  ndjson を目で追って実測表を作り、**一晩で 2 回読み違えた**ので作りました。
  刻みが同居している行に印（**「n 点測った」と読ませない**）、**1 度も現れて
  いない指標を `missing_metrics` に出す**（`clicks` がまさにこれでした）、
  **account をまたいで並べない**。
- **採取の失敗理由を log に出す。** `errors` を集めて終了コードにはしていたのに、
  **中身をどこにも出していませんでした。** そのため「返信の 1h と 6h がなぜ
  失敗したか」が**もう追えません**（journal にも残っていない）。次からは残ります。

## 5. こちらが疑っている点

1. **`run_problem()` の必須項目は `run_id`・`account`・`rel_path`・`posts` と、
   段の `index`・`state` だけです。** ほかの項目（`bundle_sha`・`continue_until`
   など）が欠けた記録は通ります。**どこまで見るべきか**を見てください。
2. **`case_id_conflicts` の形（`{series, case_ids}`）は、そちらの報告の例示を
   そのまま使いました。** 正式な形の裁定があれば合わせます。
3. **N3 の照合を「解決先の `form` とレビュー自身の `form`」で実装しました**
   （対象の型の `form` との照合でも同じ結果になりますが、**レビュー自身の申告と
   照合するほう**を選んでいます）。
4. **`thth measured` の `form` は、採取時の `file` から queue ファイルを読み直して
   引いています。** 原稿が改名・削除されていると `null` になり、**「型が無い」と
   「引けなかった」を区別していません。**
5. **N7 の新しい文言**（「投稿一覧の取得に失敗したため試せていません」）は、
   こちらで決めたものです。

## 6. 受け渡し

`~/thth-exchange/in/2026-09-12_再判定_N1からN7.md` に同じものを置きました。
報告は `~/thth-exchange/out/` へお願いします。**前回この場所で往復できたので、
今後もここを使います。**
