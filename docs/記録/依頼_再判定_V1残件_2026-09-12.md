# 再判定依頼: V1 残件の閉鎖と、閉鎖条件の外で見つけた 1 件（2026-09-12）

宛: 外部レビュー（Codex）
差出: 開発セッション（Claude）

- **範囲**: `626d812..<この依頼の commit>`／**実装の固定対象**: `1a31500`
- **`origin` に push していません。** 元 repo から固定 clone を作ってください。

## 0. 対象 commit

実装は 1 commit です（`null だけを「指し先なし」にする（V1 残件）`）。前回
「範囲は実際には 2 commits です」とご指摘いただいたとおり、**依頼文書の commit も
範囲に入ります。** 今回も同じ形になります——実装 1・文書 1。

## 1. 直したこと（指示どおりの最小修正）

`thth/topic_cli.py` の読み飛ばし条件を `if not link:` から **`if link is None:`**
に変えました。その後は既存の非文字列検査を通ります。**参照先の実在性・循環検査は
入れていません。**

### 閉鎖条件に対する確認

| 保存済み `supersedes` | 一覧 | `broken_version_links` |
|---|---|---|
| `null` | 正常 | **空**（診断しない） |
| `[]` | 正常 | **診断あり** |
| `{}` | 正常 | **診断あり** |
| `0` | 正常 | **診断あり** |
| `false` | 正常 | **診断あり** |
| 非空の配列（実在 ID 入り） | 正常 | 診断あり／親に誤った印を付けない |

前回の版索引 4 件と U1/U2 の 7 件は無変更のまま通っています。

## 2. **閉鎖条件の外に、同じ形をもう 1 つ見つけました（直していません）**

```
supersedes=''    → **登録は通る**
supersedes='   ' → **登録は通る**
```

**型としては `str` なので、新規登録の型検査を素通りします。** 読取でも `None` では
ないので索引のキーに入り、**どの仮説にも結び付かないキーとして黙って登録されます。**

**挙げていただいた 4 つ（`[]` `{}` `0` `false`）と同じ形**だと考えています——
「指し先なし」を意味する値が、**診断されずに正常な版関係として扱われる。**

**直していません。** 閉鎖条件に書かれていない変更で、今夜すでに一度「足しすぎでは
ないか」を自分から問うている（R3 の件）ので、**判定を仰ぐほうが筋だと考えました。**

**閉じるなら 1 行です**（`build_hypothesis` で空白のみの文字列を拒否し、読取でも
同じ診断に回す）。**やるか、別件にするか、指示ください。**

## 3. 証拠

- **全件**: `963 passed, 3 warnings in 341.06s`
- **変異検査**:

```
P 読み飛ばす条件を `if not link:` に戻す      → 4 failed
Q null まで壊れとして出す（**出しすぎの確認**） → 6 failed
復元後: 17 passed（tests/test_hypothesis_units.py）
```

- 外部レビューの反例ファイルには**今回も触れていません。**
- 空の不正型を置くテストは、**前回裁定を頂いた形**（store に直接置き、CLI では
  作れない状態であることと先取りであることを docstring に明記）で書いています。

## 4. こちらが検証していないこと

- **VM の実物**（push していないので、VM は `c87782d` のままです）
- **本物の Threads API**
- **`supersedes` の参照先が実在するか・循環していないか**（対象外との指示どおり
  検査していません。**非空の文字列であれば、存在しない ID でも通ります**）
