# 検収に出す主張: v2-3 媒体の追加（Bluesky・Mastodon・WhatsApp の芽）——開発セッション Fable・2026-09-13

**合格の申告ではありません。** 監査 2（まっさらな文脈の Opus 5）が各主張をコマンドで確かめ、食い違いを証拠つきで
返すための一覧です。対象は `main` の先端（`git log --oneline -1`。この文書の commit の 1 つ前がコードの先端）。

## A. 状態

- A1. `origin/release` は **`7902e51`**（v1.1.0）のまま。v2-3 は**配っていない**。
- A2. 全件テストは **1506 passed** 以上（`python -m pytest tests/ -q -n auto -p no:cacheprovider`・rc=0）。
  v1.1.0 時点は 1335。
- A3. Threads の既存テストは T0〜T3 で **1 本も変えていない**（変えたのは T1/T2 の自分のテスト 2 本と、
  `test_post_idにパス区切りがあれば書かない` の守り方、`tests/test_collect.py` の偽アダプタに `capabilities()` を足した件）。
- A4. worktree は `main` 1 本。BG 用は全部 merge 後に削除。

## B. 境界（T0・設計 §4.2）

- B1. `thth/adapters/__init__.py` の `REGISTRY` に `threads`・`bluesky`・`mastodon`。`core` は `make_adapter` だけを呼び、
  未知の `media` は既知の一覧を添えて loud に断る。
- B2. `base.Message` に `medium`・`author_key`・`reply_deadline`。`Reply = Message`。`author_key` は非可逆で媒体ごとに違う。
- B3. `Adapter.inbox()` の芽: `tests/helpers/fake_push_adapter.py` を REGISTRY に足すだけで `collect` が
  `data/sns/inbox/<YYYY-MM>.ndjson` に書き、`reply_deadline` が残る。**core は触っていない。**
- B4. `views` の無い実測は `comparable_views` が「媒体に views が無い（<medium>）」で数え、`--advise` は rc=0。
- B5. トークンの「判らない」（None）と「期限を持たない」（`no_expiry: true`）を言い分ける。board は `token=ok/期限なし`。
- B6. `select` の topic 検査は `topic` capability を持つ媒体だけ。Threads は現行どおり。
- B7. 文字数上限は `queuefile.limit_for(media, account_cfg)`（bluesky 300・mastodon 500・`char_limit` で上書き）。
  lint・select・core・bundle が同じ口を使う。

## C. Bluesky（T1）

- C1. facets は UTF-8 の**バイト位置**（日本語＋URL 2 本のテストで、文字位置とずれることも固定）。
- C2. 返信は `parent`/`root` の strongRef。親が返信なら root を引き継ぐ。
- C3. `getPostThread` → Message 3 階層。blocked は飛ばし notFound は失敗として上げる。
- C4. insights に `views` は無い（`available` に入らない）。
- C5. App Password・accessJwt は例外文・ログに出ない（5 本のテスト）。
- C6. L3（記憶で書いた）箇所は `thth/adapters/bluesky.py` の docstring に列挙してある（XRPC の口・人向け URL・
  createSession の上限・App Password の形・URL の正規表現・grapheme 近似・refreshSession 未実装）。

## D. Mastodon（T2）

- D1. `POST /api/v1/statuses` に `Idempotency-Key`。同じ本文で同じ、返信先と公開範囲で変わる、トークンを混ぜない。
- D2. `/context` → Message（`in_reply_to_id`・根は引数の post_id・HTML 除去）。descendants 欠落を「0 件」にしない。
- D3. `/api/v2/instance` から `max_characters`。
- D4. 一次資料 4 本を読解（L2）。L3 は「views が無い＝一覧に無い」「HTML 除去の限界」の 2 点だけ。

## E. 配線（T3）

- E1. `author_key` は両媒体とも `base.author_key(medium, identity)`。Bluesky は `did`、Mastodon は**ドメイン付き `acct`**。
- E2. `collect` の重複除去は `message_id` か `id`。Bluesky/Mastodon の返信が「id 欠落」で捨てられない。
- E3. `account_insights` capability。持たない媒体では日次を呼ばず `errors` に積まない。
- E4. doctor は `TOKEN_KEYS` で判定。Bluesky は「thth auth を先に」、app.env の一手は Bluesky には出ない。
- E5. `thth auth` は `media` で分岐。bluesky は `auth_interactive` → 600 で原子的に書く・端末でなければ断る・値を出さない。
  mastodon は `token set` へ案内して rc=2。
- E6. `token set` は Mastodon で `no_expiry: true`・`expires_in` 無し。Threads は `expires_in` あり。
- E7. `accounts/masaru-bluesky.json`・`accounts/masaru-mastodon.json` は **`production: false`・`scheduled: false`**。
  `thth board` が両方を `token=no_token` で出す。
- E8. 端から端（偽サーバ）: 両媒体で `throw` → post_id 書き戻し → `collect` → 返信 ndjson に `message_id`/`medium`/`author_key`、
  実測に `views: null`。
- E9. **`thth/postid.py`**: Bluesky の `at://` を含む post_id をファイル名に使うとき `quote(safe="")`。Threads のファイル名は
  1 文字も変わらない。
- E10. **`account_report.fetch_posts` は Threads 以外では graph.threads.net を叩かない**（Mastodon の access token が Meta に
  飛ぶ経路を止めた）。テストは HTTP の口を monkeypatch して**叩いたら即落ちる**（開発セッションの変異で、返り値しか
  見ていなかったのを直した）。

## F. やっていないこと（正直に）

- F1. **実物の Bluesky・Mastodon には 1 件も投げていない。** 偽サーバまで。実投稿は masaru の `thth auth`/`token set` の後。
- F2. `Adapter.recent_posts()` を境界に足していない（`fetch_posts` は Threads 専用のまま）。`thth account` の人向け画面が
  他媒体でも「Threads 側 : …」と出る。
- F3. `metrics_of()` が旧い平の dict を黙って受ける（規約 5 と逆向き・T0 の妥協）。
- F4. Bluesky の `refreshSession` は未実装（App Password で毎回 `createSession`）。
- F5. WhatsApp の実装は無い。芽（`inbox`・`reply_deadline`・偽 push アダプタ）だけ。
- F6. 泉・台帳の外出し・public 化・`thth share`・英語文書は v2 の別段（裁定待ち）。

## G. 監査の結果（2026-09-13・主張の後に起きたこと）

**監査 2**（主張の検証）: A1〜F6 は全部一致。L2 の出典照合は不一致ゼロ。指摘は文書側（Mastodon 導入文書 §4 の
`author_key` の式が配線前のまま・Bluesky 導入文書の `--dry-run` は実在しないフラグ・README の版と件数が古い・
T3 の「VM で試す手順」が設計 §3.2.1 に反する）。**全部直した**（`e36b4fb`）。

**監査 1**（壊しにいく）: P1 2 件・P2 2 件・P3 4 件・変異の網の穴 1 件。**全部直した**（`24c523e`）。
- P1-1 `thth refresh`／`maintain` が媒体を見ずに Meta の更新 API を叩き、Mastodon の token が 50 日後に自動で Meta へ
  飛んで返り値で上書きされる経路 → `"refresh"` capability の門を `run_refresh` の入口に。開発セッションの変異で門を外すと
  3 件落ちることを確認。
- P1-2 Bluesky の秘密の伏字が `publish`/`probe` だけ → 全経路で値ごと伏せる。`collect` の errors・ログ・ファイルまで固定。
- P2-3 doctor が Bluesky にも app.env の案内 → Threads だけ。P2-4 board/account/maintain の案内が媒体を見ない → `TOKEN_SETUP_HINT`。
- P3-5 inbox の月またぎの重複／P3-6 長すぎる post_id で collect が落ちる／P3-7 `author_key` が instance の綴りで変わる／
  P3-8 doctor と board の言い分の不一致／M6 `from_filename` の消費側テスト。

全件 **1552 passed・1 skipped**。`release` は `7902e51` のまま。
