# 実装計画: 記事別トピック提案 — 2026-09-11

**基準**: Codex の詳細設計（`/tmp/thth-topic-design-xIHnU2/設計_記事別トピック提案_2026-09-11.md`。
`docs/設計_記事別トピック提案_2026-09-11.md` として取り込む予定）。
**完成範囲**: P3 まで（masaru 指示 2026-09-11）。
**検収**: Codex の独立レビュー。「THTH を使う LLM が、実際の記事・投稿文・観測から
根拠付きでトピックを選び、既存の承認へ渡せる」ところまで。

**導入の条件**（masaru 指示）: 07:00 等の時刻に間に合わせることは条件にしない。
**小さく実装し、既存記録を読み込んだ実際の `--advise` 出力と独立レビューで
確認してから使い始める。**

---

## 工程

| # | 内容 | 設計の節 | 受け入れ | 状態 |
|---|---|---|---|---|
| 1 | 観測と判断の分離／継承の解消／実測の合算停止／「指示ではない」の明示 | §4.2・§8・§9・§2 表・§12.3・§5 | T07・T11 | **完了**（363 緑・実出力で確認） |
| 2 | 取得結果（`status`）を判断から分ける | §4.2 | T05 | **完了** |
| 3 | P0: schema・canonical hash・入力 snapshot・profile | §4.1〜4.5・§10・§13 | T02・T03・T26・T10 の一部 | **完了**（`thth/topic_models.py`・17 件） |
| 4 | P1: `topic_store.py`・legacy reader・observe/decision 保存 | §8・§9・§13 | T13・T14 | 未 |
| 5 | P2: `topic_advice.py`・suggest/evaluate・引用と参照の検査 | §4.4・§5・§6 | T01・T04・T06・T08〜T10・T25 | 未 |
| 6 | P3: CLI 互換 dispatcher・MCP 3 ツール・LLM 手順 | §6・§7 | T15〜T18・T27・T28 | 未 |

**P4 以降（ブラウザ証拠の運用・検索 API・実績照合・承認プレビュー）は今回の範囲外。**

---

## 設計から変えたところ（理由つき）

| 変更 | 理由 |
|---|---|
| **引用の文字 offset を省き、引用文字列の一致だけで検査する** | masaru 指示。空白の正規化ひとつでずれるので、脆いわりに得るものが少ない。改竄検知の目的は文字列一致で足りる |
| **既存 46 件を `legacy_note` の二級市民にせず、観測として全部読む** | masaru 指示「既存 22 語は活用する」。`audience` に書かれた「誰がいたか」は、取り直せない価値のある事実。**ただし当時の判断は「参考・アカウント未指定」として区別する**（§9 の趣旨は保つ） |
| **`by` から account を推測しない** | masaru 指示「不明な取得状況は推測で埋めない」。`claude（kopicha セッション）` から `kopicha-threads` を機械的に埋めると、**確かめていないことを確かめた形で残す**ことになる |
| **他アカウントの判断を「参考」として併記する** | 継承はしない（§8）が、隠すのも情報の損失。kopicha が `お茶` について書いた内容は、nigamilab が読む価値がある。**採らないが見せる** |

---

## 工程 1・2 の残件

- **旧 46 件に `--status` が無い。** 「検索結果 0 件」と書かれていても、取得条件の
  記録が無いので `empty` に変換しない（§9）。**出力に「取得結果を記録していません。
  『人がいない』と読み替えないでください」と注記する**形で据え置いた。
  各セッションが付け直すのは任意。
- **各アカウントの判断が 0 件から始まる。** 3 セッションに通知済み。
  `--account` を付けた記録が溜まるまで、`--advise` の「適合と判断した語」は空。
  **観測は全部見えるので、判断の材料は失われていない。**
- **`others_disagree()` が未使用のまま残っている。** 工程 4 で `topic_store` へ
  移すときに整理する。

## §10（profile）の変更 — masaru 裁定 2026-09-11

設計 §10 は「profile 不在は account 名から推測して埋めず、初回に LLM が仮 profile を
提案して利用者が確認する」とだけ書いていて、**誰が確認するか**が決まっていなかった。

**変更後の §10**:

> 初回 profile は**担当セッションが既存のプロジェクト方針に基づいて作成・確認する。
> 根拠資料と確認者を記録する。** 新しい方針の決定、既存方針の変更、解消できない
> 矛盾については masaru に確認する。

**確認の範囲を 3 つに分ける。**

| 場合 | 誰が決めるか |
|---|---|
| **既存方針を profile に整理する** | **担当セッションが確定してよい。**根拠と確認したセッションを記録する |
| **目的や編集方針を新しく決める・変更する** | **masaru に確認する** |
| **方針が不明・矛盾する** | **推測で確定しない。仮 profile のままにする** |

**これはトピック選定用 profile の確認権限であって、投稿そのものの承認権限は変えない。**
`thth approve` の二段確認と `--by` の記録は従来どおり。

実装への反映（工程 3）:

- profile に `status: confirmed | provisional`、`confirmed_by`（セッション）、
  `basis`（根拠資料の参照）を必須で持たせる。
- **`basis` が空のまま `confirmed` にできない**（根拠なしの確定を作らない）。
- `provisional` は設計 §4.5 のとおり `provisional` 判断の理由になる。
- 既存 profile の `primary_goal` / `editorial_scope` を**変更**する操作は、
  `--changes-policy` のような明示の申告を要求し、**その旨を記録に残す**
  （masaru に確認したことを人が後から辿れるように）。

## 工程 3（P0）でやったこと・残件

`thth/topic_models.py`。**保存も判断もしない**——形を決めて、検査して、ID を計算する
だけ。保存は工程 4（P1）、判断は工程 5（P2）。

固定したもの:

- `canonical_json()`（sort_keys・固定 separators・UTF-8・NaN 禁止）と
  `content_id()`（`sha256:` ＋ 64 桁）。**同じ中身なら必ず同じ ID。**
- `build_article()` — `content_sha256` と `article_id`。**`summary` で全文を代用しない。**
- `build_context()` — 本文・topic・宛先・返信先・予約時刻・記事内容・profile・観測が
  1 つでも変われば別の `context_id`（T02・T03・T26 をテストで固定）。
  **表示用のパスは ID に入れない**（同じ中身なら置き場所が違っても同じ判断）。
  **観測 ID の順序も影響しない。**
- `validate_proposal()` — **引用が記事本文にそのまま在るか**・観測 ID が実在するか・
  点数が 0〜3 か。**空白を整形して照合しない**（整形して合わせると検査にならない）。
- `build_profile()` — §10 の変更を実装で担保。**`basis` が空のまま `confirmed`
  にできない**／`confirmed_by` が要る／不明・矛盾は `provisional` のまま置ける。

**残件:**

- **判断の語彙が 2 つある。** `topic_models.FIT`（`suitable/unsuitable/uncertain`・
  設計 §4.4）と、既存 `topics.py` の `alive/mismatch/dead/unknown`。
  **工程 5 で寄せる。** 移行は機械的にできる（`--status` が付いているため）:
  `alive → suitable`、`mismatch/dead → unsuitable`、`unknown → uncertain`。
  **各セッションが入れ直す必要はない。**
- `source_state`（`local_draft / synced / unverified`）は形だけ用意した。
  実際の判定は工程 5 で `matches_synced_commit` を**必須にせずに**参照する。
- `policy_version`（鮮度の初期値 7 日等）はまだ定数化していない。工程 5 で決める。

## 工程 3 以降で決めること（設計に無い・実装時に確定する）

- **`state/topic_advice/` の置き場が VM ローカルであること**（§8）。Mac 側の
  `thth` ラッパは VM を呼ぶので実害は無いが、**VM を作り直すと消える**。
  既存 `topics.json` と同じ性質なので、今回は同じ扱いにする。
