# 2.13.0 リリースノート（未配布 draft）

実装・検証中です。版の配布、タグ作成、本番の媒体投稿はまだ行っていません。

- 添付の原稿、公開用 bytes の準備、alt・元/公開 SHA を含む承認を追加。第2段は Mastodon の対応画像・MP4/MOV の upload と処理待ちまで。第3段では Bluesky 画像の同一bytes upload・CID照合・images/gallery投稿を追加。他の媒体機能、ブラウザーの添付承認、残る型付き API 機能は後段です。
- `media` は top-level の予約 key になりました。既存原稿に非標準の `media:` 行がある場合、厳密な配列文法と合わなければ malformed として拒否します。本文だけへ落として公開はしません。
- MP4/MOV の未知 box／uuid と、非0 payload を持つ free/skip を拒否。repo_dir の祖先 symlink、添付型の重複、字幕言語の表示理由を明確化しました。

instance の実上限、実 upload の処理待ち、媒体での実表示は配布担当による本番実測が必要です。詳細は [添付原稿の手順](原稿_添付_2.13.md) を参照してください。

Mastodon は添付のために `write:media` を含む再認可が必要です。旧client/tokenを保全するscope世代別保存を追加し、旧pendingはやり直しを求めます。


第4段（ローカル候補）: Threads JPEG/PNGと最大20枚カルーセル、署名付きWorker/R2配信を追加。画像上限はC11の暫定SI8,000,000 bytes（8,000,001〜8,388,608帯はAPI実測待ち）。第4段時点では動画と招待添付承認画面は後段。R2/providerの実サービス検証と配布は未実施。

- 第5段候補: Threads MP4/MOV と画像混在カルーセル。C11 の既知規格を検査し、未観測codec条件／edit-list／moov配置は限定警告、privacy拒否は維持。動画1GBは暫定SI、provider1800秒はC10裁定であり実API実測ではない。

- C12追補: Threads動画のaudio/video bitrate宣言max/avgとcodec等は警告に限定。静的規格拒否はcontainer・duration・bytes・幅・比率、privacy／構造不正の拒否は維持。実媒体受理は未測定。

- 第6段候補: Bluesky MP4動画1本（300MB／600秒）、動画サービスの有界処理待ちと字幕20件を追加。入力公開SHAとサービス最適化後CIDを区別し、応答不明時は再POSTしません。実PDS／動画サービスと実表示は未測定です。
