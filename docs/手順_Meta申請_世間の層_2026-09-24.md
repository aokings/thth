# 手順: Meta の申請（外の人が masaru と同じに使える）

2026-09-24 に世間の層の手順として作り、2026-09-25 に 10 権限の申請の文書へ広げた（計画 `docs/計画_Meta申請_2026-09-25.md` の #4 と #5）。範囲は見通し `docs/見通し_Meta審査_2026-09-24.md` の案 2 と、その末尾の追記「公開βで 2 回目の審査を呼ばない書き方」に従う。Advanced を新しく求めるのは 6 権限。承認済みの 4 権限は説明を出し直す。

- 下敷き: 差し替えの文 `docs/手順_Meta説明の差し替え_2026-09-23.md` §2、招待リンク `docs/設計_3.10.0_招待リンク_2026-09-25.md` と `docs/運用_招待する側.md` §10、privacy と利用規約（`tools/build_site.py` の `build_privacy()`・`build_terms()`・どちらも 2026-09-25 施行）、世間の層の規約の照合 `docs/照合_観測の地図_集計の定点観測_2026-09-23.md`。
- **出すのも貼るのも masaru の手。** Dashboard に貼るのは英語。日本語は確認用の訳。
- 説明の文には「招待制」と書かない。書くのは公開βのあとも変わらない事実だけ（運営者の管理するサーバで動かす、運営者が各人を確かめてから使えるようにする、投稿・返信・削除は本人が承認する、token は人にも AI にも渡さない、データの扱いは privacy）。
- 各文の裏づけは、節ごとの「裏」の行に書いたコードの場所。**コードに無いことは書いていない。** Dashboard の欄名・ボタン名は、2026-09-23 に masaru が見たもの（差し替えの文 §0-3）のほかは**未確認**。

## 0. 何を出すか

| 権限 | いま | 今回 | 録画（§5） |
|---|---|---|---|
| `threads_basic` | 標準 | Advanced を求める | 録画 1 |
| `threads_content_publish` | 標準 | Advanced を求める | 録画 2（と録画 0） |
| `threads_manage_replies` | 標準 | Advanced を求める | 録画 3（と録画 0） |
| `threads_read_replies` | 標準 | Advanced を求める | 録画 4 |
| `threads_manage_insights` | 標準 | Advanced を求める | 録画 5 |
| `threads_delete` | 標準 | Advanced を求める | 録画 6（と録画 0） |
| `threads_keyword_search` | Advanced 承認済み | 説明の出し直し（集計の 180 日の保存を含む） | 録画 7a・7b |
| `threads_manage_mentions` | Advanced 承認済み | 説明の出し直し | 録画 8 |
| `threads_profile_discovery` | Advanced 承認済み | 説明の出し直し | 録画 9 |
| `threads_location_tagging` | Advanced 承認済み | 説明の出し直し | 録画 10 |

`threads_share_to_instagram` は求めない。招待リンクの認可もこの権限を外している（`thth/invites.py` の `invite_scopes()`）。C3（他人の枝の観測）も入れない。

承認済みの 4 権限を新しい申請でもう一度選べるかは**未確認**。選べれば §3 の流れ A、選べなければ流れ B で出す。

## 1. 貼る文（各権限の用途説明）

各権限の欄には §1.1 の前置きを先に貼り、その後に各節の本文を続ける。流れ B では、前置きと本文の間に §1.12 を挟む。

### 1.1 共通の前置き

> THTH is an open-source tool that lets a person publish and manage posts on their own social media accounts through their own AI assistant (for example Claude Code). The operator (gotoq) runs it on an operator-managed server: people connect their own Threads account on Meta's authorization screen and do not run a server or a Meta app of their own. The operator checks each person before their account can be used and can stop an account at any time. THTH acts only when the account's owner asks for it, through the owner's own assistant or on a thth.me page, and it never posts, replies or deletes on its own. Owners who want to review each action first can require approval on a thth.me approval page with a personal approval secret. Because an assistant can make mistakes, every account has safety limits that the owner controls: a minimum interval between posts, daily caps on posts and deletions, and an automatic stop when too many actions happen within ten minutes. An assistant can only tighten these limits; loosening them, resuming a stopped account or revoking the assistant's key is done by the owner on https://thth.me/activity with their secret, where they also see everything that was published or deleted. THTH itself sends no data to AI providers, and what it shows the owner is passed to the owner's assistant only when they use one. Access tokens reach the server through the authorization flow, are kept in owner-only files, are redacted from logs and output, and are never returned to AI assistants; an approval secret is shown once to be saved, and only a salted verifier of it is kept. Privacy policy: https://thth.me/privacy/ — Terms: https://thth.me/terms/

訳: THTH は、自分の SNS アカウントの投稿を、自分の AI アシスタント（例 Claude Code）を通して公開・管理するためのオープンソースの道具です。運営者（gotoq）が、運営者の管理するサーバで動かしています。利用者は Meta の認可画面で自分の Threads アカウントを接続し、自分のサーバや Meta アプリは持ちません。運営者は各人を確かめてから、そのアカウントを使えるようにします。どのアカウントもいつでも止められます。THTH が動くのは、アカウントの持ち主が、自分のアシスタントか thth.me のページで求めたときだけで、自分から投稿・返信・削除をすることはありません。一件ずつ先に確かめたい持ち主は、thth.me の承認ページで本人の承認 secret による承認を必須にできます。アシスタントは誤ることがあるので、どのアカウントにも持ち主が決める安全装置があります。投稿の最短間隔、投稿と削除の 1 日の上限、10 分の間に操作が多すぎたときの自動停止です。アシスタントはこれを締めることしかできません。緩める・止まったアカウントを戻す・アシスタントの鍵を取り消すのは、持ち主が https://thth.me/activity で secret を使って行い、そこで公開・削除されたものをすべて見られます。THTH 自身は AI の提供者にデータを送りません。THTH が持ち主に見せたものがその人の AI アシスタントに渡るのは、持ち主がアシスタントを使うときだけです。アクセストークンは認可の流れでサーバに届き、所有者だけが読めるファイルに置き、ログと出力では伏せ、AI アシスタントには返しません。承認 secret は保存してもらうために 1 度だけ表示し、残すのはソルト付きの検証値だけです。プライバシーポリシー: https://thth.me/privacy/ ・利用規約: https://thth.me/terms/

裏: 3.12.0 の設計（`docs/設計_3.12.0_承認を関所から選ぶものへ_2026-09-26.md`）、安全装置（`thth/guard.py`）、持ち主の画面（`/activity`）、認可の流れと招待（`thth/authflow.py`・`thth/invites.py`・`callback/src/invite.js`）、承認ページ（`callback/src/approval.js`・PBKDF2 は privacy の "Human approval pages"）、二段の確認（`thth send` の dry-run と `--confirm`・`thth approve` の一段目と二段目・`thth/core.py`）、運営者の停止（`thth account leave`・`thth/leave.py`）、AI の扱い（privacy の "AI (LLM) sessions"）、token の扱い（利用規約の "What the operator does and does not do"）。

### 1.2 `threads_basic`

> threads_basic identifies the Threads account THTH acts for and lets the owner see their own posts. When the account is connected, THTH reads its user ID and username (/me) and keeps them with the account, so every later action is bound to that account; `thth doctor <account>` repeats this check and shows what the token can do. `thth posts <account>` lists the account's own recent posts (ID, link, time and text) so the owner can check what is live and choose a post to reply to or delete. Before a reply is sent, THTH may read the one post being answered to record a one-way key for its author; the text of that post is discarded.

訳: threads_basic は、THTH がどの Threads アカウントのために動くかを確かめ、持ち主が自分の投稿を見られるようにします。アカウントを接続したとき、THTH はユーザー ID とユーザー名（/me）を読んでアカウントと一緒に保存し、その後の操作をすべてそのアカウントに結びつけます。`thth doctor <account>` は同じ確認をもう一度行い、その token で何ができるかを示します。`thth posts <account>` はそのアカウント自身の最近の投稿（ID・リンク・時刻・本文）を並べ、持ち主が何が出ているかを確かめて、返信や削除の対象を選べるようにします。返信を送る前に、答える先の投稿 1 件を読み、投稿者の一方向の鍵を記録することがあります。その投稿の本文は捨てます。

裏: `whoami`（`/v1.0/me?fields=id,username`）は `thth/oauth.py` の認可の直後と `thth/doctor.py`、`recent_posts`（`id,permalink,timestamp,text,topic_tag`）は `thth/account_report.py`（`thth posts`）・`thth/retract_cli.py`・`thth/inflight_resolve.py`、返信先の `fetch_post` は `thth/core.py`（`author_key` だけを埋め、本文は捨てる）。

### 1.3 `threads_content_publish`

> THTH publishes a post when the owner asks for it: through their own AI assistant (the assistant sends the text and THTH publishes it, or schedules it when the account has a scheduler), or, if the owner requires approval, after they approve its exact content on a thth.me approval page. The text, with its topic tag, reply target, place and attachments if any, is sent unchanged through the container-then-publish flow, and when approval is required, changing any of these afterwards requires a new approval because the approved content is bound by a digest. Before publishing, THTH checks the account's safety limits (minimum interval, daily cap, quiet hours, burst stop) and refuses with the time when it may try again. Images have location and other embedded metadata removed before upload, and attachments reach Threads through short-lived links. The resulting post ID and link are recorded in the account's post history on the operator's server.

訳: THTH は、持ち主が求めたときに投稿を公開します。持ち主が自分の AI アシスタントを通して求めるとき（アシスタントが本文を送り、THTH が公開する。予約を使うアカウントなら予約する）と、持ち主が承認を必須にしているなら、thth.me の承認ページでその内容そのものを承認したときです。本文は、話題のタグ・返信先・場所・添付があればそれも含めて、変えずにコンテナ作成→公開の流れで送ります。承認を必須にしているときは、あとでこれらを変えると承認のやり直しが要ります。承認した内容は digest で結ばれているからです。公開の前に、THTH はアカウントの安全装置（最短間隔・1 日の上限・夜間・急な連投での停止）を確かめ、越えるときは、もう一度試せる時刻を添えて断ります。画像はアップロードの前に位置情報などの埋め込みのメタデータを取り除き、添付は短い期限のリンクで Threads に渡します。できた投稿の ID とリンクは、運営者のサーバのそのアカウントの投稿の履歴に記録します。

裏: `publish`・`publish_container`（`thth/adapters/threads.py`・`thth/adapters/threads_media.py`）、digest に入る項目（`thth/approval.py` の `_OPTION_ORDER`・`thth/core.py`・利用規約の「承認のあとで…変えたら」）、承認の直後の公開（`thth admin approval request --send`・`thth send --production --confirm`）、予約（`thth run` の timer）、画像のメタデータ（privacy の "Attachments"）。

### 1.4 `threads_manage_replies`

> THTH uses threads_manage_replies to post the owner's replies: to replies under their own posts, to mentions of their account, or to a public post they chose to join. A reply names the post it answers and is sent when the owner asks for it through their own assistant, or after they approve its exact text and reply target on a thth.me approval page if they require approval, the same way as a post. Replies count toward the daily cap and the burst stop. THTH never replies on its own. It does not hide replies or change the reply settings of existing posts.

訳: THTH は threads_manage_replies を、持ち主の返信を送るために使います。返信の相手は、自分の投稿への返信、自分のアカウントへの言及、または持ち主が加わると決めた公開の投稿です。返信はどの投稿に答えるかを持ち、投稿と同じく、持ち主が自分のアシスタントを通して求めたとき、または承認を必須にしているなら thth.me の承認ページでその本文と返信先を承認したときに送ります。返信も 1 日の上限と急な連投での停止に数えます。THTH が自分から返信することはありません。返信を非表示にしたり、既にある投稿の返信の設定を変えたりはしません。

裏: 返信は `reply_to` を付けた公開（`thth send --reply-to`・下書きの `reply_to:`・`thth admin draft put --reply-to`）。返信先は digest に入る（`thth/approval.py`）。非表示の口は無い（`thth/adapters/threads.py` に hide の呼び出しが無い・見通し §1）。

### 1.5 `threads_read_replies`

> THTH reads the conversation under the owner's own posts so they can see who answered and decide whether to reply. Its collection (`thth collect`) fetches the full reply tree of each recent post at fixed ages after publishing, from 1 hour to 30 days, and saves each reply's username, text, time and link in the account's records on the operator's server. `thth replies <account>` shows these replies, and `thth unanswered <account>` lists the ones not yet answered. `thth thread <account> <post_id>` reads a branch on request and saves nothing. The saved replies are kept until the account leaves THTH.

訳: THTH は、持ち主の投稿の下の会話を読み、誰が答えたかを見て返信するかを決められるようにします。採集（`thth collect`）は、最近の投稿ごとに、公開から 1 時間〜30 日の決まった経過時間に返信の全階層を取り、各返信のユーザー名・本文・時刻・リンクを運営者のサーバのそのアカウントの記録に保存します。`thth replies <account>` はそれらの返信を表示し、`thth unanswered <account>` はまだ答えていないものを並べます。`thth thread <account> <post_id>` は求められたときに枝を読み、何も保存しません。保存した返信は、アカウントが THTH を離れるまで持ちます。

裏: `conversation`（`/v1.0/{id}/conversation`・`thth/adapters/threads.py`）、刻み `AGE_MARKS_HOURS = [1, 6, 24, 72, 168, 720]`（`thth/collect.py`）、表示（`thth/replies.py`・`thth/unanswered.py`）、保存しない枝の読み（`thth/thread_read.py`）、保持（privacy の "Retention"）。

### 1.6 `threads_manage_insights`

> THTH measures how the owner's posts were received so they can learn which topics and forms work for them. Its collection reads each post's views, likes, replies, reposts, quotes and shares at fixed ages after publishing (1 hour, 6 hours, 1, 3, 7 and 30 days), and once a day reads the account's metrics for the previous day (views, likes, replies, reposts, quotes, followers and link clicks). The numbers are saved in the account's records on the operator's server and shown to the owner with `thth measured <account>`, `thth topics <account>` and the daily observation summary. If the owner chooses, figures computed from their own metrics can be posted on THTH's plaza, which by default is visible only within the same project; showing a post to other owners requires joining and a two-step confirmation.

訳: THTH は、持ち主の投稿がどう受け取られたかを測り、どの話題や形がその人に合うかを学べるようにします。採集は、投稿ごとに公開から決まった経過時間（1 時間・6 時間・1 日・3 日・7 日・30 日）に、表示・いいね・返信・再投稿・引用・シェアを読みます。また 1 日 1 回、前日のアカウントの指標（表示・いいね・返信・再投稿・引用・フォロワー・リンクのクリック）を読みます。数は運営者のサーバのそのアカウントの記録に保存し、`thth measured <account>`・`thth topics <account>`・毎日の観測の一枚で持ち主に見せます。持ち主が選べば、自分の実測から計算した数を THTH の広場に書けます。広場は既定では同じ project の中だけで見えます。ほかの持ち主に見せるには、参加と二段の確認が要ります。

裏: `insights`（`POST_METRICS`）と `account_insights`（`thth/adapters/threads.py`）、刻みと前日の 1 行（`thth/collect.py` の `AGE_MARKS_HOURS`・`_collect_account_daily`）、表示（`thth measured`・`thth topics`・`thth/morning.py`）、広場の見える範囲（privacy の "Plaza"・`thth/plaza.py`）。

### 1.7 `threads_delete`

> THTH deletes a published post only when the owner asks for it: through their own AI assistant, on a thth.me approval page that shows the post and the reason if the owner requires approval for deletions, or, for the operator's own accounts, with `thth retract <account> <post_id>`, which first shows the post and a digest and deletes only when that digest is confirmed. Deletions have their own daily cap and count toward the burst stop, so a malfunctioning assistant cannot delete an account's history. THTH sends one DELETE request for that post ID and reports success only when Threads confirms it. The time, the person and the reason are added to the account's record of that post. THTH never deletes posts on its own.

訳: THTH が公開済みの投稿を削除するのは、持ち主が求めたときだけです。持ち主が自分の AI アシスタントを通して求めるか、削除に承認を必須にしているなら投稿と理由を表示した thth.me の承認ページで承認します。削除には別の 1 日の上限があり、急な連投での停止にも数えるので、誤ったアシスタントがアカウントの履歴を消してしまうことはありません。運営者自身のアカウントでは `thth retract <account> <post_id>` を使い、まず投稿と digest を表示し、その digest を確かめたときだけ削除します。THTH はその投稿 ID に DELETE を 1 回送り、Threads が確かめたときだけ成功と言います。時刻・実行した人・理由を、そのアカウントのその投稿の記録に書き足します。THTH が自分から投稿を削除することはありません。

裏: `delete_post`（1 回だけ・`success: true` のときだけ成功・`thth/adapters/threads.py`）、二段（`thth/retract_cli.py`）、承認ページの削除（`thth admin approval request --retract`・`thth/server_writes.py` の `retract_request`・`callback/src/approval.js` の "Delete this post"）、記録（`retracted_at`・`retracted_by`・`retract_reason`）。

### 1.8 `threads_keyword_search`

> THTH uses keyword search so the owner can see what people are saying about a topic before posting or joining a conversation. On request (`thth topics <account> --search <word>`, `thth where <account> <words>`), and in the daily observation summary using up to 5 keywords the operator sets for the account, it shows counts, distinct authors, the latest time and, for individual posts, the author, a link and the first 60 characters; these results are shown for that request only, and their text and authors are not saved. Separately, the observation map, which is off by default and turned on by the operator, searches at most once a day for topic keywords that a person registered for a project (at most 20; @handles, URLs and personal names are refused). From those results it saves only daily figures per keyword for at most 180 days: the number of posts, distinct authors, the share of the three most frequent authors, a rounded age of the latest post and co-occurrence counts, with any number below 5 stored as missing. It never saves post text, post IDs, usernames or links, shows the figures only within that project, and deletes them when the project leaves or on request. When the owner wants to join a conversation found this way, THTH can show that public post's thread on request (`thth thread <account> <post_id>`, read on the spot and not saved) and reads the one post being answered when a reply is prepared; any reply the owner makes is handled like a post (section on threads_manage_replies).

訳: THTH は、持ち主が投稿する前や会話に加わる前に、その話題について人が何を言っているかを見られるように語の検索を使います。求められたとき（`thth topics <account> --search <語>`・`thth where <account> <語>`）と、運営者がアカウントに設定した最大 5 語を使う毎日の観測の一枚で、件数・投稿者の異なり数・最新の時刻と、個々の投稿の投稿者・リンク・先頭 60 字を見せます。これらの結果はその求めに対してだけ表示し、本文と投稿者は保存しません。これとは別に、観測の地図は、既定で無効で運営者が有効にしたときだけ、人が project に登録した話題の語（最大 20・@ハンドル・URL・人名は断る）を 1 日 1 回まで検索します。その結果から保存するのは語ごとの日々の数だけで、最大 180 日です。件数・投稿者の異なり数・上位 3 人の占有率・丸めた最新の投稿からの経過・語どうしの共起の数で、5 未満の数は欠測として保存します。本文・投稿 ID・ユーザー名・リンクは保存せず、数はその project の中だけで見せ、project が離れたときと依頼があったときに削除します。こうして見つけた会話に持ち主が加わりたいときは、求めに応じてその公開投稿の枝を見せ（`thth thread <account> <post_id>`・その場で読み、保存しない）、返信を用意するときは返信先の 1 件を読みます。持ち主の返信は投稿と同じ扱いです（threads_manage_replies の節）。

裏: 求めの検索（`thth/threads_read_cli.py` の `--search`・`thth/where_cli.py`）、見つけた投稿の枝（`thth/thread_read.py`・保存しない。許可用途の照合は `docs/照合_MetaPlatformTerms_枝と語の観測_2026-09-18_Codex.md` §4 の K・設計 3.11.0 §5）、観測の一枚（`thth/morning.py`・語は `thth admin watch`・`thth/accounts.py` の `watch_words`）、60 字（差し替えの文 §1）、地図（`thth/map_world.py` の `NODE_KEYS`・`EDGE_KEYS`・`THTH_MAP_WORLD`・`thth/map_store.py`）、180 日と削除（privacy の "Observation map"・`thth admin map purge`）。

### 1.9 `threads_manage_mentions`

> THTH retrieves public mentions of the owner's own Threads account so they can see who is talking to them and decide whether to answer. Its collection saves each mention (username, text, time and link) in the account's records on the operator's server. `thth mentions <account>` shows current mentions on request and marks the ones already answered, and the daily observation summary lists the ones not yet answered. Any answer is a reply that goes through the same human approval as a post; THTH never replies automatically. Saved mentions are kept until the account leaves THTH.

訳: THTH は、持ち主の Threads アカウントへの公開の言及を取得し、誰が話しかけているかを見て答えるかを決められるようにします。採集は言及ごとに（ユーザー名・本文・時刻・リンク）を運営者のサーバのそのアカウントの記録に保存します。`thth mentions <account>` は求められたときに今の言及を表示して答え済みのものに印を付け、毎日の観測の一枚はまだ答えていないものを並べます。答えは返信で、投稿と同じ人の承認を通ります。THTH が自動で返信することはありません。保存した言及は、アカウントが THTH を離れるまで持ちます。

裏: `mentions`・`inbox`（`thth/adapters/threads.py`）、保存（`thth/collect.py` の `_collect_inbox`・`data/sns/inbox/`）、表示だけ（`thth/threads_read_cli.py` の `cmd_mentions`・`thth/morning.py` は答え済みを除く）。

### 1.10 `threads_profile_discovery`

> THTH looks up public Threads profiles on the owner's request so they can understand the accounts in their conversations, for example someone who replied to or mentioned them, or an account posting on the same topic, before deciding whether and how to engage. `thth profile <account> <username>` (and `thth who <account> @username --profile`) requests only public fields: username, name, profile picture, biography, follower count, total likes, quotes, reposts and views, and verification status. The result is shown for that request and is not saved. THTH does not look up profiles on its own.

訳: THTH は持ち主が求めたときに Threads の公開プロフィールを引き、会話にかかわるアカウント（たとえば返信や言及をしてきた人、同じ話題で投稿しているアカウント）がどんな相手かを、関わるか・どう関わるかを決める前に把握できるようにします。`thth profile <account> <username>`（と `thth who <account> @username --profile`）が求めるのは公開の項目だけです。ユーザー名・名前・プロフィール画像・自己紹介・フォロワー数・いいね・引用・再投稿・表示の累計・認証の有無です。結果はその求めに対して表示し、保存しません。THTH が自分からプロフィールを引くことはありません。

裏: `profile_lookup`（`PROFILE_FIELDS`・`thth/adapters/threads.py`）、呼ぶのは `thth/threads_read_cli.py` の `cmd_profile` と `thth/who_cli.py` だけ。

### 1.11 `threads_location_tagging`

> A draft may name a place. `thth location search <account> <name>` shows up to 5 candidate places (name, address, city, country and ID), and the owner writes the chosen place into the draft. The approval step shows the place name and ID with the text, the place is part of the approved content, and only an approved post carries the tag. THTH never infers or adds a location on its own, and location search results are not saved.

訳: 下書きに場所を書けます。`thth location search <account> <名前>` が候補の場所を最大 5 件（名前・住所・市・国・ID）表示し、持ち主が選んだ場所を下書きに書きます。承認の段では本文と一緒に場所の名前と ID を表示し、場所は承認の対象に入り、承認された投稿にだけ場所が付きます。THTH が場所を推測したり自分で付けたりすることはなく、場所の検索結果は保存しません。

裏: `location_search`（`LOCATION_SEARCH_FIELDS`・`thth/adapters/threads.py`）、5 件（`thth/retract_cli.py` の `LOCATION_LIMIT`）、承認の一段目の「場所:」の行（`thth/cli.py`）、digest（`thth/approval.py`）。

### 1.12 流れ B で前置きの後に挟む段落

承認済みの 4 権限を新しい申請に入れられないときだけ使う（§3 の流れ B）。

> Update to permissions approved earlier: threads_keyword_search, threads_manage_mentions, threads_profile_discovery and threads_location_tagging were approved for this app on 2026-09-23. How THTH uses them has changed since that submission: it now runs on the operator's server rather than on each user's machine, it saves mentions of the owner's account in that account's records on the server, and it can save daily aggregate keyword figures for up to 180 days (the observation map, off by default). We have sent the current descriptions of those four permissions through "Ask a question" on the approved submission, and the observation map stays off until that is answered.

訳: 先に承認された権限の更新: threads_keyword_search・threads_manage_mentions・threads_profile_discovery・threads_location_tagging は 2026-09-23 にこのアプリで承認されました。その提出のあとで THTH の使い方が変わりました。各利用者の機械ではなく運営者のサーバで動くこと、持ち主のアカウントへの言及をサーバのそのアカウントの記録に保存すること、語の検索の日々の集計を最大 180 日保存できること（観測の地図・既定で無効）です。この 4 権限の今の説明は、承認済みの提出の「質問する」から送りました。返事があるまで観測の地図は無効のままにします。

### 1.13 流れ B の「質問する」の文

承認済みの提出（2026-09-22 23:20 JST 送信・承認）の「質問する」に貼る。最後に §1.8〜§1.11 の英語の本文を 4 つ続ける（前置きは付けない）。

> Hello. This app (THTH) was approved on 2026-09-23 for threads_keyword_search, threads_manage_mentions, threads_profile_discovery and threads_location_tagging. Since then, how the app uses them has changed. We want the descriptions on file to stay complete and accurate (Platform Terms 7(a)), and we want to confirm whether the change needs review before we turn on the new processing (Platform Terms 3(a)(vii)). The changes are: (1) THTH runs on the operator's server for the people who use it, instead of on each user's machine; (2) mentions of the owner's own account are saved in that account's records on the server; (3) an optional daily keyword aggregate, off by default, saves only counts per keyword and day for at most 180 days, and never post text, post IDs, usernames or links. We have submitted a new App Review request for six other permissions, and its descriptions explain these changes. Could you tell us (a) whether the four approved permissions need to be reviewed again, and (b) if so, from which screen we should submit the updated descriptions? The updated descriptions follow. Privacy policy (effective 2026-09-25): https://thth.me/privacy/

訳: こんにちは。このアプリ（THTH）は 2026-09-23 に threads_keyword_search・threads_manage_mentions・threads_profile_discovery・threads_location_tagging の承認を受けました。そのあと、アプリの使い方が変わりました。登録してある説明を完全かつ正確に保ちたいこと（Platform Terms 7(a)）と、新しい処理を有効にする前に審査が要るかを確かめたいこと（Platform Terms 3(a)(vii)）から、お尋ねします。変わったのは、(1) THTH が各利用者の機械ではなく、使う人のために運営者のサーバで動くこと、(2) 持ち主のアカウントへの言及をサーバのそのアカウントの記録に保存すること、(3) 既定で無効の日々の語の集計が、語と日ごとの数だけを最大 180 日保存し、本文・投稿 ID・ユーザー名・リンクは保存しないこと、です。ほかの 6 権限については新しい App Review を申請し、その説明にもこの変更を書きました。(a) 承認済みの 4 権限を審査し直す必要があるか、(b) 必要なら、更新した説明をどの画面から出せばよいかを教えてください。更新した説明を以下に続けます。プライバシーポリシー（2026-09-25 施行）: https://thth.me/privacy/

## 2. 審査員向けの試験の手順（Test instructions）

欄: 申請の試験の手順の欄（**欄名は未確認**）。`<INVITE_URL_1>` などは、提出の直前に作った招待 URL に masaru が置き換える（§2.2 の 1）。この文書にも会話にも URL の実物は書かない。

### 2.1 貼る文

承認 URL の届け方（旧 `<HOW_URLS_REACH_YOU>`）は §2.3 の 1 で決め、3.11.0 の承認待ちの一覧（`https://thth.me/pending`）で埋めた。**Worker の 3.11.0 の deploy が済んでから貼る**（deploy の前は一覧のページが無い）。

> **How to test THTH (about 10 minutes of your time)**
>
> You need a Threads test account and a place to keep one secret (a password manager is ideal). Each link below is valid only during the review and can be used once; if a link says it is no longer valid, use the next one. If you already finished steps 1–3 with your test account, skip them and keep using the secret you saved.
>
> - Invitation 1: `<INVITE_URL_1>`
> - Invitation 2: `<INVITE_URL_2>`
> - Invitation 3: `<INVITE_URL_3>`
>
> 1. Open an invitation link. The page "Invitation to THTH" explains what will happen and lists the permissions requested. Press "Threads で認可する / Authorize with Threads".
> 2. After a few seconds the page shows "Open Threads authorization". It opens in a new tab: log in with your Threads test account and allow the permissions. Go back to the first tab; it continues on its own (within 10 minutes).
> 3. The page "Your account is ready" shows your Threads username and your THTH account name. Press "Show approval secret" and save the secret right away; it is shown only once. (Username: the account name. Website: thth.me.)
> Normally an owner asks their own AI assistant to post, reply or delete. So that you can test without setting up an assistant, your test account is created with approval required: the operator prepares the actions, and nothing happens until you approve each one on thth.me.
>
> 4. The operator is notified when your account is created and then prepares three actions for it, one after another: a post, a reply to that post, and the deletion of that post. You do not need a link from the operator: open https://thth.me/pending and sign in with the same username and approval secret (your password manager can fill them in). The list shows each action waiting for your approval; reload it to see the next one. Each action stays in the list for up to 24 hours, and an approval page opened from the list is valid for 10 minutes (if it expires, open it again from the list).
> 5. In the list, press "開く / Open" on the first action to open its approval page ("Publish this content"). It shows the exact text. Enter your approval secret and press "Approve". The page confirms "Publication approval received", and the post appears on your Threads test account shortly after (check it in the Threads app).
> 6. Open the second action from the list. Its approval page shows the reply text and the post it answers. Approve it; the reply appears under your post.
> 7. Open the third action from the list ("Delete this post"). It shows the post and the reason. Approve it; the post disappears from Threads.
>
> Because your test account requires approval, nothing is published, replied to or deleted unless you approve it on such a page with your secret. You can also open https://thth.me/activity with the same username and secret to see what happened on the account, stop it, and change its settings.
>
> **Features without a thth.me screen.** Insights, replies, mentions, keyword search (including the daily keyword aggregates of the observation map), profile lookup and location search run on the operator's server and are shown to the account owner through their own THTH session (command line or their own AI assistant). There is no web screen for them, so each is shown in the screencast attached to that permission, recorded with accounts that have a role on this app. During the review the operator also runs these read-only tools once on your test account.
>
> **After the review** the operator removes your test account from THTH (`thth account leave`), which deletes its data on the operator's server. You can also remove THTH from your Threads settings.

訳:

> **THTH の試し方（審査員の手で約 10 分）**
>
> Threads のテスト用アカウントと、secret を 1 つ保存する場所（パスワード管理が最適）が要ります。下のリンクはどれも審査の間だけ有効で、1 回だけ使えます。「使えません」と出たら次のリンクを使ってください。テスト用アカウントで 1〜3 を済ませていれば飛ばし、保存した secret を使い続けてください。
>
> 1. 招待リンクを開きます。「THTH への招待」のページが何が起きるかを説明し、求める権限を並べます。「Threads で認可する / Authorize with Threads」を押します。
> 2. 数秒で「Threads の認可ページを開く」が出ます。新しいタブで開くので、Threads のテスト用アカウントでログインし、権限を許可します。最初のタブに戻ると自動で進みます（10 分以内）。
> 3. 「用意ができました」のページに Threads のユーザー名と THTH の口座名が出ます。「承認 secret を表示する」を押し、すぐに保存します。表示は 1 度だけです（ユーザ名は口座名、Web サイトは thth.me）。
> ふだんは持ち主が自分の AI アシスタントに投稿・返信・削除を頼みます。アシスタントを用意せずに試せるよう、テスト用アカウントは承認を必須にして作ります。運営者が操作を用意し、thth.me であなたが一つずつ承認するまで何も起きません。
>
> 4. 口座ができると運営者に通知が届き、運営者がその口座のために 3 つの操作を順に用意します。投稿、その投稿への返信、その投稿の削除です。運営者からのリンクは要りません。https://thth.me/pending を開き、同じユーザ名と承認 secret で入ります（パスワード管理が入れてくれます）。一覧にあなたの承認を待つ操作が並びます。次の操作は一覧を読み込み直すと出ます。操作は一覧に最大 24 時間残り、一覧から開いた承認ページは 10 分有効です（切れたら一覧から開き直します）。
> 5. 一覧の 1 つ目の操作で「開く / Open」を押し、承認ページ（「この内容を公開」）を開きます。本文そのものが出ます。承認 secret を入れて「承認」を押します。「公開の承認を受け付けました」と出て、少しあとにテスト用アカウントに投稿が出ます（Threads のアプリで確かめてください）。
> 6. 一覧から 2 つ目の操作を開きます。承認ページに返信の本文と返信先が出ます。承認すると、投稿の下に返信が出ます。
> 7. 一覧から 3 つ目の操作（「この投稿を削除」）を開きます。投稿と理由が出ます。承認すると、投稿が Threads から消えます。
>
> テスト用アカウントは承認を必須にしているので、このページで secret を入れて承認しない限り、公開・返信・削除は起きません。同じユーザ名と secret で https://thth.me/activity を開くと、そのアカウントで起きたことを見て、止め、設定を変えられます。
>
> **thth.me に画面の無い機能。** 実測・返信・言及・語の検索（観測の地図の日々の集計を含む）・プロフィールの参照・場所の検索は運営者のサーバで動き、持ち主には自分の THTH のセッション（コマンドか自分の AI アシスタント）で見えます。Web の画面は無いので、それぞれ、その権限に添えた録画（アプリに役割を持つアカウントで撮ったもの）で示します。審査の間、運営者はテスト用アカウントでもこれらの読む道具を 1 回ずつ動かします。
>
> **審査のあと**、運営者がテスト用アカウントを THTH から外します（`thth account leave`）。運営者のサーバのそのデータは削除します。Threads の設定から THTH の接続を外すこともできます。

裏: ページの見出しとボタンは `callback/src/invite.js`（"Invitation to THTH"・"Authorize with Threads"・"Open Threads authorization"・"Your account is ready"・"Show approval secret"）と `callback/src/approval.js`（"Pending approvals"・"開く / Open"・"Publish this content"・"Delete this post"・"Approve"・"Publication approval received"・開いてから 10 分・一覧に最大 24 時間）。1 回だけ使える招待と、同じ Threads 口座の 2 回目の断り（`docs/設計_3.10.0_招待リンク_2026-09-25.md` §2）。管理者の通知（`docs/運用_招待する側.md` §10 の 3）。

注:

- 承認ページの補足の見出し（媒体・返信先・削除理由など）は 3.11.0 で「日本語 / English」になった（`callback/src/approval.js` の `labels`）。返信先の行は「返信先 / Reply to: <post_id>」と出る。
- 画像を添えた承認は、一覧に出ても期限が 10 分のまま（画像の表示は 10 分より延ばせない）。§2.1 の 3 つの操作は本文だけなので 24 時間。
- 審査員のテスト用アカウント（アプリに役割が無い）が、審査中の権限を認可できるかは Meta の側の扱いで、**未確認**。
- 審査員が 1 人か、権限ごとに別の人かは**未確認**。別の人が試すと招待が足りなくなるので、3 本入れる。
- 「公開の承認を受け付けました」のあと実際に出るまでの時間は、承認 worker の巡回しだい。何秒と約束する数字は書いていない。

### 2.2 審査の間の運営者の手順

打つのは masaru（VM に `ssh -t` で入る）。台本は `docs/運用_招待する側.md` §10 の「審査の間の手順」と同じ。`<口座>` は `inv-<project>-<6 桁>`。

1. **提出の直前**に招待を 3 本作る。URL は tty に 1 度だけ出る。Dashboard の欄にだけ貼る。

   ```sh
   thth admin invite create --media threads --project <審査用の project> --label meta-review-1 --expires 30d --production --approval all --by masaru
   ```

   project は持ち主の組に入っていない名前にする（組に入っていると `invite_project_in_owner_group` で断られる）。審査が 30 日を超えそうなら、切れる前に作り直して試験の手順を差し替える（**提出後に差し替えられるかは未確認**）。
2. 審査員が使ったかは、`thth admin invite list` の状態（`used`）と `approver_set`、それに管理者の通知（`account_added`）で見る。
3. 投稿の承認を出す。

   ```sh
   printf '%s\n' 'Testing THTH for Meta App Review. This post was approved on a thth.me approval page.' > ~/review/post.txt
   thth admin draft put <口座> --body-file ~/review/post.txt --by masaru
   thth admin approval request <口座> --draft <draft_id> --send --by masaru
   ```

   2 行目が `draft_id` を出し、3 行目がその操作を審査員の承認待ちの一覧に出す（最大 24 時間・出力に「承認待ちの一覧」の行と承認 URL）。URL は渡さなくてよい（審査員は https://thth.me/pending から開く）。
4. 公開されたら post_id を見る: `thth posts <口座> --limit 3`。
5. 返信の承認を出す。

   ```sh
   printf '%s\n' 'A reply, approved the same way.' > ~/review/reply.txt
   thth admin draft put <口座> --body-file ~/review/reply.txt --reply-to <post_id> --by masaru
   thth admin approval request <口座> --draft <draft_id> --send --by masaru
   ```

6. 削除の承認を出す: `thth admin approval request <口座> --retract <post_id> --reason review --by masaru`。公開された返信も同じ形で消せる。
7. 読む道具を審査員の口座で 1 回ずつ動かす（§2.1 の約束・30 日以内の呼び出しの足しにもなる）。

   ```sh
   thth collect <口座>
   thth replies <口座> --refresh
   thth mentions <口座>
   thth topics <口座> --search <語>
   thth profile <口座> threads
   thth location search <口座> 渋谷駅
   ```

8. 審査が終わったら（通っても落ちても）: `thth account leave <口座> --by masaru`。使われなかった招待は `thth admin invite revoke <id> --by masaru`。

### 2.3 決定: 承認 URL を審査員に届ける道（3.11.0 の承認待ちの一覧）

**決定（2026-09-25）**: 下の 1 を選び、3.11.0 で実装した（`docs/設計_3.11.0_承認待ちの一覧ページ_2026-09-25.md`）。審査員は `https://thth.me/pending` にユーザ名と承認 secret で入り、自分の口座の承認待ちから承認ページを開く。一覧に出した承認は最大 24 時間待ち、承認ページの 10 分は一覧から開いた時点から数える。以下は決める前に並べた案（記録として残す）。

承認ページは 10 分で切れる（`thth/approval_jobs.py` の `expires_at`・privacy の "at most 600 seconds"）。運営者から審査員へ連絡する道は、いまの THTH にも App Review の流れにも無い（承認 URL が出るのは `thth admin approval request` の出力だけ）。運営者が URL を出してから 10 分以内に審査員が開かないと、§2.1 の 4〜7 は通らない。`<HOW_URLS_REACH_YOU>` を埋める前に、次のどれかを決める。

1. **開発する**: 招待の口座の持ち主が secret で入る固定の「承認待ちの一覧」ページを作り、完了ページにその URL を出す。運営者が置いた承認要求を、審査員が好きなときに開ける。10 分を一覧から開いた時点から数えれば、privacy の 600 秒の文は保てる見込み（設計が要る）。
2. **開発しない・録画で示す**: 審査員の手は 1〜3（招待・認可・secret）まで。投稿・返信・削除の承認は録画 0 で示すと書く。試せない部分を審査が却下の理由にする恐れがある（見通し §0 の 4）。
3. **開発しない・連絡先を書く**: 運営者の待機の時間帯と連絡先を試験の手順に書き、審査員から連絡が来たら URL を出して返す。審査員が連絡してくる保証は無い。privacy と利用規約の連絡先は GitHub の issue だけなので、メールを書くなら先に privacy に足す。

## 3. 画面の流れ

2026-09-23 に masaru が見たもの（差し替えの文 §0-3）のほかは**未確認**。

### (A) 新しい申請に 10 権限を入れて出す

1. アプリレビュー →「アプリレビューの申請」の未申請の枠を開く。
2. 6 権限（§0 の上 6 行）を足す。承認済みの 4 権限も足す。4 権限が選べなければ (B) へ。
3. 各権限の用途説明の欄に、§1.1 の前置きと、その権限の本文（§1.2〜§1.11）を貼る。
4. 各権限に録画を添える（§5.1 の一覧）。
5. 試験の手順の欄に §2.1 を貼る（招待 URL を埋めたもの・Worker の 3.11.0 の deploy のあと）。
6. プライバシーポリシーの URL は `https://thth.me/privacy/` のまま。利用規約の URL の欄があれば `https://thth.me/terms/` にする（09-15 は MIT の LICENSE を入れた・`docs/手順_AppReview_2026-09-14.md` §2.3）。
7. §4 の点検表がすべて済んだのを確かめてから送信する。送信のあとはアプリの設定を変えない（見通し §4 の 5）。
8. 審査の間は §2.2 の手順で待つ。結果が出るまで世間の層は無効のまま。

### (B) 承認済みの 4 権限を入れられないとき

1. (A) の 1〜2 で 6 権限だけを足す。
2. 6 権限の各用途説明の欄に、§1.1 の前置き、§1.12 の段落、その権限の本文（§1.2〜§1.7）の順で貼る。
3. 録画は 6 権限の分（録画 0〜6）を添える。
4. 試験の手順の欄に §2.1 を貼る。
5. (A) の 6〜7 と同じにして送信する。
6. アプリレビュー → 過去の提出情報（2026-09-22 23:20 JST 送信・承認）→「リクエストの詳細を見る」→「質問する」に §1.13 の文を貼り、続けて §1.8〜§1.11 の英語の本文を 4 つ貼って送る。録画 7a〜10 を添える口があれば添える（有無は未確認）。
7. 返事が来るまで世間の層は無効のまま。返事で別の画面から出すよう言われたら、その画面で §1.1 と §1.8〜§1.11 を出す。

## 4. 出す前の点検表

| 点検 | 確かめ方 | 状態 |
|---|---|---|
| privacy と利用規約が施行済み | `https://thth.me/privacy/` と `https://thth.me/terms/` の頭に "Effective 2026-09-25"。privacy に "Observation map (daily keyword aggregates)" と "Plaza"、利用規約に "Contact" がある | 済（2026-09-25 施行）。提出の日に表示だけ確かめる |
| 招待リンクが deploy 済み | Worker が 3.10.0（`/invite/*`）。`thth admin invite create` が `invite_registration_rejected` で止まらない | 済。§2.2 の 1 で本物を作るときに確かめる |
| 承認 worker が常駐している | `systemctl status thth-approval-worker`。止まっていると、招待のページが 2 分で「運営者に連絡してください」に変わる | 未（審査の間ずっと） |
| Meta アプリに `https://thth.me/callback/` が登録されている | Dashboard（`docs/運用_招待する側.md` §10 の前提） | 未確認 |
| App Purpose が "Clients" | Dashboard（見通し §4 の 5・欄名は未確認） | 未 |
| データ削除のコールバック URL の登録 | Dashboard に `https://thth.me/data-deletion` がある。登録したら署名付きの要求を 1 回試す（`callback/src/deletion.js`） | 未確認 |
| 認可解除のコールバック URL | `https://thth.me/deauthorize`（欄の有無と名前は未確認） | 未確認 |
| Live のあとで tester が 10 権限を認可できる | 計画 #2（新しいメディアの口座で認可し、`thth doctor <口座> --json` の scope を見る）。できないと録画 0 が撮れない | 未 |
| 各権限を 30 日以内に 1 回以上成功させた | 下の表の命令（録画がそのまま呼び出しになる）。Dashboard の API の呼び出しの欄に印が付く（欄名は未確認・見通し §4 の 3） | 未 |
| 録画を撮った | §5 の録画 0〜10（7 は 7a と 7b） | 未 |
| 招待を 3 本作り、§2.1 に埋めた | §2.2 の 1。`thth admin invite list` に 3 本が `open` で並ぶ | 未（提出の直前） |
| 承認 URL の渡し方を決めた | §2.3（承認待ちの一覧・3.11.0） | 決定済み。Worker の 3.11.0 の deploy と `https://thth.me/pending` の表示を提出の前に確かめる（未） |
| 世間の層は既定で無効のまま | VM で `thth map collect <project>` を環境変数なしで打つと `world_layer_disabled`。VM の unit・シェルの設定に `THTH_MAP_WORLD` を置いていない | 未 |
| 録画の後に集計を消した | 録画 7b を撮り終えた後、`ls /srv/thth/state/_map/` に録画の project が無い | 未 |
| Data Use Checkup の時期 | Dashboard で次の期日を見る（期日は未確認）。審査中に重なるなら実態どおり申告する | 未 |
| ビジネス認証と技術提供者の認証 | Dashboard（見通し §4 の 11） | 未確認 |
| アプリの説明文・アイコン・Primary Contact のメール | Dashboard（見通し §4 の 5） | 未確認 |
| privacy の「運営者が認可を開始し」 | 3.11.0 で「招待リンクのボタン（本人）か、運営者の `thth auth`」の 2 通りを書く形に直した（`tools/build_site.py`）。承認ページの保持に一覧の 24 時間も足した | 直し済み。施行日（`PRIVACY_EFFECTIVE`）と deploy は主セッション（未） |

30 日以内の呼び出しの命令（どれも役割のある口座で打つ・録画の番号は §5）:

| 権限 | 命令 | 録画 |
|---|---|---|
| `threads_basic` | `thth doctor masaru-threads`・`thth posts masaru-threads --limit 3` | 1 |
| `threads_content_publish` | `thth send masaru-threads --text-file <本文> --production --confirm <digest>` | 2 |
| `threads_manage_replies` | `thth send masaru-threads --text-file <本文> --reply-to <返信の id> --production --confirm <digest>` | 3 |
| `threads_read_replies` | `thth replies masaru-threads --post <post_id> --refresh` | 4 |
| `threads_manage_insights` | `thth collect masaru-threads`（公開から 1 時間より後） | 5 |
| `threads_delete` | `thth retract masaru-threads <post_id> --reason … --by masaru --confirm <digest>` | 6 |
| `threads_keyword_search` | `thth topics masaru-threads --search <語>` | 7a |
| `threads_manage_mentions` | `thth mentions kopicha-threads` | 8 |
| `threads_profile_discovery` | `thth profile masaru-threads threads` | 9 |
| `threads_location_tagging` | `thth location search kopicha-threads 渋谷駅` | 10 |

## 5. 録画の台本

**実行はまだしない（台本だけ）。** 撮るのは masaru。VM に `ssh wt`（録画 0 は `ssh -t`）で入った端末、ブラウザ 1 枚、Threads のアプリ（スマホの画面を映す）。字幕は英語で 1〜2 行（話す要点も同じ）。

### 5.0 撮る前の約束

**秘密は画面に映さない。映ったら撮り直す。**

| 秘密 | どこに出るか | 映さない方法 | 映ったとき |
|---|---|---|---|
| 招待 URL（`https://thth.me/invite/…`） | `thth admin invite create` の tty・ブラウザのアドレス欄 | 招待は録画を始める前に作り、ブラウザへは画面の外で移す。アドレス欄は切り取るかぼかす | 撮り直す。使う前なら `thth admin invite revoke <id> --by masaru` で取り消して作り直す。使った後なら §5.3 の片付けで口座ごと外す |
| 承認 secret | 招待の完了ページの枠・`thth admin approver set` の tty | 完了ページの secret の枠はぼかす。承認ページの入力欄は伏せ字なので映してよい | 撮り直し、`thth admin approver set <口座> --by masaru` で出し直す（前の secret は無効になる） |
| 承認 URL（`https://thth.me/approve/…`） | `thth admin approval request` の出力の最後の行・アドレス欄 | その行とアドレス欄はぼかす（URL を持つ人は本文を読める。承認には secret が要る） | 撮り直す。一覧に出した承認は URL を使わずに一覧から開ける |
| 一覧から開いた承認ページのアドレス（`https://thth.me/pending/…`） | アドレス欄 | 映してよい（開くには一覧に入った cookie が要る）が、念のためぼかす | — |
| token | 出ない作り（ログと出力で伏せる） | — | token らしいものが見えたら録画を止め、使わずに相談する |

ほかの約束:

- **1 本 1 分以内。** 待ち時間は切る。解像度は 1080p 以上。音声なし。端末の文字とカーソルは大きく。
- **撮るのは役割のある口座だけ**（役割の無い口座は未承認の権限を認可できない）。投稿系は `masaru-threads`（@aoking・masaru の手動の口座）、読む系は `masaru-threads` か `kopicha-threads`（@kopi_chaba）。
- **`masaru-threads` の認可はやり直さない**（計画 #2。Live のあとの認可で権限が減ると、審査が通るまで投稿できなくなる）。撮る前に画面の外で `thth doctor masaru-threads` を打ち、scope が揃っているのを確かめる。
- **認可の場面**: 各権限の録画は、Threads の認可の流れを見せる必要がある（見通し §4 の 2）。録画 1〜10 の頭に、録画 0 の Threads の認可画面（権限の一覧が見える 5〜8 秒）を付ける。字幕 "Login: the same Threads authorization screen requests every permission in this submission (full flow in video 0)." 同じ場面の使い回しで足りるかは**未確認**。
- **Threads のアプリで確かめる場面**: 公開・削除・語の検索は、アプリでの確認も求められる（見通し §4 の 2）。録画 2・3・6・7a・0 はアプリの画面で終える。
- **本物の投稿が出る**（録画 0・2・3・8 の下ごしらえ）。撮り終えたら §5.3 で全部消す。
- 画面に他人の名前が出るのは、録画 7a の検索結果だけにする。プロフィールは Meta の公式の口座（@threads）を引く。

### 5.1 一覧

| 録画 | 権限 | 口座 | 打つもの（1 行） |
|---|---|---|---|
| 0 | 審査員の流れ（`threads_basic`・`threads_content_publish`・`threads_delete` にも添える） | 役割のあるテスト用の口座（招待で作る） | 招待リンク → 認可 → `thth admin approval request <口座> --draft <id> --send` → 承認ページ → `--retract <post_id>` → 承認ページ |
| 1 | `threads_basic` | masaru-threads | `thth doctor masaru-threads` と `thth posts masaru-threads --limit 3` |
| 2 | `threads_content_publish` | masaru-threads | `thth send masaru-threads --text-file ~/rec/post.txt [--production --confirm <digest>]` |
| 3 | `threads_manage_replies` | masaru-threads | `thth send masaru-threads --text-file ~/rec/reply.txt --reply-to <R> [--production --confirm <digest>]` |
| 4 | `threads_read_replies` | masaru-threads | `thth replies masaru-threads --post <P> --refresh` と `thth thread masaru-threads <P>` |
| 5 | `threads_manage_insights` | masaru-threads | `thth collect masaru-threads` と `thth measured masaru-threads --post <P>` |
| 6 | `threads_delete` | masaru-threads | `thth retract masaru-threads <P> --reason "App Review recording" --by masaru [--confirm <digest>]` |
| 7a | `threads_keyword_search`（求めの検索） | masaru-threads | `thth topics masaru-threads --search コーヒー` |
| 7b | `threads_keyword_search`（世間の層の集計の保存） | project `masaru` | `THTH_MAP_WORLD=1 thth map collect masaru` ほか（旧 §4 の台本） |
| 8 | `threads_manage_mentions` | kopicha-threads | `thth mentions kopicha-threads` |
| 9 | `threads_profile_discovery` | masaru-threads | `thth profile masaru-threads threads` |
| 10 | `threads_location_tagging` | kopicha-threads | `thth location search kopicha-threads 渋谷駅` と `thth approve <原稿>`（一段目で止める） |

`<P>` は録画 2 で出す投稿、`<R>` は kopicha が Threads のアプリから `<P>` に付ける返信、`<Q>` は録画 3 で出す返信の post_id。

`threads_keyword_search` の録画は 7a と 7b の 2 本。欄に 1 本しか添えられないとき（**未確認**）は、つないで 2 分以内の 1 本にする。

### 5.2 撮る順

前の録画が後の下ごしらえになる。

1. 録画 0（ほかと独立。テスト用の口座で）。
2. 録画 1。
3. 録画 2 で `<P>` を出す。
4. kopicha の Threads のアプリから `<P>` に返信 `<R>` を付ける（「録画のための返信です」程度）。
5. 録画 4（`<R>` を読む）。
6. 録画 3（`<R>` に答えて `<Q>` を出す）。
7. `<P>` を出してから 1 時間より後に、録画 5。
8. 録画 6（`<P>` を消す）。
9. 録画 7a・7b・8・9・10（順は問わない）。
10. §5.3 の片付け。

### 録画 0: 審査員の流れ（招待リンクから削除まで）

**撮る道具: `bash tools/appreview_rec0.sh jiangshi-lab`**（Mac の Terminal.app で・2026-09-25）。招待を作り、区間ごとに録り（secret を控える間と、投稿が出る・消えるのを待つ間は止める）、最後に `review0.mp4` につなぐ。承認 URL は端末の表示で伏せ、招待 URL は録画の前に Chrome のアドレス欄の無い窓で開く。人の手（ブラウザ・スマホ）は Return で進める。

示すもの: 役割の無い人が試験の手順（§2.1）どおりに入り、本人の承認でだけ投稿と削除が起きること（3.12.0 からは、承認を必須にした口座＝審査員のテスト用アカウントと同じ設定 `--approval all` で撮る）。

**3.12.0 で足す録画 0b（持ち主の AI アシスタントの流れ）**: 招待で作った `approval: none` の口座で、`/activity` から LLM の鍵を発行 → Claude Code に登録 → 「これを Threads に出して」と頼む → アプリに出る → 「消して」→ 消える → `/activity` に両方並ぶ → `/activity` で口座を止める → LLM の次の依頼が `account_stopped` で断られる。字幕 "Normally the owner asks their own AI assistant. Safety limits and the owner's stop button still apply." 台本と撮る道具は 3.12.0 の配布のあとで書く。撮る口座は**役割のあるテスト用の Threads 口座で、台帳にまだ無いもの**（台帳にある口座は「既に THTH にあります」で断られる）。

下ごしらえ（録画の外）:

```sh
thth admin invite create --media threads --project <録画用の project> --label rec-0 --expires 1d --production --by masaru
mkdir -p ~/rec
printf '%s\n' 'THTH test post: approved on a thth.me approval page before publishing. / 承認してから出す投稿の試しです。' > ~/rec/post0.txt
```

招待 URL は tty に 1 度だけ出る。ブラウザに移してから録画を始める。承認 worker が動いていることを `systemctl status thth-approval-worker` で確かめる。

場面:

1. ブラウザ: 招待のページ（"Invitation to THTH"・"What happens"・"Permissions requested" の 10 行・本当に投稿する旨の行）。アドレス欄は隠す。
   - 字幕: "A person opens the invitation link the operator created for them. The page lists every permission THTH will request."
2. "Authorize with Threads" を押す → "Preparing authorization" → "Open Threads authorization"。
   - 字幕: "The operator's server prepares a Threads authorization for this invitation."
3. 新しいタブ: Threads の認可画面。テスト用の口座でログインし、権限の一覧を映してから許可する。
   - 字幕: "The person logs in with their own Threads account and grants the permissions on Meta's screen."
4. 最初のタブ: "Your account is ready"（@ハンドルと口座名）→ "Show approval secret" → secret のページ（**枠をぼかす**）。
   - 字幕: "The account is ready. The approval secret is shown once to its owner (blurred here); THTH keeps only a salted verifier."
5. 端末:

   ```sh
   thth admin draft put <口座> --body-file ~/rec/post0.txt --by masaru
   thth admin approval request <口座> --draft <draft_id> --send --by masaru
   ```

   最後の行（承認 URL）は**ぼかす**。
   - 字幕: "The operator places a draft and requests approval. THTH cannot publish it until the owner approves."
6. ブラウザ: `https://thth.me/pending`（"Pending approvals"）にユーザ名と secret で入る（secret は伏せ字）→ 一覧の "開く / Open" → 承認ページ（"Publish this content"・本文・digest）→ secret を入れる（伏せ字）→ "Approve" → "Publication approval received"。
   - 字幕: "The owner signs in to their list of pending approvals with the same secret; no link from the operator is needed."
   - 字幕: "The owner reads the exact text and approves it with their secret."
7. Threads のアプリ: テスト用の口座にその投稿が出ている。
   - 字幕: "Only now is the post published on the owner's Threads account."
8. 端末:

   ```sh
   thth posts <口座> --limit 1
   thth admin approval request <口座> --retract <post_id> --reason recording --by masaru
   ```

   承認 URL の行は**ぼかす**。
   - 字幕: "Deleting a post also needs the owner's approval."
9. ブラウザ: 一覧を読み込み直す → "開く / Open" → 承認ページ（"Delete this post"・本文・削除する投稿・理由）→ "Approve" → "Deletion approval received"。Threads のアプリ: 投稿が消えている。
   - 字幕: "After approval, THTH deletes the post, and it is gone from Threads."

撮り直しの注意:

- 招待は 1 回だけ使える。撮り直すときは、できた口座を `thth account leave <口座> --by masaru` で外し、テスト用の口座の Threads の設定で接続を外してから、招待を作り直す。
- 手順 5 で一覧に出した承認は最大 24 時間待つ。一覧から開いた承認ページは 10 分で切れるので、開いてから承認までを続けて撮る。
- 公開の承認のあと投稿が出るまでは承認 worker の巡回しだい。待つ間は切る。

### 録画 1: `threads_basic`

示すもの: THTH がどの口座のために動くかを `/me` で確かめ、持ち主が自分の投稿を見られること。

1. （頭に認可の場面・§5.0）
2. 端末:

   ```sh
   thth doctor masaru-threads
   ```

   「本人の確認」の行（`threads_basic`・○）と、debug_token の scope の行を映す。
   - 字幕: "threads_basic: THTH confirms which Threads account it acts for (/me returns the user ID and username)."
3. 端末:

   ```sh
   thth posts masaru-threads --limit 3
   ```

   - 字幕: "The owner sees their own recent posts (ID, link, time and text) to choose one to reply to or delete."
4. Threads のアプリ: @aoking のプロフィールで同じ投稿が並ぶ。
   - 字幕: "The same posts in the Threads app."

撮り直しの注意: 書き込みは無い。何度でも撮れる。

### 録画 2: `threads_content_publish`

示すもの: 本文と digest を見せる一段目、digest が合うときだけ出す二段目、アプリに同じ本文で出ること。

下ごしらえ（録画の外）:

```sh
printf '%s\n' 'THTH test post: shown first, published only after the digest is confirmed. / 確かめてから出す投稿の試しです。' > ~/rec/post.txt
```

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth send masaru-threads --text-file ~/rec/post.txt
   ```

   「投げるはずの本文」と `digest:` の行。
   - 字幕: "Step 1 (dry run): THTH shows the exact text and a digest. Nothing is sent."
3. 端末:

   ```sh
   thth send masaru-threads --text-file ~/rec/post.txt --production --confirm <digest>
   ```

   「投稿しました: post_id=…」。この post_id が `<P>`。
   - 字幕: "Step 2: only with the matching digest does THTH publish the post (create a container, then publish)."
4. Threads のアプリ: @aoking にその投稿が出ている。
   - 字幕: "The approved post appears on Threads, unchanged."

撮り直しの注意: 撮り直すたびに本物の投稿が 1 本増える。失敗した回の投稿は §5.3 で消す。`<P>` は録画 3〜6 で使うので、ここでは消さない。

### 録画 3: `threads_manage_replies`

示すもの: 返信先を名指した返信も、同じ二段で承認してからでないと出ないこと。

下ごしらえ（録画の外）: §5.2 の 4 で `<R>` が付いていて、録画 4 でその id が分かっていること。

```sh
printf '%s\n' 'Thanks! This reply was approved before it was sent. / 承認してから返しています。' > ~/rec/reply.txt
```

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth send masaru-threads --text-file ~/rec/reply.txt --reply-to <R>
   ```

   - 字幕: "A reply is prepared with the post it answers; the reply target is part of the approved digest."
3. 端末:

   ```sh
   thth send masaru-threads --text-file ~/rec/reply.txt --reply-to <R> --production --confirm <digest>
   ```

   この post_id が `<Q>`。
   - 字幕: "Only the approved reply is sent. THTH never replies automatically, and it does not hide replies."
4. Threads のアプリ: `<P>` の下で `<R>` に `<Q>` が付いている。
   - 字幕: "The reply appears under the other person's reply."

撮り直しの注意: 本物の返信が出る。失敗した回の返信は §5.3 で消す。

### 録画 4: `threads_read_replies`

示すもの: 自分の投稿の下の会話を読むこと。枝の読みは保存しないこと。

1. （頭に認可の場面）
2. Threads のアプリ: `<P>` に @kopi_chaba の返信 `<R>` が付いている（§5.2 の 4）。
   - 字幕: "Someone replies to the owner's post."
3. 端末:

   ```sh
   thth replies masaru-threads --post <P> --refresh
   ```

   @kopi_chaba・時刻・本文の行。
   - 字幕: "threads_read_replies: THTH reads the conversation under the owner's own post."
4. 端末:

   ```sh
   thth thread masaru-threads <P>
   ```

   - 字幕: "thth thread shows the whole branch on request and saves nothing."

撮り直しの注意: 読むだけ。`--refresh` は刻みを進めない。

### 録画 5: `threads_manage_insights`

示すもの: 採集が投稿の数を読み、記録に残して持ち主に見せること。

下ごしらえ: `<P>` を出してから 1 時間より後に撮る（最初の刻みが 1 時間・`thth/collect.py` の `AGE_MARKS_HOURS`）。

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth collect masaru-threads
   ```

   - 字幕: "threads_manage_insights: the collection reads each post's views, likes, replies, reposts, quotes and shares at fixed ages after publishing."
3. 端末:

   ```sh
   thth measured masaru-threads --post <P>
   ```

   - 字幕: "The numbers are saved in the account's records and shown to the owner."
4. Threads のアプリ: `<P>` の数（表示・いいね）。
   - 字幕: "The same figures in the Threads app."

撮り直しの注意: 同じ刻みは 1 度しか採らない。撮り直すときは `thth measured` だけを撮り直し、手順 2 は最初の回を使う。

### 録画 6: `threads_delete`

示すもの: 一段目で投稿・理由・digest を見せ、二段目でだけ消すこと。記録は消さないこと。

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth retract masaru-threads <P> --reason "App Review recording" --by masaru
   ```

   「取り下げません（確認の一段目です）」・URL・理由・本文・`digest:`。
   - 字幕: "threads_delete: step 1 shows the post, the reason and a digest. Nothing is deleted yet."
3. 端末:

   ```sh
   thth retract masaru-threads <P> --reason "App Review recording" --by masaru --confirm <digest>
   ```

   「取り下げました」と、記録に `retracted_at` などを足した行。
   - 字幕: "With the matching digest, THTH sends one DELETE and records who deleted the post, when and why."
4. Threads のアプリ: @aoking のプロフィールを更新すると `<P>` が無い。
   - 字幕: "The post is gone from Threads."

撮り直しの注意: 1 度消した投稿は戻らない。撮り直すなら録画 2 から `<P>` を出し直す。

### 録画 7a: `threads_keyword_search`（求めの検索）

示すもの: 求めたときの検索で、集計と個々の投稿の投稿者・リンク・先頭 60 字を見せ、保存しないこと。

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth topics masaru-threads --search コーヒー
   ```

   集計（件数・異なり数・直近の時刻）と、個々の投稿の行。
   - 字幕 1: "threads_keyword_search: on request, THTH shows counts, distinct authors and the latest time, and for each post the author, a link and the first 60 characters."
   - 字幕 2: "The results are shown for this request only; their text and authors are not saved."
3. Threads のアプリ: 同じ語で検索した画面。
   - 字幕: "The same search in the Threads app."

撮り直しの注意: 読むだけ。他人のユーザー名と本文の頭が映るので、映す時間を短くする（ぼかすかは任意）。

### 録画 7b: `threads_keyword_search`（世間の層の集計の保存）

旧 §4 の台本（場面 0〜7）。1 分に収めるため、場面ごとに 5〜8 秒で切る。場面 5 は 3 行目の出力（項目の一覧）を主に映す。頭に認可の場面を付ける。


前提:

- project は `masaru`（account 名 `masaru-threads` を渡してもその project に解決される・`thth/map_store.py` の `project_accounts()`）。**masaru の台帳の実名で置き換える**（`masaru` という project 名は未確認）。
- `thth map collect` は project の中の Threads・Bluesky・Mastodon の account をそれぞれ 1 本使って検索する（`thth/map_world.py` の `WORLD_MEDIA`）。project に Bluesky・Mastodon の account があれば、その行も画面に出る。
- 語は話題の語で、検索に 5 件以上・5 人以上が出そうなもの（5 未満は欠測 `small_sample` になる。それも見せてよい）。例: コーヒー・ラーメン・読書。
- `THTH_MAP_WORLD=1` は**その 1 行の頭にだけ付ける**（`export` しない）。`thth map show` も同じ変数が無いと世間の層を見せない（`thth/map_view.py` の `_world_cell()`）。
- 同じ日に撮り直すと 2 回目の collect は `already_collected_today` になる。撮り直す前に `thth admin map purge masaru --world-only --by masaru` で行だけ消す。
- 置き場は `/srv/thth/state/_map/masaru/`（`$THTH_ROOT/state/_map/<project>/`・ファイルは 0600）。

#### 場面 0: privacy の該当の段落

- 画面: ブラウザで `https://thth.me/privacy/` の「Observation map (daily keyword aggregates)」と、保持の一覧の「Observation map daily figures」の行。
- 字幕: "Our privacy policy states what the daily keyword aggregates contain, what is never stored, and that they are kept for at most 180 days."

#### 場面 1: 既定では何も叩かない

```sh
thth map collect masaru
```

- 画面: 「世間の層は無効です（world_layer_disabled）。媒体は叩いていません」。
- 字幕: "The daily collection is off by default: without the operator's switch, THTH makes no search calls."

#### 場面 2: 人が話題の語を登録する

```sh
thth admin map node add masaru コーヒー --by masaru
thth admin map node add masaru ラーメン --by masaru
thth admin map node add masaru 読書 --by masaru
thth admin map node add masaru @someone --by masaru
```

- 画面: 3 語は「点「…」を足しました（n/20）」。4 行目は断り `node_handle`。
- 字幕 1: "A person registers topic keywords for the project, at most 20; THTH never picks keywords from other people's posts."
- 字幕 2: "Handles, URLs and personal names are refused, so the map cannot be used to watch a specific person."

#### 場面 3: 1 回だけ集める

```sh
THTH_MAP_WORLD=1 thth map collect masaru
```

- 画面: 「masaru <日付>: 検索 3 回・行 n 行」（Threads だけの project の場合。媒体が増えれば語の数×媒体の数）と、媒体ごとの `collected`。
- 字幕: "For this one run the switch is on: THTH calls threads_keyword_search once per keyword, counts the results in memory, and discards them."

#### 場面 4: 集計だけが見える

```sh
THTH_MAP_WORLD=1 thth map show masaru
```

- 画面: 語ごとの「世間 threads（…）: 件数 n/25  異なり …  上位 3 の占有率 …  直近 …」と共起の行。本文・投稿者名・リンクが無いこと。欠測の語は「理由 small_sample」。
- 字幕 1: "The map shows only counts, distinct authors, the top-3 share and a rounded age of the latest post — no post text, usernames or links."
- 字幕 2: "Any number below 5 is stored as missing, so no row points to a single post or person."

#### 場面 5: 保存ファイルの中身

```sh
ls -l /srv/thth/state/_map/masaru/
cat /srv/thth/state/_map/masaru/*.ndjson
python3 -c 'import json,sys; print(sorted({k for f in sys.argv[1:] for l in open(f) for k in json.loads(l)}))' /srv/thth/state/_map/masaru/*.ndjson
```

- 画面: `map.json` と `<YYYY-MM>.ndjson` が `-rw-------`。1 行ずつ `date`・`medium`・`node`・`n`・`requested`・`distinct`・`with_username`・`top3_share`・`latest_age_bucket`・`search_type`・`window_hours`・`reason`（共起の行は `edge`・`co`・`denominator`）。3 行目の出力が項目の一覧で、`text`・`post_id`・`username`・`author_key`・`permalink`・`timestamp` が無い（`thth/map_world.py` の `NODE_KEYS`・`EDGE_KEYS`）。
- 字幕 1: "The stored file is owner-only, outside Git, and holds one line per keyword, platform and day."
- 字幕 2: "These are all the fields it contains: aggregates and the fixed search settings, nothing from individual posts."

#### 場面 6: 消す

```sh
thth admin map purge masaru --by masaru
```

- 画面: 「masaru: 地図を丸ごと消しました（ファイル n）」。
- 字幕: "On a deletion request, or when the project leaves, the operator deletes the whole map with one command; the deletion is logged without the keywords."

#### 場面 7: 消えたことを見せる

```sh
ls /srv/thth/state/_map/
THTH_MAP_WORLD=1 thth map show masaru
thth map collect masaru
```

- 画面: 一覧に `masaru` が無い。`map show` は「点がありません」。最後の collect は `world_layer_disabled`。
- 字幕: "The map and its figures are gone, and the daily collection stays off until review is complete."

### 録画 8: `threads_manage_mentions`

示すもの: 自分の口座への公開の言及を取ること。答えるなら承認を通すこと。

下ごしらえ: masaru が @aoking の Threads のアプリから「@kopi_chaba 録画のための言及です / A mention for the App Review recording.」を 1 本出す（言及が 0 件だと権限が映らない）。これを `<M>` とする。

1. （頭に認可の場面）
2. Threads のアプリ: `<M>`。
   - 字幕: "Someone mentions the account on Threads."
3. 端末:

   ```sh
   thth mentions kopicha-threads
   ```

   @aoking・時刻・post_id・本文の頭・リンク。
   - 字幕 1: "threads_manage_mentions: THTH retrieves public mentions of the owner's own account."
   - 字幕 2: "Any answer is a reply that the owner approves first; THTH never replies automatically."
4. （あれば）kopicha の定期の採集が `<M>` を拾ったあとで、記録の 1 行を映す。置き場は kopicha の repo の `data/sns/inbox/<YYYY-MM>.ndjson`（`thth/collect.py` の `INBOX_DIR`）。`thth collect kopicha-threads` を手で打たない（kopicha の運用の timer が採る）。
   - 字幕: "The collection saves the mention (username, text, time, link) in the account's records until the account leaves."

撮り直しの注意: 読むだけ。`<M>` は §5.3 で消す。

### 録画 9: `threads_profile_discovery`

示すもの: 求めたときに公開プロフィールの公開の項目だけを見せ、保存しないこと。

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth profile masaru-threads threads
   ```

   名前・自己紹介・フォロワー・認証・累計の行。承認前に書いた標準アクセスの注は 3.11.0 で引けたときの出力から外した（API が断ったときの断りにだけ残る・`thth/threads_read_cli.py` の `STANDARD_ACCESS_NOTE`）。
   - 字幕: "threads_profile_discovery: on request, THTH shows only the public fields of a public profile, to understand an account in the owner's conversations."
3. Threads のアプリ: @threads のプロフィール。
   - 字幕: "The same public profile in the Threads app. The result is not saved."

撮り直しの注意: 読むだけ。引くのは Meta の公式の口座にする（個人を映さない）。

### 録画 10: `threads_location_tagging`

示すもの: 人が場所を検索して下書きに書き、承認の段に場所が出ること。承認の二段目は押さない（本物を出さない）。

下ごしらえ（録画の外）:

1. `thth queue kopicha-threads` で queue の置き場を確かめる。kopicha の repo は運用のレーンなので、置くのは録画の間だけ。commit しない。
2. `thth location search kopicha-threads 渋谷駅` で id を 1 つ控え、原稿 `zz-rec-location.md` をその queue に置く。

   ```
   ---
   thth: 1
   account: kopicha-threads
   status: draft
   publish_at: 2026-12-31T09:00:00+09:00
   location: 渋谷駅
   location_id: <控えた id>
   ---

   ## threads

   （録画用・出さない）場所を付けた下書きの見本です。
   ```

3. `thth lint <原稿のパス>` が通ることを確かめる。

場面:

1. （頭に認可の場面）
2. 端末:

   ```sh
   thth location search kopicha-threads 渋谷駅
   ```

   候補（最大 5 件・名前・住所・id）と「front-matter に `location:` と `location_id:` を書いてください」の行。承認前に書いた "Menlo Park" の注は 3.11.0 で外した（`thth/retract_cli.py`）。
   - 字幕: "threads_location_tagging: the owner searches for a place; THTH shows up to 5 candidates with their IDs."
3. 端末: `cat <原稿のパス>`（`location:` と `location_id:` の 2 行）。
   - 字幕: "The owner writes the chosen place into the draft. THTH never infers a location."
4. 端末:

   ```sh
   thth approve <原稿のパス> --wait 30
   ```

   一段目の「場所: 渋谷駅（id …）」と `digest:`。**`--confirm` は打たない。**
   - 字幕: "The approval step shows the place with the text. The place is part of the approved content, and only an approved post carries the tag."

撮り直しの注意: 二段目を押さない限り何も書き換わらない。撮り終えたら原稿を消す（§5.3）。アプリで場所の付いた投稿まで見せる必要があるかは**未確認**（見通し §4 の 2 が挙げるのは公開・削除・語の検索）。

### 5.3 撮ったあとの片付け

| 残るもの | 消し方 |
|---|---|
| 録画 0 の口座 | `thth account leave <口座> --by masaru`。テスト用の口座の Threads の設定で接続を外す |
| 録画 0 の使わなかった招待 | `thth admin invite revoke <id> --by masaru` |
| 録画 2 の撮り直しで増えた投稿 | `thth retract masaru-threads <post_id> --reason "App Review recording" --by masaru` の一段目と二段目 |
| `<Q>`（録画 3 の返信）と撮り直しの返信 | 同じく `thth retract masaru-threads <Q> …` |
| `<R>`（kopicha の返信） | kopicha の Threads のアプリで消す |
| `<M>`（@aoking の言及） | @aoking の Threads のアプリで消す |
| 録画 7b の集計 | 旧 §4 の場面 6（`thth admin map purge masaru --by masaru`）。§4 の点検表の「録画の後に集計を消した」 |
| 録画 10 の原稿 | `rm <原稿のパス>`。kopicha の repo に commit していないことを `git -C <kopicha の repo> status --short` で確かめる |
| `~/rec/` の本文 | `rm -r ~/rec` |
