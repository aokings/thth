# 申し送り: THTH 開発（2026-09-26 朝）

前のセッション（Opus 5.5・Agent Mail 名 DustyCave）から、次のセッションへ。

**masaru の言葉（09-26 朝）: 「ちょっと本気で信用なりません」。** 前のセッションの説明がちぐはぐだった（下の「前のセッションのぶれ」）。新しいセッションは、**前のセッションの推薦をそのまま信じず、一次資料と repo の事実から組み直す**こと。

## 最初に読む順

1. この書
2. `docs/設計_3.12.0_承認を関所から選ぶものへ_2026-09-26.md`（§0 masaru の決定・§9 夜間に前のセッションが決めたこと＝**masaru の確認が済んでいない**）
3. `docs/朝の報告_THTH_ナイトシフト_2026-09-26.md`（ただし「録画 0 は今の撮り方で筋が通る」の段は撤回済み。下を見よ）
4. `docs/見通し_Meta審査_2026-09-24.md`（Meta の一次資料の引用がある。§0・§3・§4）
5. `docs/手順_AppReview_2026-09-14.md` §2.3（9/15 に 4 権限に絞った経緯）
6. Claude の記憶 `MEMORY.md` の索引と、`thth-tool-provider-stance`・`state-premise-dependence`・`paste-text-in-full`・`dont-repeat-operator-error-hints`

## 現在地（事実）

- **3.12.0 を配布済み**（09-26 06:30〜06:45）。main `2e04fca`・release `2e04fca`・tag `v3.12.0`・PyPI と MCP registry に 3.12.0・Worker を deploy 済み（`thth.me/activity` が 200、`/pending/`・`/activity/` は 308）。VM の承認の常駐も 3.12.0（`thth board` に「承認の常駐: 3.12.0（2e04fca）」）。
- 3.12.0 の中身: 承認を口座ごとに選ぶ `approval`（none/publish/all）・安全装置（`thth/guard.py`）・持ち主の画面 `/activity`（見る・止める・戻す・予約取り消し・設定・LLM の鍵の発行）・CLI `thth account set|resume|status`・MCP `thth_schedule_request`/`thth_settings`/`thth_account_status`・09-25 夜の穴 6 つ。詳細は `docs/リリースノート_3.12.0_2026-09-26.md`。
- **Meta**: 承認済みは 9/22 送信の 5 権限（`threads_basic`・`keyword_search`・`manage_mentions`・`profile_discovery`・`location_tagging`）。**投稿・返信の管理・返信の読み取り・インサイト・削除は一度も申請していない**（masaru が 09-26 朝に Dashboard の「過去の提出情報」で確認）。masaru の口座と `sometokagaku`・`jiangshi_lab` は Threads テスター（役割あり）なので全権限が使える（09-25 23:04 に `sometokagaku` で実測）。**テスターを外さない**（外すと投稿が止まる恐れ）。
- 承認済みの 5 権限は Dashboard で選び直せない（アクションは「アクセスレベルを下げる」「削除」だけ）。「質問する」は答えを探すだけで、担当者への問い合わせは却下のあとにしか開かない。
- `threads_delete` は Dashboard の呼び出し数が 0 だった（09-26 00:2x に録画用の口座で 1 回成功・反映待ち）。

## 決めたこと（masaru・日付つき）

- 09-14: 「取れるものは全部・App Review は 1 回で」。→ 09-15 に前のセッションの分析（当時の前提: 他の利用者は自分の Meta アプリを持ち込む）を受けて 4 権限に絞った。前提が 09-20・09-24 に変わり、6 権限の 2 回目の審査が要る。**masaru は「最初から全部入れようといった私を否定した判断はなんだったの」と言っている**（前のセッションは経緯と落ち度を説明済み）。
- 09-24 夜: 外の人も masaru と同じように使えないと意味が無い。
- 09-26 00:40: 「LLM とユーザの信頼性をこちらが疑うのは越権。私たちは道具の提供者に過ぎない」。masaru は「ハナからそのつもり」だった。
- 09-26 00:50: 「LLM が暴走して使い方を誤った場合はセーフティが効くようにする」。
- 09-26 朝: 「承認の流れはもうやらないのですよね？」（承認ページを主役にした流れは使わない、が masaru の理解）。
- 常設: 選択肢カード UI（AskUserQuestion）は使わない。貼る文は差分でなく全文。release/tag は CI 緑のあと Claude が押してよい。

## 前のセッションのぶれ（新しいセッションが同じ轍を踏まないように）

1. 「疑わない」と決めた直後に、審査員向けだけ `--approval all`（承認ページ）で試させる形を申請の文（`docs/手順_Meta申請_世間の層_2026-09-24.md` §2.1・§2.2）と録画の道具（`tools/appreview_rec0.sh`）に入れた。**masaru の方針と矛盾している。見直すこと。**
2. 朝の報告で「録画 0 は今の撮り方で筋が通る」と書き、そのあと「使わないほうがよい」と言った。**昨夜撮った録画 0（`~/Movies/thth-appreview-rec0-20260925-235324/`・承認の流れ）は使わない方向**。
3. 「6 権限の審査」を問われて、最初に経緯を確かめずに答えた。
5. **配布の手順を昔のやり方に戻した。** 今の決まりは「release と tag は CI 緑のあと Claude が押してよい」（09-25 の申し送り・記憶の索引）。前のセッションは Worker の `wrangler deploy` が auto mode（Production Deploy）で止められたのを見て、release と tag も止まると**試さずに決めつけ**、9/23 の古いやり方（masaru に命令を渡して押してもらう）に戻した。masaru は「deploy の手順もいきなり昔のやり方になっていたのも気になっています」と言っている。**事実として止められたのは `wrangler deploy` だけ。** 次の配布では release・tag を Claude が押してみて、止められたらそのときに masaru に渡す。
4. 最後に「`/activity` に持ち主が自分で投稿・返信・削除する欄を足し、審査員はそこで試す」を提案したが、masaru は同意していない（その直後に「ちぐはぐで信用できない」）。**これも一案にすぎない。**

## 残り（優先順・方針はまだ決まっていない）

1. **事実を 1 枚にまとめ直してから、方針を 1 本にして masaru に出す**（前のセッションが最後に約束したこと）。中身: (a) Meta が 6 権限の審査で求めること（一次資料の引用つき。`docs/資料_Meta公式_2026-09-11.md`・見通し §4）、(b) 審査員が THTH を試すのに何があれば足りるか（LLM を持たない審査員・承認の流れを主役にしない、の両立）、(c) 3.12.0 で今あるもの・無いもの、(d) 進め方 1 本（前提が変わったら結論がどう変わるかを添える）。
2. 外の人が自分の LLM から THTH につなぐ道が無い（MCP は stdio で起動口は VM の上＝`ssh <server> thth-mcp-invitee`）。リモートの MCP（`https://thth.me/mcp`）を置くかは masaru の判断待ち。`/activity` の「LLM の鍵を発行する」は、今は `THTH_REPORT_TOKEN=<鍵>` を出すだけ（Worker の変数 `THTH_MCP_COMMAND` は未設定）。
3. 利用規約と privacy の改訂（下書き `docs/下書き_利用規約とprivacyの3.12改訂_2026-09-26.md`・本体は未変更）。**改訂まで `approval: none` の新しい招待は作らない。** `callback/public/llms.txt` の冒頭 "THTH is a human-approval gate…" も要検討。
4. 設計 §9 の夜間の裁定 7 つを masaru に確かめる。

## 待っているもの

- masaru の判断（上の 1〜4）。hub には `waiting_on_masaru` で出してある。
- Agent Mail: 未読なし（09-26 朝の時点）。

## 触ってはいけないもの・決まり

- 秘密は会話にも repo にも出さない。承認 secret・招待 URL・bearer は tty だけ。Cloudflare は `~/.local/bin/cf-api`。
- `git add` はパス明示（`-A` を使わない）。`pytest | tail && commit` の形にしない（rc を取ってから）。全件の Python 試験は約 38 分。
- main への push は本番を変えない。release を進めると VM が拾い、tag で PyPI と registry に出る。**release と tag は CI 緑のあと Claude が押してよい**（常設）。`callback/` を変えたら Worker の deploy（`cd callback && npx --no-install wrangler deploy`）。09-26 に Worker の deploy だけは auto mode に止められたので、止められたら masaru に命令を全文で渡す。Worker が先・VM があと。3.12.0 からは常駐が版の変化で自分で入れ替わるので、手の restart は要らない（3.12.0 の初回だけ要った）。
- VM の管理の書き込み命令は `THTH_ROOT=/srv/thth` を付ける。手で `approval-worker --once` を打たない（3.12.0 で umask は直したが、常駐に任せる）。
- 運用セッション（kopicha・asmon・nigamilab）のレーンは直さない。kopicha・asmon-kanto・nigamilab・masaru はすべて masaru の持ち物。
- 試験に「— 下書き」の題・「deploy が要る」の文言・施行日を固定しない。

## 残してあるもの

- 録画用の口座 `inv-jiangshi-lab-fce905`（`approval: all` 扱い・台帳に残っている）。
- 録画の道具 `tools/appreview_rec0.sh`（招待を `--approval all` で作る版。上のぶれ 1 のとおり見直し対象）。
- writer の作業場所 `.claude/worktrees/agent-*`（片付けていない）。

## Agent Mail

- 名前 **DustyCave**・合言葉のファイル `~/.config/agent-mail/thth.json`（600）。
- 送る・返す・ack: `~/tools/mcp_agent_mail/.venv/bin/python /Volumes/NexDev/Developer/media-hub/hub/agentmail/am.py --as thth reply|ack|send …`。
- 見張り（必須）: Monitor に `/Volumes/NexDev/Developer/media-hub/hub/agentmail/watch_inbox.sh --take DustyCave`（引き継ぐので `--take`。30 分で切れたら `--take` 無しで張り直す）。前のセッションの見張りは止める。
- hub status post には `--title` を毎回・`--am DustyCave`。`waiting_on_masaru` には `--question` が要る。
