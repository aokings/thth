# v3.1 C 実装検証

基点2dd68c7。対象C1/C2/C5のみ。C3/C4・実API・VM操作・配布は含まない。

## C1

insightsの刻み720hと刻み由来38日窓を追加。候補窓はmax(指標窓,collect_days)、各呼出は独立gate。返信刻みは旧5点固定。refresh_targetsの返信窓は不変。
queue/sent/bundle × collect_days 14/40/60 のfake時計で40日間、正常insights6回・conversation5回。900〜912hの遅れたretryは書けるがstrict report帯では採用しない。collect_days60でも38日超insightsを呼ばない。独立失敗retry・refresh対象外を確認。
対象 collect/v31/sent:42 passed。返信刻みをinsightsと同じ表に戻す単一実変異がfail、逆置換・pyc除去後v31 11 passed。証拠 exchange/out/2026-09-18_v31_検証/collection-writer/C1-mutation.txt。

## C2

新観測のcontextへ既存同JST日の非未来日次だけを写す。最新同時刻の値が競合/不正ならnull。フォロワー数は非負の有限整数値、boolは拒否。日次不在/壊れ/他account/別日/未来・不正時刻を否定テスト。現在の日次は前日値なので通常nullと利用文書に明記した。
queue/sent/bundleで当初context null、その後日次を追加しても既存bytes不変、次刻みの新行だけ0を含むcontextを持つ。旧行に後補完する実変異でfail、逆置換・pyc除去後v31 26 passed。既存sentの新行キー集合テストは承認済み追加contextを許容しnullも確認、単体1 passed。変更前キー/過去行の意味は維持。ログ collection-writer/C2-backfill-mutation.txt。

## C5

既存Bluesky/Mastodon adapterの指標抽出は変更不要。collectorは全媒体同じ刻み表を使う。Threads/Bluesky/Mastodon × queue/sent/bundleの9経路でinsights6・reply5、1/6/24/72/168/720h、account/mediumの保持を検査。Bluesky/Mastodonは実adapter.insightsをfake取得応答へ接続し、socketを禁止。likes/replies/reposts、Bluesky quotesを保持、viewsはnull、Mastodon quotesはnull。720h reportでもviews欠測をnullのまま保持。
対象v31/Bluesky/Mastodon/boundary:206 passed。欠測補完Noneを0へ変える実変異でfail、逆置換pyc除去後v31 35 passed。ログ C5-missing-views-mutation.txt。実API・VMでの新採集は行わず合成応答による媒体契約検証。媒体間の数値を合算しない。

## C2 独立監査修正

深いJSONのRecursionErrorを欠測として閉じ、当日dateに対し前日collected_atの日時矛盾を拒否。同じJSON object内の重複鍵も拒否し、競合followers_countが後勝ちで採用されることを防ぐ。これら任意contextの不備でcollect全体/insights新行保存を止めない。対象v31 36 passed、補強後の3否定入力ケース単体1 passed。
