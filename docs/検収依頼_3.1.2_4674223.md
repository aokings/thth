# 検収依頼 3.1.2「報告の口」＋件 6・件 7（`4674223`）

2026-09-23。設計 [設計 3.1.2](設計_3.1.2_報告の口_2026-09-23.md)（裁定 5 件はい）、下書きの
[リリースノート](リリースノート_3.1.2_2026-09-23.md)。枝 `worktree-agent-a67fcf319c3bab4a9`（起点は `dcb633a`・`origin/main` = `079fbed`（3.1.1）を `5b3e663` で合流済み）。この文書の名前の sha は最後の製品 commit で、この文書の commit はその上に 1 本載る。

**版は上げていません**（`thth/VERSION`・`server.json` は合流した 3.1.1 のまま・tag も未作成）。release も deploy もしていません。Worker（`callback/`）は触っていません。

## 段ごと（commit 1 本 = 段 1 つ）

### 第 1 段 `88c12b2` 記録と利用者の CLI

- 変えたファイル: `thth/report_inbox.py`（新規）・`thth/redact.py`（`looks_like_secret()` を足す）・`thth/admin_log.py`（事象 3 つ・通知しない）・`thth/cli.py`（登録 2 行）・`README.md`／`README.en.md`（`report` を列挙に）・`tests/test_v312_report_inbox.py`（新規）
- 意味試験: `test_file_list_show_の往復`・`test_置き場は私有でSNSの台帳には書かない`・`test_byが無ければ断って置かない`・`test_秘密らしき値は置かずに断る`（形 11 通り）・`test_題と再現手順も秘密を検査する`・`test_登録された秘密の値そのものも当てる`・`test_不具合の報告に普通に出る綴りでは断らない`（8 通り）・`test_長さの上限ちょうどは置け_1字超えは断る`（3 欄）・`test_同じ題と本文は24時間以内なら既存のidを返して断る`・`test_1accountにつき1日20件まで`・`test_変更ログはpresence_onlyで本文を入れない`・`test_変更ログに書けなければ置いた1件を戻す`・`test_listは自分のprojectだけ`・`test_Scopeは他のprojectの報告を読ませない`・`test_壊れた1件で一覧を止めない`・`test_本文のTHTH_ROOTとホームの絶対パスは畳んで置く`・`test_置き場がまだ無ければ0件_symlinkなら読まずに断る`（第 4 段で追加）
- 秘密の型: `redact()` の綴りの型（`access_token=`・`client_secret=`・`code=`・`app_password`・`accessJwt`・`refreshJwt`・`Authorization:`・`hc-ping.com/`・登録された値）を再利用し、足したのは Bearer・`sk-`・Meta の `THAA`／`EAA`・JWT・秘密鍵の見出し・`token|secret|password|api_key` の綴り・32 桁以上の 16 進。綴りの型は「値が 16 字以上で英字と数字が混ざる」ときだけ当てる（`exit code: 2`・`error code: invalid_scope` で断らないため）。足した形は上の 2 本の parametrize で 1 つずつ固定。

### 第 2 段 `0bc5ae0` MCP の利用者側

- 変えたファイル: `thth/report_service.py`（`execute_user_reports`）・`mcp/server.py`（`REPORT_TOOLS`・振り分け・断りの符丁）・`thth/report_isolation.py`（`state/_reports` を共有の読み場として検査）・`tests/test_v312_report_mcp_user.py`（新規）
- 意味試験: `test_MCPでfile_list_showの往復`・`test_誰が置いたかは要求の欄から作れない`・`test_actorの無いcredentialはby_required`・`test_書く口の無いcredentialでも置ける_書く口があっても同じ`・`test_credentialの外のaccountには置けない`・`test_他のprojectの報告は読めない`・`test_断りは静的な符丁だけ`（4 通り）・`test_重複は既存のidを返す`・`test_admin_scopeと手元のstdioには利用者の口を出さない`

### 第 3 段 `bb6ef5f` 実装側（admin）と書き出し

- 変えたファイル: `thth/report_inbox.py`（`reply`・`close`・`export`・`render_markdown`・`register_admin`）・`thth/admin_report.py`（登録 3 行）・`thth/report_service.py`（`execute_admin_reports`）・`mcp/server.py`（`thth_admin_reports_*` 4 本）・`tests/test_v312_report_admin.py`（新規）・`tests/test_admin_http_mcp.py`（ADMIN_TOOLS の名前の集合に 4 本を足す）
- 意味試験: `test_管理者は全projectを読む`・`test_返事を足すと報告した側のshowに出る`・`test_返事にも秘密は置かない`・`test_返事と閉じるはbyが要る`・`test_閉じると版と理由が残り閉じ直せない`・`test_版の形が違えば閉じない`（4 通り）・`test_無いidは断る`・`test_ログに書けなければ返事を戻す`・`test_書き出しは1件1ファイルで絶対パスも秘密も無い`（leak probe）・`test_閉じた報告は前に書き出していれば閉じた形に書き直す`・`test_書き出し先が使えなければ断る`・`test_MCPの管理口は全projectを読み返事と閉じるを書く`（leak probe 込み）・`test_MCPの返事はbyが要り_利用者のcredentialには出ない`

### 第 4 段 `6074591` 見落とさない口と受け口の案内

- 変えたファイル: `thth/report_inbox.py`（`handoff_summary`・`morning_summary`・置き場が無いときの 0 件）・`thth/operations_handoff.py`（`--since-last-read` のときだけ `tool` に `report_channel`・`reports`・人向けに 2 行）・`thth/morning.py`（0 段「道具」の `reports` と描画 1 行・`_tool_section` の呼び出し 1 行・limitations 1 行）・`thth/cli.py`（`main` を包んで rc≠0 のとき案内 1 行・`--help` の epilog）・`mcp/server.py`（isError の 2 つ目の text・利用者の tools/list の 1 句）・`tests/test_v312_report_channel.py`（新規）
- 既存の試験の直し（意味は変えていない）: `tests/test_v212_read_coordination.py`（3 か所）・`tests/test_v213_carryover_timeout.py`・`tests/test_v213_review_followups.py`・`tests/test_v213_stop_observation.py`（各 1 か所）の stderr の完全一致を「理由行＋案内の 1 行」に。`tests/test_v210_json_contract.py` の `--json` の入口の表に `report`／`admin reports` の 8 本を足す。
- 意味試験: `test_受け口の文面は1種類で設計のまま`・`test_返事はhandoff_reportのtoolに出て_栞より後の分だけが新しい`・`test_projectでまとめても他projectの返事は出ない`・`test_ふだんのhandoff_reportには受け口を載せない`・`test_人向けのhandoff_reportにも返事と受け口の1行`・`test_置き場が読めなくてもhandoff_reportは止まらない`・`test_管理者の毎朝の一枚に開いている件数と新規`・`test_サーバ型の利用者の一枚には件数を出さない`・`test_CLIの断りの後ろに受け口の1行_成功には載せない`・`test_lintの検査結果の1は断りではない`・`test_helpの末尾に1行`・`test_MCPの断りは2つ目のtextに受け口_成功には載せない`・`test_MCPのoperations_handoffにも返事と受け口`・`test_利用者のtools_listの説明文の末尾に1句`・`test_手元のstdioの断りにも受け口`

### 第 5 段 `5960aa5` 文書と skill

- 変えたファイル: `docs/使い方_プロジェクトのセッション向け_2026-09-09.md`（開始手順に 1 行）・`skills/thth/SKILL.md`（開始手順に 1 行・「詰まったら」に 1 行）・`skills/thth-admin/SKILL.md`（開始時に `admin reports list`）・`docs/リリースノート_3.1.2_2026-09-23.md`（下書き）
- 試験: `tests/test_docs_links.py`・`tests/test_skill_doc.py`・`tests/test_packaging.py` が緑。

### 合流 `5b3e663`（`origin/main` = `079fbed`・3.1.1 の 7 件と版 3.1.1）

- `git merge origin/main` は**文字の衝突なしで自動合流**した。重なったのは `thth/morning.py`（3.1.1 の world の `own_excluded`・today の `overdue_items`・unanswered の窓・next_steps の「超過」と、3.1.2 の 0 段の `reports`・`_tool_section` の呼び出し・limitations の 1 行）と `thth/operations_handoff.py`（3.1.2 だけが触った）。手で直した箇所は無い。
- 合流後に 3.1.1 の試験（`tests/test_v311_*.py` 7 本）と 3.1.2 の試験、stderr を完全一致で見る試験（`test_v212_read_coordination.py` ほか）を回して全部緑（387 本）。3.1.1 側の変更（own_excluded・overdue_items・unanswered の窓・replies の owned_only・leave・mastodon の delete・incident の account_required）はそのまま残っている。

### 合流後の追加 1 `df442d6` 次の一手の報告・export は私有の置き場へ

- 変えたファイル: `thth/morning.py`（`next_steps(…, reports=None)` に「報告」の行・`_steps` で管理者の 1 枚だけ `report_inbox.list_reports(status="open")` を渡す・描画 1 行）・`thth/report_inbox.py`（export の help・docstring）・`docs/設計_3.1.2_報告の口_2026-09-23.md`（§1・§3 の export の行を「`--to <私有の dir>`・公開 repo には置かない」に、§3 の next_steps に形を足す、§7-2 に追記）・`skills/thth-admin/SKILL.md`・`docs/リリースノート_3.1.2_2026-09-23.md`・`tests/test_v312_report_channel.py`・`tests/test_v312_report_admin.py`
- 「報告」の行の形: `{"kind": "report", "report_id", "report_kind", "title"}`（題は先頭 60 字・本文は載せない）。報告の種類は `kind` と名前がぶつかるので `report_kind` にした。3.1.1 の「超過」の行のあとに並ぶ。
- 意味試験: `test_管理者の次の一手に開いている報告が1件1行で本文は載らない`（開いている 2 件だけ・閉じた報告は出ない・題 70 字が 60 字に・本文が payload のどこにも無い・描画・サーバ型の利用者の 1 枚には並ばない）・`test_書き出しは既定の行き先を持たず_toが要る`
- 既存の `test_管理者の毎朝の一枚に開いている件数と新規` の「id が payload に無い」の主張は「0 段に id が無い」に直した（id は第 5 段に並ぶようになったため）。

### 合流後の追加 2 `359f739` 件 6: 退出で token が共有なら遠隔で失効させずに完了する

- 変えたファイル: `thth/leave.py`（`_run_locked` の遠隔の段）・`tests/test_v312_leave_shared_token.py`（新規）・`tests/test_v212_leave_providers.py`（3 本）・`tests/test_v311_leave_same_user.py`（1 本）
- 直し: `token_shared`（値・ファイルが他の生きている account と同じ）なら `/oauth/revoke` 等を投げず、`remote='unconfirmed_shared'`・phase `manual_unconfirmed` に進み、台帳・state・env・managed repo の削除まで完了する。自分の token file は `targets` から外れたまま（`preserved: ['token_shared']`）、他 account の token file は触らない。`shared_credential_revoke_refused` は「共有なのに `targets` に token が残る」矛盾のときだけ。停止を戻さない・再試行資格を消さない規律はそのまま。Threads・Bluesky は元から遠隔の解除が本人の手なので、結果は従前と同じ（`unconfirmed_shared`）。
- 意味試験: `test_a_同じ値の2台帳は失効を投げずに完了する`（`leave.command` の案内の 1 行・`/oauth/revoke` 0 回・他方の token file と台帳が byte 一致・他方は停止しない）・`test_b_値が違う2台帳は自分のtokenだけ失効して完了する`・`test_c_worker_revokedで止まったrowから再実行して完了する`（3.1.1 の形で止まった row＝`token_shared: True`・`preserved: ['token_shared']` から）・`test_共有なのに消す対象にtokenが残るrowは矛盾として止める`
- **既存の試験 4 本を新しい形に直した**（旧い止まり方そのものを固定していたため）: `test_v212_leave_providers.py` の `test_shared_mastodon_grant_refuses_remote_revoke_but_shared_client_alone_does_not`・`test_b_credential_commit_before_revoke_is_rechecked_after_inventory`・`test_b_exchange_to_commit_holds_registry_lease_and_late_shared_grant_vetoes_a`、`test_v311_leave_same_user.py` の `test_same_token_bytes_still_refuse_remote_revoke`（→ `test_same_token_bytes_do_not_revoke_remotely`）。どれも「遠隔を呼ばない（calls が空）・相手の token が残る」という守りの主張は残し、`raises(shared_credential…)` を「completed・`unconfirmed_shared`」に替えた。

### 合流後の追加 3 `4674223` 件 7: 停止中の台帳が未回答を潰さない

- 変えたファイル: `thth/replies.py`（`load_for_ownership()` を足し、`_own_handles()` がそれを通る）・`thth/unanswered.py`（他 account の持ち主の見分けも同じ関数を通す）・`tests/test_v312_own_handles_stopped.py`（新規）
- 直し: 停止中（`account_stopped`）の台帳は `leave_gate.recovery(name)` の下で読んで handle を集める。停止の状態そのものが読めない（`account_stop_state_unreadable`）・台帳が壊れている（JSON でない等）ときは従前どおり unreadable。`unanswered` 側にも、他 account の台帳を `load_account` で読んで停止中なら `reply_ownership_unknown` を足す箇所があったので、同じ関数に揃えた（直さないと件数は戻っても `cannot_say` に `reply_ownership_unknown` が残る）。
- 意味試験: `test_停止中の台帳があっても未回答が減らない`（n=3 のまま・`reply_ownership_unknown`・`unproven_roots_excluded` が出ない）・`test_停止中の台帳のhandleも自分として集める`（停止中の handle からの返信は未回答に数えない・停止は戻らない）・`test_壊れた台帳は従前どおりunreadable`・`test_morningの1段も停止中の台帳で潰れない`・`test_whoも停止中の台帳で相手の返信を見失わない`

## 単変異（製品コード 1 か所を置き換え → 試験が落ちる → 戻して sha256 が一致）

設計 §5 の 6 本（M1〜M6）に、段ごとに 1 本以上になるよう補助を足した。すべて killed、すべて戻したあと byte 一致。

| # | 段 | 変異 | 結果 |
|---|---|---|---|
| M1a | 2 | project の絞り: `Scope.allows` が常に真 | 1 failed（`test_他のprojectの報告は読めない`） |
| M1b | 2 | project の絞り: MCP の利用者の scope を `None`（全部） | 1 failed（同上） |
| M2 | 1 | 秘密検査: `looks_like_secret` の判定を `if False` | 14 failed |
| M3 | 1 | 重複検査: sha の一致を `if False` | 1 failed（`test_同じ題と本文は…既存のidを返して断る`） |
| M4 | 1 | 長さ: `len(value) > limit` → `> limit + 1` | 3 failed（3 欄の上限） |
| M5 | 4 | 返事の表示: 栞より後の返事だけ → 栞があれば 0 件 | 1 failed（`test_返事はhandoff_reportのtoolに出て_栞より後の分だけが新しい`） |
| M6 | 4 | 断りの案内文: `CHANNEL_LINE` の文面を縮める | 5 failed |
| M6b | 4 | 断りの案内: CLI の rc≠0 で載せる条件を `if False` | 1 failed |
| M6c | 4 | 断りの案内: MCP の isError の 2 つ目の text を外す | 1 failed |
| M7 | 3 | 書き出しの絶対パス: `$THTH_ROOT`・ホームを畳まない | 1 failed（`test_書き出しは…絶対パスも秘密も無い`） |
| M8 | 3 | 版の検査: `tool_version.numbers(version) is None` → `not version` | 4 failed |
| M9 | 追加 1 | 次の一手の報告: 題を 60 字で切らない | 1 failed（`test_管理者の次の一手に…本文は載らない`） |
| M9b | 追加 1 | 次の一手の報告: サーバ型の利用者の 1 枚にも並べる（`allowed_names is None` → `True`） | 1 failed（同上） |
| M10 | 件 6 | 共有時に遠隔の失効を投げる形に戻す（`if row['token_shared']:` → `if False:`） | 3 failed（a・c・矛盾の試験） |
| M11 | 件 7 | recovery gate を外す（`with leave_gate.recovery(name):` → `if True:`） | 4 failed（停止中で 0 件・`who` の met 0 に戻る） |

## 全件

- 合流と件 6・件 7 のあと（`4674223`）: `python -m pytest -q -p no:cacheprovider -n auto` … **7143 passed, 1 skipped**（3 分 43 秒・0 failed）。3.1.2 の新規は 97 本（報告の口 88・件 6 が 4・件 7 が 5）。
- 合流前に落ちていた `tests/test_ask.py::test_本物の台帳でrc0でcannot_sayが出る` は、3.1.1 の合流で緑になった。

## 設計から外れた点・判断が要る点

1. ~~`next_steps` の `kind: "report"` は入れていない~~ → **合流後に入れた**（`df442d6`・裁定 (b)）。0 段の `reports.next` も残してある。
2. **stdio（手元の pip 版）の MCP には利用者の報告の口を出さない**。手元で置いても VM の実装側には届かないため。サーバ型の user credential と VM の CLI（`thth` のラッパ）だけに出す。stdio の isError には案内の 1 行を添える（CLI の `thth report file` は手元にもある）。
3. **CLI の `thth morning` は管理者とみなす**（資格情報の概念が無いため）。kopicha のセッションが VM の CLI で `morning` を打つと、他 project を含む件数（件数だけ・本文は出ない）が見える。CLI では `thth admin reports list` も打てるので、新しい漏れではない。
4. **重複の検査は同じ account の中だけ**。全体で見ると、他 project の報告の id を返してしまう（利用者 scope の漏れ）。
5. **件数の上限は「直近 24 時間」**で数える（暦日ではない）。
6. **32 桁以上の 16 進は設計どおり秘密として断る**。`draft_id`（64 桁）や git の完全な sha（40 桁）も断られる。断りの次の一手に「sha は先頭 12 桁までで足ります」と添えた。
7. **本文の `$THTH_ROOT` とホームの絶対パスは置く時点で `$THTH_ROOT`・`~` に畳む**（設計に無い）。leak probe（書き出しと JSON 応答に絶対パスが無い）を満たすため。手元の Mac のパスなど、VM の外のパスは畳まない。
8. **書き出し先は裁定 (a) で決着**: 既定の行き先を持たず `--to` 必須。help・管理者 skill・リリースノート・設計書 §1・§3 を「私有の置き場へ。公開 repo には置かない」に直した（`df442d6`）。
9. **退出（`thth account leave`）は報告を消さない**（裁定 (c) でそのまま）。件 6 で `thth/leave.py` を触ったが、報告の扱いは変えていない。
10. **断りの案内の 1 行は、CLI の JSON の断り（`read_coordination.refusal` 等）には足していない**。stderr の理由行の後ろに 1 行だけ。`report` 自身の断りの JSON には `report_channel` を入れた。既存の JSON の完全一致の試験 4 本と 3.1.1 との衝突を避けた。
11. `thth admin reports list` の既定は `--status open`、利用者の `thth report list` の既定は `all`（閉じた報告の返事も読めるように）。
12. サーバ型 MCP の `thth_report_file` は `actor` の無い credential では `by_required` で断る。読むだけの credential に `actor` が無い運用なら、置けるよう credential に `actor` を足す必要がある。
13. 3.1.1 との合流は文字の衝突なしで済んだ（上の「合流」）。3.1.1 で増えた試験に stderr の完全一致で落ちるものは無かった。
14. **件 6 で自分の token file は消さずに残る**（`preserved: ['token_shared']`）。依頼文は「自分の token file の削除まで完了」と「`targets` から `token` は既に外れている」の両方を含んでいたので、後者（`token` が消す対象に残るのは矛盾）に合わせた。値が同じなら消しても失効にはならないので、自分の file だけは消す（同じファイル・同じ inode の共有とは分けて扱う）形も取れる。そうするかは裁定待ち。
15. **件 6 は、`token_shared` と記録された退出を、今は共有でなくなっていても遠隔で失効させない**（相手が先に退出した場合など）。保守側に倒した。

## VM で打ってみる 5 行（未確認・版を上げたあと）

```bash
printf '試しの報告です' | thth report file kopicha-threads --kind request --title 試し --body-file - --by masaru --json
thth admin reports list --json
thth morning kopicha --no-mark
thth account leave kopicha-server-mastodon --by masaru      # 件 6: 止まっている退出を打ち直す
thth unanswered kopicha-threads --json                      # 件 7: 件数が戻る
```
