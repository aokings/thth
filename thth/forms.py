"""投稿の形の語彙と、形ごとの実測（設計 §8・§9・工程 7〜8）。

**軸を 2 つに分ける**（Codex 指摘）。第 1 稿の語彙は「列挙」「問い→答え」が
**構成**、「誘導」が**目的**で混ざっていた。別々に記録すると比較できる
（「問い→答え × 記事へ」と「列挙 × 記事へ」を並べられる）。

**`form` / `outlet` は承認対象ではない**（設計 §8.3・Codex 最終条件 4）。
既にある本文を**分類するだけ**のラベルなので、変えても公開される内容は
変わらない。**`--advise` への影響だけでは承認対象にしない。**
ただし**実測に使った版を記録する**——ラベルの履歴は、時刻だけでなく
**当時の値と分類の版**を保存する。

**計測でやってはいけないこと**（設計 §9）は `OUTCOME_RULES` に書いてある。
コードのコメントではなく**出力に出す**——読む人が毎回見る場所に置く。
"""
from __future__ import annotations

VOCABULARY_VERSION = "2026-09-11.4"

# 構成（**各段が何を渡すか**）。
#
# **軸を入れ替えた**（Codex の実地調査 2026-09-11・masaru 裁定）。
# 以前の語彙（単発 / 比較 / 列挙 / 問い→答え / 手順）は**文章の形**を名前に
# していたが、**3 セッションが 3 本書いて 3 本とも `問い→答え` を選んだ**——
# 中身も狙いも違う 3 本が同じ箱に落ちた。**分類が何も分けていない。**
#
# Codex は Threads の**実際の 6 本・18 投稿**から 5 型を抽出した。どれも
# 「A → B → C」の形で、**書く人が実際に決めること**（次の段に何を置くか）が
# 名前に入っている。「問い→答え」では 2 段目に何を書くか決まらないが、
# 「困り事 → 理由 → 行動」なら決まる。
#
# `単発` と `列挙` は Codex の 5 型に無いので足した——`単発` は既定で
# いちばん多いはずのもの、`列挙` は**masaru が最初に持ってきた実例
# （「対策 10 選」）がまさにこの型**で、Codex の標本 6 本に入らなかっただけ。
FORMS = {
    "単発": "**分けない。** 1 投稿で完結する。**いちばん多いはず。**",
    "概要→実用詳細": "1: 得られるものと要点／2: 手順・条件 ＋ 詳細 URL。"
                       "**読者が実行に使う情報があるとき。**",
    "体験→再現方法": "1: 実際の体験・結果／2: 条件や道具／3: 手順 ＋ 記事。"
                       "**実体験と条件が原資料にあるときだけ**"
                       "——**体験を創作しない。**",
    "困り事→理由→行動": "1: 困り事と答えの要点／2: 理由・根拠・限界／"
                            "3: 試すこと ＋ 記事。行動まで導けないなら"
                            "「分かったこと」で閉じる。",
    "具体例→解釈→応用": "1: 具体的な事例／2: 観察と解釈／3: 他の場面での"
                            "使い方 ＋ 記事。**観察した事実と筆者の推論を分ける。**",
    "比較→条件→選択": "1: 選択肢と結論の条件／2: 同じ軸での比較／"
                         "3: どちらを選ぶか ＋ 記事。**優劣を過度に一般化しない。**",
    "列挙": "数えられるものを並べる（「10 選」など）。先頭で何を並べるかを言う。",
}

# 旧語彙 → 新語彙（**書き直しを要らなくするため**）。
# `VOCABULARY_VERSION` が上がるので、どの版で測ったかは追える。
FORM_MIGRATION = {
    "問い→答え": "困り事→理由→行動",
    "手順": "概要→実用詳細",
    "比較": "比較→条件→選択",
}

# **記事 URL を付ける投稿の基本案**（Codex §4）。
BASE_SHAPE = "結論と対象読者 → 根拠・条件 → 使い方と記事 URL"

# **連投にするか決める前に答える 5 つ**（Codex §8）。
# 「記事 URL があるから連投」にしないための関門。
BEFORE_YOU_SPLIT = [
    "誰が、何を知りたい記事か。",
    "**単発に収まらず、連投にすると分かりやすくなる理由は何か。**",
    "どの型を選び、**各段で何を一つずつ渡すか。**",
    "どの主張が記事のどこに支えられ、**何が不明か。**",
    "主目的は理解・会話・記事閲覧のどれか。",
]

# 段の番号を本文に書いたか（asmon 関東セッション要望 2026-09-11）。
#
# > 番号の有無を `form` と一緒に記録するなら、**原稿の front-matter に欄が
# > ほしい**です。…経緯のメモに書いても THTH は読みません。
#
# **記録する場所が無ければ、比べられない。** 2 セッションとも独立に
# 「書かない」を選んだが、**理由が違った**——
#   nigamilab: 1 段目で止まる人に「これは途中です」と思わせるのは損
#   asmon 関東: 番号が付くとその段が単独で読めなくなる
# **どちらが効くかは実測が無い。** だから欄を作って残す。
NUMBERING = {
    "あり": "本文に `1/3` のような番号を書いた",
    "なし": "書かなかった。**各段が単独で読める形**にしたとき",
}

# 最後の導線（読んだ人をどこへ渡すか）
OUTLETS = {
    "記事へ": "最終段で自分の記事に渡す",
    "別投稿へ": "最終段で自分の別の投稿を引用する",
    "無し": "その投稿の中で完結する。**渡さないのも選択。**",
}

# **実測ゼロでも渡せる判断材料**（設計 §8.2・Codex 指摘）。
# 「使いこなせる」を実測待ちにすると満たせない。
GUIDANCE = [
    "**分けるかどうかを決める問いは 1 つ: 1 段目で止まった人にとっても有益か。**"
    "満たせないなら、その連投は**有益さを演じているだけ**。",
    "**引っぱってよいのは答え。引っぱってはいけないのは留保。**"
    "続きが気になるのは本物の効果だが、**止まった人が間違ったことを"
    "持ち帰る形にしない**。",
    "**削れるのが飾りなら削る、削れるのが根拠なら分ける。**"
    "「500 字に収まらない」は分ける理由にならない。",
    "**投稿の切れ目を意味の切れ目に合わせる。** 文字数の上限に達した場所で"
    "文を切らず、結論・根拠・方法の単位で分ける。",
    "**中段は新しい情報を渡す。** 結論の繰り返しではなく、理由・比較・"
    "具体例・適用条件を置く。",
    "**末尾で記事を読む理由が分かるようにする。** 「詳しくはこちら」だけに"
    "しない。**記事にない内容を約束しない。**",
    "**原記事の根拠を超えない。** 数字・体験・引用・因果関係を補わない。",
    "**信号（返信数・段ごとの配信）は動機になりうるが、うちは未検証。**"
    "「連投のほうが伸びるらしい」を理由にしない。",
]

# **計測でやってはいけないこと**（設計 §9・Codex 最終条件）。
OUTCOME_RULES = [
    "**全段の views を足して「到達人数」と呼ばない**（同じ人が複数段を見る）。",
    "**最終段 ÷ 先頭段を「読了率」と呼ばない**（views の定義がそれを支えない）。",
    "**取得できない記事クリックを views で代用しない**"
    "（`clicks` は Threads API から返ってこない）。",
    "投稿単位のまま保持し、束へ紐付ける。**束として言えるのは各段の数まで。**",
]


def label_error(form, outlet, numbering=None, *, avoid_forms=None) -> str | None:
    """ラベルの値を検査する。**知らない語は推測で通さない。**

    `avoid_forms` は**その account の profile が「使わない」と宣言した型**
    （masaru 裁定 2026-09-11）。**流行っているからで編集方針が溶けない**ように
    する。宣言が無ければ何も制限しない。
    """
    if form is not None and form not in FORMS:
        old = FORM_MIGRATION.get(form)
        hint = f"（`{form}` は旧語彙です。いまは `{old}`）" if old else ""
        return (f"form: 知らない語です（{form}）{hint}。"
                f"使えるのは: {'・'.join(FORMS)}")
    if form is not None and form in (avoid_forms or ()):
        return (f"form: この account は `{form}` を使わないと profile に"
                f"宣言しています（avoid_forms）。**型を変えるか、"
                f"profile を masaru に諮って変えてください。**")
    if outlet is not None and outlet not in OUTLETS:
        return (f"outlet: 知らない語です（{outlet}）。"
                f"使えるのは: {'・'.join(OUTLETS)}")
    if numbering is not None and numbering not in NUMBERING:
        return (f"numbering: 知らない語です（{numbering}）。"
                f"使えるのは: {'・'.join(NUMBERING)}")
    return None


_NUMBER_RE = __import__("re").compile(r"\d+\s*/\s*\d+")


def numbering_warning(numbering, segments: list) -> str | None:
    """名乗ったラベルと本文が食い違っていないか。**警告まで。**

    ラベルは承認の対象ではないので**止めない。** ただし食い違ったまま測ると、
    **番号ありと番号なしを取り違えて比べる**ことになる。
    """
    if numbering is None or not segments:
        return None
    found = any(_NUMBER_RE.search(seg or "") for seg in segments)
    if numbering == "あり" and not found:
        return ("warning: numbering: 「あり」と書いてありますが、本文に "
                "`1/3` のような番号が見つかりません")
    if numbering == "なし" and found:
        return ("warning: numbering: 「なし」と書いてありますが、本文に "
                "`N/M` の形が含まれています（本文の一部なら無視してください）")
    return None


def missing_form_warning(form, segments: list) -> str | None:
    """**連投なのに型を名乗っていない**（運用セッション提案 2026-09-11）。

    > `thth: 1` は `単発` と等価なので、**lint が `thth: 2` 以上にだけ
    > `form:` を要求する**形にできませんか。いまは要求されないので書かれず、
    > 書かれないので集まりません。

    そのとおりなので足した。**ただし警告まで**（`warning:` 付き）。

    **止めない理由は Codex §11。** 「診断結果、型、profile を公開時の必須条件に
    する変更は別設計・別レビュー」。lint の実エラーは `thth approve` を止める
    ので、型を必須にすると**編集の分類が公開のゲートになる。** 型を書いて
    ほしいことと、型が無いと出せないことは別。

    単発（`thth: 1`）には要求しない——**定義上すべて `単発` になる**ので、
    書いても `thth` の値から機械的に導ける以上のことを言わない
    （運用セッションが queue 86 本を数えて確かめた: 84 本が `thth: 1`）。
    """
    if form is not None or not segments or len(segments) < 2:
        return None
    return ("warning: form: 連投なので型を書いてください"
            f"（{'・'.join(FORMS)}）。**書くときに選ぶもの**で、"
            "あとから貼るものではありません。"
            "**これは警告です——型が無くても承認・公開は止まりません。**")


def label_record(*, form, outlet, at: str, numbering=None) -> dict:
    """ラベルの履歴 1 件（Codex 最終条件 4）。

    **時刻だけでなく、当時の値と分類の版を保存する。**
    語彙が増えたあとに読み返しても「どの版の分類で測ったか」が分かる。
    """
    return {"at": at, "form": form, "outlet": outlet, "numbering": numbering,
            "vocabulary_version": VOCABULARY_VERSION}


def bundle_outcome(run: dict, measured_by_post: dict) -> dict:
    """束の実測（設計 §9）。**投稿単位のまま保持して、束へ紐付ける。**

    `measured_by_post` は `{post_id: {"views": n, ...}}`。

    **足さない。割らない。** 返すのは段ごとの数と、その並びだけ。
    どう読むかは人が決める——道具が「到達人数」や「読了率」という**名前を
    付けた時点で、その名前が事実として一人歩きする。**
    """
    rows = []
    for post in run.get("posts", []):
        post_id = post.get("post_id")
        rows.append({
            "index": post["index"],
            "post_id": post_id,
            "state": post["state"],
            "measured": measured_by_post.get(post_id) if post_id else None,
        })
    return {
        "run_id": run.get("run_id"),
        "account": run.get("account"),
        "rel_path": run.get("rel_path"),
        "root_post_id": run.get("root_post_id"),
        "posts": rows,
        "notice": "／".join(OUTCOME_RULES),
    }


def advise() -> dict:
    """形を選ぶ前に読むもの（設計 §8.2）。

    **実測がまだ無いことを隠さない。** 「この形式で成果が出るかは未検証」を
    毎回出す——出さないと、**根拠のない型が権威を持つ**（トピックで
    「一般名詞なら安全」と思い込んで 0 件を踏んだのと同じ罠）。
    """
    return {
        "vocabulary_version": VOCABULARY_VERSION,
        "forms": FORMS,
        "form_migration": FORM_MIGRATION,
        "base_shape": BASE_SHAPE,
        "before_you_split": BEFORE_YOU_SPLIT,
        "outlets": OUTLETS,
        "numbering": NUMBERING,
        "guidance": GUIDANCE,
        "measured": {},          # **空。まだ 1 本も出していない。**
        "notice": "形ごとの実測はまだありません。"
                   "**この形式で成果が出るかは未検証です。**"
                   "テンプレートは書くための補助であって、成果の保証ではありません。",
        "outcome_rules": OUTCOME_RULES,
    }
