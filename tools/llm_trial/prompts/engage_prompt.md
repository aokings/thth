`thth` is installed in this venv. The account `demo-bluesky` wants to join a public
conversation about "コーヒー". Find where people are talking, read the thread before
you write, check whether we already replied, draft one reply as a queue file with
`reply_to`, and stop at the approval step—ask me for approval showing the digest. Do
not post for real.
